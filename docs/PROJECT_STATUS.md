# プロジェクト状況

最終更新: 2026-08-19

## 現在地

- 現在のフェーズ: 1「状態・数学ロジックの基礎」
- 完了した作業単位: 0.1、0.2、1.1「プロジェクト基盤を作る」
- 次の作業単位: 1.2「数学ロジックの最小版を作る」
- 配布・公開: 未実施。ローカルファイルの編集のみを行う
- Git 操作: コミット、プッシュともに未実施

## 決定済みの方針

- D-001〜D-008 は 2026-08-19 に提案どおり承認された。
- 初版は旧 MATLAB 教材相当の 2D/3D ベクトル、span、一次独立・一次従属、共有 URL に限定する。
- TypeScript、Vite、React の完全クライアントサイド静的アプリとする。
- 2D は SVG、3D は Three.js を使用する。D3 と math.js は必要性が明確になるまで追加しない。
- 2D の数値入力と先端ドラッグ、span 選択、rank と span 次元の説明表示を採用する。
- 共有状態はバージョン付き JSON を Base64URL 形式で格納する。
- Reset はページ読み込み時の `InitialState` へ戻す。

## 今回の変更

- D-001〜D-008 を `docs/DECISIONS.md` と `SPEC.md` で決定済みへ更新した。
- TypeScript、Vite、React、Vitest、pnpm のプロジェクト基盤を追加した。
- `src/domain`、`src/sharing`、`src/state`、`src/visualization`、`src/ui` を分離した。
- 教材名、初版範囲、現在の開発段階を示す最小画面を追加した。
- ローカル開発、単体テスト、静的ビルドのスクリプトを追加した。

## 検証結果

- 自動テスト: 1 ファイル、1 テスト成功
- TypeScript の型検査: 成功
- 静的ビルド: 成功
- ローカル HTTP 応答: 200 を確認

現時点のテストは基盤確認用であり、数学的な正しさを保証するテストは作業単位 1.2 で追加する。

## 次に行うこと

作業単位 1.2 に限定し、描画や UI に依存しない数学ロジックを実装する。最初にベクトル集合の入力モデルと rank 計算の数値方針を文書化し、次の代表例をテストへ固定する。

- 0 ベクトルのみ
- 重複またはスカラー倍のベクトル
- 2D の独立な 2 ベクトル
- 3D の独立な 2 ベクトルと 3 ベクトル
- ベクトル数が空間の次元を超える集合
- 許容誤差付近のほぼ一次従属な集合

## 既知のリスクと後続確認

- rank 判定の許容誤差を決めないまま UI を作ると、視覚表示と数学的判定が矛盾する可能性がある。
- URL 長上限と学内サーバーのベースパス・URL 書き換え条件は未確認である。
- 3D カメラ状態を共有対象へ含めるかは 3D 実装時に判断する。
- 3D のベクトル編集とカメラ操作はジェスチャーが競合するため、試作による確認が必要である。
