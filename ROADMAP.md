# 開発ロードマップ

## 1. 方針

本ロードマップは固定された納期表ではなく、実機確認と授業上の評価を挟みながら更新する作業順序である。各フェーズをさらに小さな「作業単位」に分け、原則として一つの作業単位ごとに利用者へ確認可能な成果を渡す。

最初から 15 週分の機能を作り込まず、旧 MATLAB 教材の中核目的と状態共有 URL を満たす最小版を先に完成させる。追加 Lab は、その最小版を授業で評価した後に優先順位を決める。

## 2. フェーズ一覧

| フェーズ | 目的 | 主な成果 | 終了時の確認ゲート | 状態 |
|---|---|---|---|---|
| 0. 要件と進め方の確立 | 仕様・授業目標・作業方法を揃える | ロードマップ、状態記録、意思決定記録 | 初版の範囲と技術方針を承認できる | 完了 |
| 1. 状態・数学ロジックの基礎 | 描画に依存しない正しい中核を作る | 状態型、rank/span 次元判定、共有状態の往復、単体テスト | 代表例・境界例と URL 往復テストが通る | 完了 |
| 2. 2D の最小縦断版 | 学生が触れる最初の教材体験を作る | 2D 座標、複数ベクトル、編集、span、判定表示、共有 URL | ブラウザで一連の操作と再現を確認できる | 進行中（2.1 完了） |
| 3. 2D 教材版の完成 | 授業で試せる品質へ整える | 追加・削除、エラー案内の仕上げ、レスポンシブ、アクセシビリティ基礎 | PC とタッチ端末相当で授業シナリオを完走できる | 未着手 |
| 4. 3D 教材版 | 旧教材の 3D 学習目的を移植する | 3D ベクトル、視点操作、直線・平面・全空間の span 表示 | 3D の代表例を操作・共有・復元できる | 未着手 |
| 5. 共有と配布の仕上げ | 実運用に耐える形にする | 不正 URL 処理、URL 互換方針、QR 運用確認、GitHub Pages／nginx 配布手順 | 選定した公開先で共有 URL を実機確認できる | 未着手 |
| 6. 初版評価と棚卸し | 初版の学習効果と保守性を確認する | 授業用例、課題一覧、仕様・テスト・文書の全体照合 | 初版完了と次期範囲を判断できる | 未着手 |
| 7. 授業拡張 | シラバス後半へ段階的に対応する | 基底・次元、線形写像、固有値、内積等の候補 Lab | 各 Lab ごとに教育目的と受入条件を個別承認する | 保留 |

## 3. 直近の作業単位

### 0.1 文書の基準点を作る

- [x] `SPEC.md` と `syllabus.txt` を確認する。
- [x] 旧 MATLAB 教材の機能と操作負荷を確認する。
- [x] ロードマップ、現在地、意思決定の記録場所を作る。
- [x] シラバス上の第 8 週が中間試験であることを仕様へ反映する。

### 0.2 初版の境界と技術方針を決める

- [x] `docs/DECISIONS.md` の D-001〜D-008 をレビューする。
- [x] 承認された内容を `SPEC.md` の決定済み事項へ反映する。
- [x] フェーズ 1 の正確な完了条件と最初のテストケースを確定する。

### 1.1 プロジェクト基盤を作る

- [x] 承認済みの技術構成でローカル開発・テスト・静的ビルドの最小構成を作る。
- [x] 数学ロジック、共有状態、UI、描画を分離するディレクトリを作る。
- [x] 空の画面ではなく、教材名と現在の開発段階が分かる最小画面を表示する。

### 1.2 数学ロジックの最小版を作る

- [x] 2D/3D の小規模なベクトル集合について rank を求める。
- [x] span の次元と一次独立・一次従属を一貫した結果として返す。
- [x] 0 ベクトル、重複、平行、ほぼ従属な入力の扱いをテストで固定する。

### 1.3 共有状態の最小版を作る

- [x] 状態スキーマを検証する。
- [x] `decode(encode(state))` の往復をテストする。
- [x] 壊れた入力、過大な入力、次元不整合を安全に拒否する。

フェーズ 1 の途中でも、1.1、1.2、1.3 の各完了時に変更内容を確認してから次へ進む。

## 4. フェーズ 2 の作業単位

### 2.1 固定例の 2D 表示

- [x] 状態から 2D の表示範囲、座標軸、目盛線を構築する。
- [x] 固定された複数ベクトルを矢印とラベルで識別可能に表示する。
- [x] 既存の数学ロジックを接続し、rank、span の次元、一次独立・一次従属を表示する。
- [x] この作業単位では入力、span 描画、共有 UI を追加しない。

### 2.2 数値入力と自動スケール

- [x] `CurrentState` を導入し、2 本のベクトルの x・y 座標を数値入力で編集できるようにする。
- [x] 入力途中の文字列と確定済み数値を分離し、不正値を数学ロジックへ渡さない。
- [x] 有効な変更をベクトル、座標一覧、行列の rank、生成する空間の次元、判定文へ反映する。
- [x] 数ベクトルを列ベクトルとして表示し、ベクトル記号の基本文字をボールド斜体小文字、添え字の数字を立体レギュラーで統一する。
- [x] 列ベクトルを並べた行列を `A` として画面上で定義し、`rank(A)` と「ベクトルが生成する空間」の `dim` を区別して対応付ける。
- [x] 原点と全ベクトルの終点が余白付きで収まる正方形の表示範囲を自動計算する。
- [x] 自動範囲は `[-5, 5] × [-5, 5]` を最小の基準範囲とし、範囲外入力では拡大し、値が小さくなれば基準範囲まで縮小する。
- [x] 表示倍率に応じて、目盛間隔を `1`、`2`、`5` と 10 の累乗の組合せから選び、目盛が過密にならないようにする。
- [x] この作業単位では手動ズーム、パン、先端ドラッグ、生成する空間の描画、共有 UI を追加しない。

### 2.3 手動の表示範囲操作（提案）

- [ ] マウスホイール／トラックパッドとタッチ端末のピンチで、操作位置を中心にズームする。
- [ ] 座標面の背景ドラッグとタッチ操作でパンできるようにする。
- [ ] キーボードでも利用できる拡大、縮小、「全体を表示」ボタンを設ける。
- [ ] 初期状態は自動追従とし、手動操作後は手動表示を維持する。「全体を表示」で自動追従へ戻す。
- [ ] 極端な倍率、非有限値、表示不能な範囲を防ぐ上下限を設ける。具体値は操作テストで調整する。
- [ ] この作業単位ではベクトル先端のドラッグを追加せず、表示範囲の操作だけを確認する。

### 2.4 ベクトル先端のドラッグ（提案）

- [ ] マウスとタッチでベクトル先端をドラッグし、数値入力と解析結果へ同期する。
- [ ] ベクトルの操作領域と座標面のパン領域を明確に分け、ジェスチャーの競合を避ける。
- [ ] 自動追従中はドラッグ開始時の表示範囲を操作終了まで固定し、終了後に全体が収まる範囲を再計算する。
- [ ] 手動表示中は利用者の表示範囲を維持し、ベクトル編集で勝手に自動追従へ戻さない。
- [ ] D-009 のほぼ平行な判定閾値と、ドラッグ時の操作感を再評価する。

### 2.5 生成する空間（span）の選択と幾何表示（提案）

- [ ] 各ベクトルを「生成する空間（span）」の対象に含めるか、キーボード・マウス・タッチで選択できるようにする。
- [ ] 選択集合の rank が 0 のときは原点、1 のときは原点を通る直線、2 のときは座標平面全体として表示する。
- [ ] 判定カードが「表示中の全ベクトル」と「span に選択した集合」のどちらを説明しているかを明示する。
- [ ] 色だけに依存せず、線種、ラベル、説明文を併用する。

### 2.6 共有 URL、復元、Reset（提案）

- [ ] 2D の表示モードと手動表示範囲を共有対象にする場合のスキーマ版を確定する。
- [ ] 現在状態から共有 URL を生成し、クリップボードへコピーできるようにする。
- [ ] 別セッションで数学的状態、span 選択、教材上意味のある表示状態を復元する。
- [ ] 不正な共有状態を安全に拒否し、復元できなかったことを画面へ表示する。
- [ ] `DefaultState`、`InitialState`、`CurrentState` を接続し、Reset で `InitialState` へ戻す。

### 2.7 フェーズ 2 統合確認（提案）

- [ ] 数値入力、自動スケール、手動表示操作、ドラッグ、span、共有、Reset の一連の操作を確認する。
- [ ] PC のマウス・キーボードとタッチ端末相当の操作を確認する。
- [ ] D-009 の rank 許容誤差、D-012 の基準範囲、D-013・D-014 の表示範囲方針を再評価する。
- [ ] 学生向け画面の用語と数式表記を `math-writing-rules.txt` に照らして棚卸しする。
- [ ] 確認結果をフェーズ 3 の視認性、アクセシビリティ、入力エラー改善へ反映する。

2.2〜2.7 の分割と詳細は利用者確認前の提案であり、承認後に実装順として確定する。

3D は 2D で確立した状態・数学・共有の仕組みを再利用し、カメラ操作と平面表示に固有の問題へ作業範囲を限定する。

## 5. 文書更新と棚卸し

各作業単位の終了時に、少なくとも次を確認する。

- `docs/PROJECT_STATUS.md`: 完了事項、検証結果、次の一手を更新する。
- `docs/DECISIONS.md`: 新しい決定、変更、却下理由を記録する。
- `SPEC.md`: 要件または受入条件が変わった場合だけ更新する。
- `ROADMAP.md`: 順序、範囲、確認ゲートが変わった場合に更新する。

文書全体の棚卸しは、各フェーズ終了時、または小さな作業単位を 3 個完了するごとに行う。利用者から依頼があった場合は、この周期を待たずに実施する。棚卸しでは、仕様と実装、受入条件とテスト、ロードマップと現在地、文書間リンク、古くなった保留事項を照合する。

## 6. 初版完了の目安

初版は、`SPEC.md` の CORE-01〜CORE-07 と AC-01〜AC-12 を満たし、学内サーバー相当の静的配信で 2D/3D の代表例を共有・復元できた時点とする。授業後半の追加 Lab は初版完了条件に含めず、フェーズ 6 の評価後に改めて選定する。
