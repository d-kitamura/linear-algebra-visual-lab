# 開発ロードマップ

## 1. 方針

本ロードマップは固定された納期表ではなく、実機確認と授業上の評価を挟みながら更新する作業順序である。各フェーズをさらに小さな「作業単位」に分け、原則として一つの作業単位ごとに利用者へ確認可能な成果を渡す。

最初から 15 週分の機能を作り込まず、旧 MATLAB 教材の中核目的と状態共有 URL を満たす最小版を先に完成させる。この初版候補はフェーズ6までに完成した。授業実施が先であるためフェーズ7の評価は延期し、シラバスとの依存関係と既存実装の再利用性を確認しながら、フェーズ8以降で追加 Lab を一つずつ設計・実装する。

## 2. フェーズ一覧

| フェーズ | 目的 | 主な成果 | 終了時の確認ゲート | 状態 |
|---|---|---|---|---|
| 0. 要件と進め方の確立 | 仕様・授業目標・作業方法を揃える | ロードマップ、状態記録、意思決定記録 | 初版の範囲と技術方針を承認できる | 完了 |
| 1. 状態・数学ロジックの基礎 | 描画に依存しない正しい中核を作る | 状態型、rank/span 次元判定、共有状態の往復、単体テスト | 代表例・境界例と URL 往復テストが通る | 完了 |
| 2. 2D の最小縦断版 | 学生が触れる最初の教材体験を作る | 2D 座標、複数ベクトル、編集、span、判定表示、共有 URL | ブラウザで一連の操作と再現を確認できる | 完了 |
| 3. 2D 教材版の完成 | 授業で試せる品質へ整える | 追加・削除、エラー案内の仕上げ、レスポンシブ、アクセシビリティ基礎 | PC とタッチ端末相当で授業シナリオを完走できる | 完了 |
| 4. 2D 一次結合エクスプローラ | 生成する空間と一次結合の存在・一意性を結び付ける | ターゲットベクトル、係数、2例・一般解、平行四辺形、共有 URL | 0〜8本の選択集合で「解なし・一意・無数」を操作・共有できる | 完了（4.1〜4.6・利用者確認済み） |
| 5. 3D 教材版 | 旧教材の 3D 学習目的と一次結合エクスプローラを3Dへ拡張する | 3D ベクトル、視点操作、直線・平面・全空間の span 表示、一次結合 | 3D の代表例を操作・共有・復元できる | 完了（5.1〜5.8・利用者確認・統合棚卸し済み） |
| 6. 共有と配布の仕上げ | 実運用に耐える形にする | 不正 URL 処理、URL 互換方針、QR 運用確認、GitHub Pages／nginx 配布手順 | 選定した公開先で共有 URL を実機確認できる | 完了 |
| 7. 初版評価と棚卸し | 初版の学習効果と保守性を確認する | 授業用例、課題一覧、仕様・テスト・文書の全体照合 | 初版完了と次期範囲を判断できる | 授業実施まで延期 |
| 8. 複数Lab基盤と基底・次元Lab | 現行教材を複数Labへ拡張し、基底・次元・座標を学べるようにする | 共通Lab選択、Lab別共有状態、基底・次元判定、座標エクスプローラ | 現行Labを回帰し、基底の2条件と座標の一意性を操作・共有できる | 完了（8.1〜8.7・利用者確認・統合棚卸し済み） |
| 9. 線形写像Lab | 入力と像の対応から核・像・ランク・退化次数・次元定理を理解できるようにする | 定義域／終域の連動図、行列による写像、核・像、次元定理、共有状態 | 低次元の代表例を操作し、写像の変化と核・像・次元の関係を説明・共有できる | 完了（9.1〜9.7・利用者確認・統合棚卸し済み） |
| 10. 0・1次元空間の基礎拡張 | 2D/3Dより前の境界例を既存3Labへ補い、次元0・1の意味を操作と定義から理解できるようにする | 1D数直線、0D一点表示、空の基底、0D/1D線形写像、既存共有状態の拡張 | 0〜3次元でspan・基底・線形写像・次元定理が一貫し、既存2D/3Dを回帰できる | 進行中（10.1承認済み、10.2実装・利用者確認待ち。D-082・D-083） |
| 11. 表現行列・基底変換Lab | 同じ線形写像でも基底と順序により行列表示が変わることを、数ベクトルと多項式の両方で理解できるようにする | 順序付き定義域／終域基底、像の座標、表現行列、座標の可換関係、基底変換、多項式空間上の線形写像 | 低次元例で表現行列を列ごとに構成し、基底変更前後の同じ写像を数ベクトル・多項式で説明・共有できる | 詳細ロードマップ更新・利用者確認待ち（D-081・D-082） |

## 3. 直近の作業単位

### 0.1 文書の基準点を作る

- [x] `SPEC.md` と `syllabus.txt` を確認する。
- [x] 旧 MATLAB 教材の機能と操作負荷を確認する。
- [x] ロードマップ、現在地、意思決定の記録場所を作る。
- [x] シラバス上の第 8 週が中間試験であることを仕様へ反映する。

### 0.2 初版の境界と技術方針を決める

- [x] `docs/DECISIONS.md` の D-001〜D-008 をレビューする。
- [x] 承認された内容を `SPEC.md` の決定済み事項へ反映する。
- [x] フェーズ 1 の正確な完了条件と最初のテストケースを確定する。

### 1.1 プロジェクト基盤を作る

- [x] 承認済みの技術構成でローカル開発・テスト・静的ビルドの最小構成を作る。
- [x] 数学ロジック、共有状態、UI、描画を分離するディレクトリを作る。
- [x] 空の画面ではなく、教材名と現在の開発段階が分かる最小画面を表示する。

### 1.2 数学ロジックの最小版を作る

- [x] 2D/3D の小規模なベクトル集合について rank を求める。
- [x] span の次元と一次独立・一次従属を一貫した結果として返す。
- [x] 0 ベクトル、重複、平行、ほぼ従属な入力の扱いをテストで固定する。

### 1.3 共有状態の最小版を作る

- [x] 状態スキーマを検証する。
- [x] `decode(encode(state))` の往復をテストする。
- [x] 壊れた入力、過大な入力、次元不整合を安全に拒否する。

フェーズ 1 の途中でも、1.1、1.2、1.3 の各完了時に変更内容を確認してから次へ進む。

## 4. フェーズ 2 の作業単位

### 2.1 固定例の 2D 表示

- [x] 状態から 2D の表示範囲、座標軸、目盛線を構築する。
- [x] 固定された複数ベクトルを矢印とラベルで識別可能に表示する。
- [x] 既存の数学ロジックを接続し、rank、span の次元、一次独立・一次従属を表示する。
- [x] この作業単位では入力、span 描画、共有 UI を追加しない。

### 2.2 数値入力と自動スケール

- [x] `CurrentState` を導入し、2 本のベクトルの x・y 座標を数値入力で編集できるようにする。
- [x] 入力途中の文字列と確定済み数値を分離し、不正値を数学ロジックへ渡さない。
- [x] 有効な変更をベクトル、座標一覧、行列の rank、生成する空間の次元、判定文へ反映する。
- [x] 数ベクトルを列ベクトルとして表示し、ベクトル記号の基本文字をボールド斜体小文字、添え字の数字を立体レギュラーで統一する。
- [x] 列ベクトルを並べた行列を `A` として画面上で定義し、`rank(A)` と「ベクトルが生成する空間」の `dim` を区別して対応付ける。
- [x] 原点と全ベクトルの終点が余白付きで収まる正方形の表示範囲を自動計算する。
- [x] 自動範囲は `[-5, 5] × [-5, 5]` を最小の基準範囲とし、範囲外入力では拡大し、値が小さくなれば基準範囲まで縮小する。
- [x] 表示倍率に応じて、目盛間隔を `1`、`2`、`5` と 10 の累乗の組合せから選び、目盛が過密にならないようにする。
- [x] この作業単位では手動ズーム、パン、先端ドラッグ、生成する空間の描画、共有 UI を追加しない。

### 2.3 手動の表示範囲操作

- [x] マウスホイール／トラックパッドとタッチ端末のピンチで、操作位置を中心にズームする。
- [x] 座標面の背景ドラッグとタッチ操作でパンできるようにする。
- [x] キーボードでも利用できる拡大、縮小、「全体を表示」ボタンを設ける。
- [x] 初期状態は自動追従とし、手動操作後は手動表示を維持する。「全体を表示」で自動追従へ戻す。
- [x] 極端な倍率、非有限値、表示不能な範囲を防ぐ上下限を設ける。具体値は操作テストで調整する。
- [x] この作業単位ではベクトル先端のドラッグを追加せず、表示範囲の操作だけを確認する。

### 2.4 ベクトル先端のドラッグ

- [x] マウスとタッチでベクトル先端をドラッグし、数値入力と解析結果へ同期する。
- [x] ベクトルの操作領域と座標面のパン領域を明確に分け、ジェスチャーの競合を避ける。
- [x] 自動追従中はドラッグ開始時の表示範囲を操作終了まで固定し、終了後に全体が収まる範囲を再計算する。
- [x] 手動表示中は利用者の表示範囲を維持し、ベクトル編集で勝手に自動追従へ戻さない。
- [x] D-009 の判定閾値は `1e-10` を維持し、ドラッグ操作の「平行に合わせにくい」問題を別の平行スナップで解決する。
- [x] D-019 の画面幅1%の平行スナップについて、異なる表示倍率での吸着範囲、視覚表示、外れるときの感触を利用者が確認する。

### 2.5 生成する空間（span）の選択と幾何表示

- [x] 各ベクトルを「生成する空間（span）」の対象に含めるか、キーボード・マウス・タッチで選択できるようにする。
- [x] 選択集合の rank が 0 のときは原点、1 のときは原点を通る直線、2 のときは座標平面全体として表示する。
- [x] 判定カードが「表示中の全ベクトル」と「span に選択した集合」のどちらを説明しているかを明示する。
- [x] 色だけに依存せず、線種、ラベル、説明文を併用する。
- [x] D-020 の選択操作、初期表示、rank別の幾何表現と説明を利用者が確認する。
- [x] D-021 に基づき、選択集合と全ベクトルの要約を常時表示し、3つの詳細カードをタブで切り替えるレスポンシブ構成へ変更する。
- [x] D-021 のPC・タブレット・スマートフォン配置とタブ操作を利用者が確認する。

### 2.6 共有 URL、復元、Reset

- [x] D-023により手動表示範囲を共有対象外とし、共有状態v1を維持する。
- [x] 現在状態から共有 URL を生成し、選択、クリップボードコピー、テキスト保存ができるダイアログを追加する。
- [x] 別セッションで数学的状態、span 選択、幾何表示を復元し、表示範囲を自動計算する。
- [x] 不正・重複・未対応の共有状態を安全に拒否し、既定例へのフォールバックと案内を画面へ表示する。
- [x] `DefaultState`、`InitialState`、`CurrentState` を接続し、Resetで入力欄と一時UIを含めて`InitialState`へ戻す。
- [x] `state`パラメーター、公開サブパス維持、閲覧中URLと履歴を変更しない規則をテストする。
- [x] D-022〜D-024のエクスポート、コピー、テキスト保存、別URLでの復元、不正URL、Resetを利用者が確認する。

### 2.7 フェーズ 2 統合確認

- [x] 数値入力、自動スケール、手動表示操作、ドラッグ、span、共有、Reset の一連の操作を、各作業単位の利用者確認と統合テストで確認する。
- [x] PC のマウス・キーボードとタッチ端末相当の操作について、2.2〜2.6での利用者確認結果を統合する。
- [x] D-009 の rank 許容誤差、D-012 の基準範囲、D-013・D-014 の表示範囲方針を再評価し、利用者判断により現状維持とする。
- [x] 学生向け画面の用語と数式表記を `math-writing-rules.txt` に照らして棚卸しする。
- [x] 共有URLで許可される空のベクトル集合について、空行列の読み上げと一次独立の説明を整える。
- [x] 確認結果をフェーズ 3 の視認性、アクセシビリティ、入力エラー改善へ反映する。

2.2〜2.7は実装・利用者確認・統合棚卸しまで完了した。フェーズ2の終了条件を満たした。続いて、現在のフェーズ6から前倒しした6.1でmainをGitHub Pagesへ初回公開した。

## 5. フェーズ 3 前の公開作業

### 6.1 GitHub Pages初回公開（フェーズ6から前倒し）

- [x] mainへのpush、GitHub Actions、pnpm、Viteビルド、`dist/`、Pages公開の役割を整理し、`docs/GITHUB_PAGES.md` に手順を記録する。
- [x] mainへのpushと手動実行を起点に、依存関係の再現可能なインストール、自動テスト、`APP_BASE_PATH=/linear-algebra-visual-lab/` での本番ビルドを行うGitHub Actionsワークフローを追加する。
- [x] ワークフローが生成した `dist/` だけをPages artifactとしてアップロードし、`github-pages`環境へデプロイする。
- [x] ローカルで公開用base pathのビルドを検証してから、利用者がワークフローをコミット・mainへpushする。
- [x] 利用者がGitHubのSettings → PagesでSourceをGitHub Actionsに設定し、Actions画面で初回実行が成功したことを確認する。
- [x] `https://d-kitamura.github.io/linear-algebra-visual-lab/` で画面、静的アセット、再読み込みを確認する。
- [x] 本番環境で共有URLの生成・別タブ復元・Reset・コピー・テキスト保存を確認し、URLが同じoriginと公開サブパスを維持することを確認する。

初回公開時は、ワークフローの追加とローカル検証をCodex、GitHub上のPages設定、コミット、push、初回実行確認をプロジェクト所有者が担当した。作業単位6.1は完了し、以後はmainへのpushごとにテスト・ビルド・デプロイが自動実行される。D-030以降の日常更新ではCodexが検証後のcommit・pushとデプロイ確認まで担当する。学内nginxは将来の代替公開先として残す。

## 6. フェーズ 3 の想定作業単位

### 3.1 ベクトル集合の編集機能

- [x] ベクトルの追加・削除で、0本から共有上限8本までの集合を編集できるようにする。
- [x] D-026として、追加時の既定座標・span選択・ID・表示名、8色の表示順パレット、3.1では名前と色を編集しない方針を決める。
- [x] 削除時のspan選択、空集合、行列表示、解析結果、Reset、共有URLとの整合を保つ。
- [x] 追加・削除、上限・下限、共有状態v1の往復を自動テストで固定する。
- [x] PC、タブレット、スマートフォン相当で追加・削除、8本時の表示、0本時の表示、共有・Resetを利用者が確認する。

3.1はデプロイ先での表示・操作・エクスポートを利用者が確認し、完了した。

### 3.2 入力エラーとアクセシビリティの仕上げ

- [x] 入力途中・上限超過・共有URLエラーの案内を授業中に理解しやすい文言と配置へ整理する。
- [x] キーボード、フォーカス順、モーダル、色以外の識別、読み上げ名、タッチ領域を画面全体で再点検する。
- [x] D-028として、座標面全体がモバイルのドラッグ・パン・ピンチを所有し、ページ操作へ途中で奪われないようにする。
- [x] PC、タブレット、スマートフォン相当で、内容追加後も主要操作と解析結果を比較できるか利用者が確認する。
- [x] 実端末で先端ドラッグ、1本指パン、2本指ピンチと、座標面外のページスクロール・ページズームを再確認する。

### 3.3 2D教材シナリオとフェーズ3統合確認

- [x] 空集合、零ベクトル、平行、一次独立、3本以上の一次従属を含む授業用確認例を状態・期待結果・本番リンクとして整理する。
- [x] 各代表例の共有URL生成・復元を自動テストし、授業シナリオを最初から最後まで再現する手順を文書化する。
- [x] 後続の3Dで再利用する状態・数学・共有・教材シナリオのインターフェースと2D固有部分を棚卸しする。
- [x] D-029として、生成する空間のrank 0・1・2の幾何表示をベクトル色から独立した灰色系へ統一する。
- [x] rank 1の破線上とrank 2の背景上で各ベクトルを識別しやすいことを利用者が確認する。
- [x] デプロイ先で入力エラー、キーボード操作、共有ダイアログ、5つの代表例の授業手順を利用者が統合確認する。

3.2・3.3は実装、自動検証、PC・タブレット・スマートフォン相当の利用者確認まで完了した。D-027〜D-029とフェーズ3終了時の文書・実装・テストの統合棚卸しも完了し、フェーズ3の終了条件を満たした。3Dはフェーズ5へ送り、その前にフェーズ4で一次結合の数学・状態・共有・教材表現を2D上で確立する。

## 7. フェーズ 4「2D 一次結合エクスプローラ」の作業単位

### 4.1 一次結合の求解ロジック

- [x] 選択ベクトルを列に持つ行列 `A` とターゲットベクトル `v` について、`Ac = v` のrankと拡大行列のrankを求める。
- [x] 選択本数0〜8本について、表現不可能、一意に表現可能、無数に表現可能を分類する。
- [x] 表現可能な場合は特解、異なる最大2例、自由係数の数、一般解を構成する零空間基底を返す。
- [x] D-009と同じ相対許容誤差と列正規化を用い、2Dと将来の3Dで再利用できる描画非依存ロジックにする。
- [x] 空集合、零ベクトル、1本、一次従属な2本、一次独立な2本、3本以上、3Dを単体テストで固定する。
- [x] 4.1の分類と係数例を利用者が確認する。

### 4.2 ターゲットを含む共有状態v2

- [x] 共有状態v1を引き続き読める後方互換性を保ち、ターゲットとエクスプローラ表示を持つ共有状態v2を追加する。
- [x] エクスポートURLからターゲット、span選択、表示状態を復元し、Resetで共有時のターゲットを含むInitialStateへ戻す。
- [x] ターゲットを持たない既存v1 URL、壊れたv2、安全上限、決定的エンコードをテストする。
- [x] 4.2のv2 URL復元とResetの基盤を利用者が確認する。v1互換性はD-032により正式リリース前の保証対象外とする。

### 4.3 ターゲットの配置・編集・吸着

- [x] 「一次結合を調べる」モードで座標面のクリック・タップからターゲットベクトル `v` を配置し、先端ドラッグと数値入力を同期する。
- [x] 一次結合の対象には既存のspan選択を使い、0〜8本を許可して別の選択機構を増やさない。
- [x] rank 1の直線とrank 0の原点にはD-019と同じ画面幅1%の操作用距離で吸着させ、D-009の数学判定とは分離する。
- [x] モバイルのパン・ピンチ、ターゲット配置、先端ドラッグを競合させず、キーボードだけでも同じ座標を指定できるようにする。
- [x] 4.3の配置、ドラッグ、数値入力、吸着、モバイルジェスチャーを利用者が確認する。

### 4.4 係数・一般解・平行四辺形の教材表示

- [x] `v` が選択集合 `S` の一次結合で表現可能か、表し方が一意か無数かを日本語と数式で対応付ける。
- [x] 係数は当面小数で表示し、表示上の丸めがある場合の等号・近似記号を一貫させる。
- [x] 一般の `{}^t[a, b]` に対する係数公式は既定で折り畳み、利用者が常時展開できるようにする。
- [x] D-034に従い `a₁`〜`a₈`、`S`、`A`、ターゲット `v` へ統一し、`v = Ac = (a₁, …)[c₁; …]` と `c = {}^t[c₁, …]` を表示する。
- [x] 不定では異なる2例を常時表示し、折り畳み一般解に特解・同次解方向の導出理由と展開した一次結合を示す。
- [x] 2項の表示では `c₁a₁`、`c₂a₂` を原点からの2辺として重ね、平行な2辺を補って平行四辺形を描く。経路として頭尾接続する描画は採用しない。
- [x] 表し方が無数の場合は異なる2例を常時表示し、自由係数を使った一般解だけを既定で折り畳む。
- [x] 4.4の分類、係数、一般解、係数公式、平行四辺形を利用者が確認する。

### 4.5 共有・授業シナリオ・統合確認

- [x] 一次結合エクスプローラを共有URL、Reset、レスポンシブ配置、アクセシビリティへ統合する。
- [x] 1本、一次従属な2本、一次独立な2本、3本以上について、解なし・一意・無数を比較する授業シナリオを追加する。
- [x] PC、タブレット、スマートフォンで一連の操作と共有復元を確認し、3Dへ再利用する境界を棚卸しする。

4.1〜4.5の実装・自動検証、利用者による一連の操作確認、文書と3D再利用境界の統合棚卸しを完了したため、フェーズ4は終了とする。フェーズ5の作業単位検討へ進む前に、利用者要望による追加作業4.6を実施する。

### 4.6 フェーズ5前の追加作業: 共有QRコード

- [x] 共有URLダイアログで、URL欄の上に同じ共有URLを表すQRコードを表示する。
- [x] QRコードは外部サービスへURLを送らず、ブラウザ内で余白付きのPNGとして生成する。
- [x] 「QRコードを保存」を「テキストで保存」の左隣に追加し、時刻付きファイル名のPNGを保存する。
- [x] 生成中・容量超過等の失敗を案内し、コピー、テキスト保存、閉じる操作は継続して利用可能にする。
- [x] デスクトップでは4操作を横並び、640px以下では1列にし、QR画像の代替テキストと状態通知を用意する。
- [x] 公開版でQRコードのスマートフォン読取、PNG保存、PC・スマートフォンの表示を利用者が確認する。

4.6は公開版でのスマートフォン読取、同じ教材状態の復元、PNG保存、PC・スマートフォン表示を利用者が確認し、完了した。

## 8. フェーズ 5「3D 教材版」の作業単位

5.0の設計境界は利用者がすべて推奨案どおり承認し、D-037として決定した。各作業単位は、原則として実装・自動検証・デプロイ先での利用者確認を終えてから次へ進む。

### 5.0 実装前に承認する設計境界

| 論点 | 推奨案 | 理由・確認点 |
|---|---|---|
| 2D/3Dの切替 | **採用**: Lab上部に「2D座標平面」「3D座標空間」の2択セグメント型タブを置く | 2Dと3Dは対等な表示モードであり、オン／オフを表すスイッチより意味が明確。既存の詳細タブとは階層と見た目を分け、キーボードの左右キーでも切り替える |
| 切替時の教材状態 | **採用**: 2Dと3Dの状態を別々に保持し、同じ閲覧セッション内では往復しても各状態を失わない | 2Dから3Dへ`z = 0`を補う変換は可能だが、3Dから2Dで`z`を捨てる操作は破壊的。自動変換を既定動作にせず、初回の各モードは専用の既定例を使う |
| 共有URLとReset | **採用**: 共有URLには現在開いている一方の次元だけを保存し、その次元を初期表示する。Resetは現在の次元を、その次元のInitialStateへ戻す | 非表示側の状態をURLへ重複保存せず、URL長と挙動を単純に保つ。別次元へ切り替えた際は、そのセッション内で保持している状態または既定例を使う |
| 3D投影 | **採用**: 右手座標系とz軸上向きの正投影を使う。初期視点ではx軸を右、y軸を左下（手前）から右上（奥）、z軸を鉛直上向きの正方向として見せる | 遠近による見かけの長さの変化を避ける。正面・右・上・等角・全体表示のボタンも用意する |
| カメラ状態の共有 | **採用・実装済み**: 3Dでは方向、注視点、上方向、拡大率を教材上意味のある状態として共有する | 3Dでは視点により直線・平面の見え方が大きく変わるため、教員が意図した見え方の再現価値が2Dの手動表示範囲より高い。D-039の共有状態v3と安全上限を用いる |
| 3Dベクトルの直接編集 | **採用・改修済み**: 数値入力を維持し、矢先ドラッグは開始時の画面に平行な面内の移動、背景ドラッグは視点操作としてモードなしで自動分離する | D-042のモード・軸拘束試作は操作切替の負担が大きかった。D-043では正投影を生かして奥行きを開始時の矢先へ固定し、異なる視点から上下・左右の操作を繰り返して3次元位置を調整する |
| 一次結合の幾何表示 | **採用・確認済み**: 2項は3D空間内の原点基準平行四辺形、3項は係数例1の原点基準平行六面体を使う | 5.5〜5.7の操作確認で教育的な対応と視認性を確認した。退化形は説明を添え、4本以上は一つの幾何形状へ対応させず代数表示に限定する |

### 5.1 次元切替と状態境界

- [x] 2D/3Dのセグメント型タブをLab上部へ追加し、選択状態、関連パネル、キーボード操作、狭い画面での配置を整える。
- [x] 2Dと3DのCurrentState・InitialState・未確定入力・一時UIを分離し、切替往復で各状態を維持する。
- [x] 既存の2D共有URLは2Dタブ、3D共有URLは3Dタブを初期選択する。5.1では3D側を説明付きのプレースホルダーとし、既存2Dの挙動を変えない。
- [x] 次元を切り替えても座標を暗黙変換せず、Resetが現在の次元だけへ作用することをテストする。
- [x] タブの意味、状態保持、Reset範囲をPC・スマートフォンで利用者が確認する。

5.1はデプロイ先での利用者確認を完了し、D-037の状態境界を確定した。

### 5.2 固定例の3D表示とカメラ操作

- [x] Three.jsを導入し、数学座標のx・y・zをそのまま使うz軸上向きの3Dシーンを、Reactの状態・ライフサイクルから分離した描画層として構築する。
- [x] 原点、x・y・z軸、目盛または補助格子、固定された複数ベクトル、矢じり、数式規則に沿うラベルを表示する。
- [x] 正投影カメラ、等角的な初期視点、回転、ズーム、パン、正面・右・上・等角・全体表示を実装する。カメラ操作中だけ必要な再描画を行い、モバイルで常時描画し続けない。
- [x] Canvasのリサイズ、高DPI、WebGL初期化失敗、コンポーネント破棄時のリソース解放を扱う。
- [x] PCのマウス・ホイールとスマートフォンの1本指回転・ピンチ等を試し、正投影、初期視点、操作感を利用者が確認する。

5.2はデプロイ先での利用者確認を完了し、固定3D表示とカメラ操作を確定した。

### 5.3 3D状態・数値編集・共有の縦断接続

- [x] 3Dの専用既定例を決め、0〜8本のベクトルについてx・y・zの数値入力、追加・削除、自動命名、8色、span選択を既存ロジックへ接続する。
- [x] 全ベクトルと選択集合について、既存の`analyzeVectorSet`から`rank(A)`、`dim(span(S))`、一次独立・一次従属を2Dと同じ用語・数式規則で表示する。
- [x] 原点と全ベクトルが収まる3Dの基準範囲・自動全体表示を定め、極端な座標でもカメラのnear/far、目盛、矢じり、ラベルを安全に調整する。
- [x] カメラ状態を検証可能な共有状態v3へ追加し、旧v1・v2を既定カメラで読めるようにする。新しい3D URL、Reset、QRコード、URL長を同時に検証する。
- [x] 数値編集、集合編集、数学カード、共有復元、Resetをデプロイ先で利用者が確認する。

5.3は共有URLによるベクトル設定・カメラ状態の保存確認と、rank 3カードの立方体アイコン修正を経て完了した。この時点で未実装だった3D先端ドラッグは5.6でD-042を試作し、D-043の統一操作へ置換して利用者確認を完了した。

### 5.4 3Dで生成する空間の幾何表示

- [x] rank 0を原点、rank 1を原点を通る太い破線、rank 2を格子なし・両面表示の半透明平面として描く。現在の表示範囲に応じて直線と平面の見える範囲を計算する。
- [x] rank 3は中立色の半透明立方体で表示範囲内の空間を満たし、実線の外枠Boxと「3次元座標空間全体」の明示を組み合わせる。
- [x] span幾何はD-029の中立的な灰色系を継承し、ベクトル、ターゲット、軸、ラベルを隠しにくい透明度・描画順・深度処理を調整する。
- [x] rank 2の平面を真横から見た場合を含め、平面の輪郭、ラベル、説明カードで次元を識別できるようにする。
- [x] 空集合、零ベクトル、平行な2本、独立な2本、独立な3本、3本以上の一次従属を利用者が視点回転しながら確認する。

### 5.5 3D一次結合エクスプローラ

- [x] 3Dターゲット`v`のx・y・z数値入力と消去を追加する。初期段階ではCanvasクリック配置を見送ったが、D-046で奥行き規則を定めて追加した。
- [x] 既存の`analyzeLinearCombination`、不能・唯一解・不定、係数例2つ、自由係数、零空間基底による一般解を再利用する。
- [x] ちょうど2本で表現可能な場合は、`c₁a₁`と`c₂a₂`を原点からの2辺とする平行四辺形を3D空間内に描く。
- [x] ちょうど3本で表現可能な場合の原点基準平行六面体を試作し、係数例1を描画、係数が作る辺のrankが3未満なら退化形と示す。4本以上は代数表示だけを既定とする。
- [x] 1本、一次従属な2本、独立な2本が張る平面、独立な3本、4本以上について、不能・唯一解・不定を利用者が確認する。

### 5.6 3D直接編集とジェスチャー競合の試作

- [x] D-042として数値入力を常時利用可能にしたまま操作モードと軸拘束ハンドルを試作し、利用者確認でモード切替の負担を確認する。
- [x] D-043としてモードUIと軸ハンドルを削除し、通常ベクトルの矢先を画面平行面内で移動、背景を視点回転としてpointerdown時に自動分離する。
- [x] マウス、タッチ、ペンの画面距離ヒット領域、ドラッグ中のカメラ固定、元位置・ガイド・新矢印・3成分プレビュー、終了時確定を実装する。
- [x] 既存数値入力をキーボード代替として維持し、共有URLには確定座標だけを含め、ドラッグ途中の一時状態を追加しない。
- [x] 誤操作、学習上の分かりやすさ、スマートフォンの操作領域、背景視点操作との切替、数値入力との併用を利用者が確認し、D-043の統一操作を本採用する。
- [x] D-044として、ドラッグ中だけ表示幅相対距離以内で他の1本が張る直線へ平行スナップし、平行候補がなければ独立な2本が張る平面へ同一平面スナップする。平行を優先し、緑系ガイド・リングと対象名を表示する。当初の1%はD-060で3%へ改定した。
- [x] PCで平行・同一平面の吸着幅と操作感を利用者が確認し、良好としてD-044を採用する。
- [x] D-045として、選択ベクトルのドラッグ中にプレビューrankを再計算し、灰色のspan形状・ラベル・操作中状態をリアルタイムに切り替える。右側カードと共有状態は終了時確定を維持する。
- [x] D-046として、一次結合モード中の短い背景クリック・タップを原点通過の画面平行面上のターゲット配置とし、8px超の視点ドラッグ、2点操作、通常ベクトル編集と分離する。
- [x] PCでターゲットのワンタップ配置・再配置を利用者が確認し、D-046を採用する。
- [x] D-047として、配置済みターゲットの矢先を画面平行面内でドラッグし、一次結合の平行四辺形・平行六面体、係数倍ベクトル、ラベルをドラッグ中にリアルタイム更新する。解析カードと共有状態は終了時確定を維持する。
- [x] PCとスマートフォンで、span形状のリアルタイム切替、ターゲット矢先ドラッグと係数幾何の追従、視点操作・背景配置との分離を利用者が確認する。

### 5.7 3D授業シナリオと品質仕上げ

- [x] rank 0・1・2・3、一次独立・一次従属、一次結合の不能・唯一解・不定を比較する代表例を、期待結果と本番共有URL付きで定義する。
- [x] デスクトップは3D表示とインスペクターの2列、タブレット・スマートフォンは1列とし、Canvas内の操作とページスクロール・ズームを分離する。
- [x] 数値入力、上限、共有URL、WebGL/GPU失敗を理解しやすく案内し、Canvasを利用できない場合も数値と数学結果へ到達できるようにする。
- [x] Canvasの代替説明、視点プリセット、フォーカス順、タッチ領域、色以外の識別を実装・自動確認し、代表例で端末回転と描画負荷を確認できる手順を定義する。
- [x] 教員が共有URL・QRコードで3D例を配布し、学生側で同じベクトル、span、ターゲット、カメラを復元できる授業手順を文書化する。
- [x] 公開版のPCと実機スマートフォンで代表例10〜16、縦横回転、共有URL・QR復元、Canvas外スクロール、代表例14の追従性を利用者が確認する。

### 5.8 フェーズ5統合確認と棚卸し

- [x] 2D/3D切替、各次元の状態保持、3D数値編集、カメラ、span、一次結合、共有、QR、ResetをPC・タブレット・スマートフォンで通して確認する。
- [x] 2Dの既存機能・共有URL・授業シナリオに回帰がないことを自動テストと利用者確認で確かめる。
- [x] 3Dの数学・状態・描画・カメラ・操作・教材シナリオの責務、採用しなかった試作、再評価条件を文書化する。
- [x] `SPEC.md`、`ROADMAP.md`、`docs/DECISIONS.md`、`docs/PROJECT_STATUS.md`、教材シナリオ、実装、テスト、依存関係を横断して棚卸しし、フェーズ5の終了を判断する。

5.1〜5.8の実装、自動検証、公開版のPC・スマートフォン確認、文書・依存関係の統合棚卸しが揃ったため、フェーズ5は完了とする。

## 9. フェーズ6「共有と配布の仕上げ」の作業単位案

GitHub Pages初回公開は6.1として完了し、QRコードは4.6で前倒し実装済みである。不正URLの安全な拒否、既定例へのフォールバック、共有状態v1・v2からv3への移行も既に実装されている。6.2〜6.7の作業単位は利用者承認後に実施し、実運用上限、正式リリース後の契約、配布手順、初版候補の利用条件を順に確定した。

### 6.2 共有URL・QRコードの運用上限監査

- [x] 2D・3Dの代表例16件に加え、0本・8本、最大絶対座標、3Dカメラ、ターゲット、span選択を組み合わせた画面から到達可能な境界状態について、状態文字数、完全URL長、QR生成可否を自動測定する。
- [x] 現在の代表例URLが268〜612文字であることを基準点とし、8192文字のデコード防御上限と、授業で生成・読取を保証する実運用上限を別の値として定義する。
- [x] 空・重複・不正Base64URL・不正JSON・未知バージョン・次元不整合・上限超過を、クラッシュさせず既定例へ戻し、学生が理解できる理由を表示する現在の挙動を公開版でも確認する。
- [x] URL短縮・圧縮は先に導入せず、境界測定で実運用上限またはQR容量を超える場合だけ候補を比較する。必要な場合は6.3のスキーマ判断より前に方式を決める。
- [x] **確認ゲート:** D-050として完全URL 2048文字、QRも同じ範囲、圧縮なしを採用する。代表例の現行v3新規生成は286〜612文字、画面到達可能な8本・3D境界状態は1309文字で、誤り訂正MのPNG生成に成功した。

6.2は実装・自動検証とローカルブラウザ確認を完了した。8192文字は受信データを早期拒否する防御上限であり、アプリが授業用として生成する完全URLの保証値ではない。完全URLが2048文字を超える場合はQR生成前に理由付きで拒否する。

### 6.3 正式リリース後の共有URL互換契約

- [x] 6.2の結果を受け、現行v3を最初の保証対象にするか、正式リリース前に新スキーマへ更新するかを決める。
- [x] 正式リリース後は、旧版を厳密に検証して現行状態へ移行し、新版は現行版だけを生成する原則、未知の将来版を安全に拒否する原則、互換保証期間を文書化する。
- [x] 正式リリース時の共有URLを固定fixtureとして保存し、将来の変更で復元結果が変わらない自動テストと互換表を追加する。互換用の冗長な情報を新規URLへ重複して埋め込まない。
- [x] D-032どおり、この作業単位の完了だけでは正式リリースを宣言せず、互換保証の開始はフェーズ7で利用者が初版リリースを承認した時点とする。
- [x] **確認ゲート:** D-051として、v3を最初の保証対象、本アプリ提供期間中は期限なし、現在の有効なv3も対象、v1・v2は保証対象外のまま読込を当面維持する方針を利用者が承認した。

6.3は互換表、3Dカメラと一次結合を含む固定v3 URL・期待状態fixture、復元・決定的再生成テストを追加して完了した。将来v4以降へ移る場合もv3 fixtureを変更せず、移行デコーダーと新fixtureを追加する。

### 6.4 配布先の可搬性とnginx手順

- [x] GitHub Pagesを初版の主配布先として維持し、ルート配信と任意サブパス配信の両方で、入口・遅延3Dチャンク・CSS・共有URL・再読み込みを検証できるビルド手順を整える。
- [x] 実行時のoriginとパスから共有URLを生成する現行方式が、HTTPS、ルート、サブパスの各構成で同じ配布先を指すことを自動・手動確認する。
- [x] 学内nginx向けに、配置パス、`index.html`、MIME type、HTTPS、圧縮、ハッシュ付きアセットとHTMLのキャッシュ方針、リクエスト行長を確認する手順と設定例を別文書へまとめる。
- [x] 現状はクエリーパラメーターだけを使いクライアント側ルートを持たないため、不要なSPA fallbackを必須設定にしない。将来ルーティングを追加した場合の再検討条件だけを記録する。
- [x] **確認ゲート:** D-052としてGitHub Pagesを初版の正規配布先とし、nginxは設定ガイドと可搬ビルド確認までを必須、実サーバー配置は任意の後続作業とする。

6.4は`verify:dist`とActionsの成果物検証ステップを追加し、`/`、`/class/tools/vector/`、`/linear-algebra-visual-lab/`で実ビルド・入口・4アセットの整合を確認して完了した。nginx設定は公式の`root`、`try_files`、`add_header`、gzip、リクエスト行の仕様を参照し、SPA fallbackを使用しない。

### 6.5 授業配布シナリオと対応環境の受入確認

- [x] 学内管理端末、教員PC、学生のスマートフォン／タブレットについて、必要能力と確認済み範囲を整理し、実際に使うOS・ブラウザ・最小画面幅・WebGL利用可否を授業前に記録するチェック欄と代替導線を定める。
- [x] 教員が公開版で状態を作成し、URL、QR表示、QR画像、テキスト保存、LMSまたはスライドを経由して学生が開き、操作後にResetする一連の授業手順を確認する。
- [x] プロジェクター等に表示したQRコードを実距離から確認する授業前手順を定め、2D・3D、縦横画面、Clipboard API失敗、ファイル保存制限、WebGL失敗時の代替導線を確認する。
- [x] URLやQRが使えない場合の短い復旧手順、問い合わせ時に伝える情報、公開版の更新タイミングを教員向け運用文書へ記録する。
- [x] **確認ゲート:** D-053として、能力別の対応範囲、公開Chromium・レスポンシブ相当・利用者の実機スマートフォン確認を基準点とする。学内端末の具体版、教室ネットワーク、物理的な投影距離は変動するため、実装完了条件ではなく各授業前の記入式チェックとする。

6.5は教員配布、学生確認、Reset、QR・ネットワーク・Clipboard・保存・WebGL・ジェスチャー失敗時の復旧を`CLASSROOM_DISTRIBUTION.md`へ統合し、文書要件を自動検証した。公開版で通常QRダイアログと固定v3の3D状態復元を再確認した。

### 6.6 初版候補の配布情報と利用条件

- [x] アプリと文書の版表示、初版候補の識別方法、変更履歴の残し方を決める。Git tagやGitHub Releaseの作成は、利用者が別途明示的に承認した場合だけ行う。
- [x] リポジトリのライセンス、旧教材からの再利用範囲、依存ライブラリのライセンス表示要否を確認し、利用者判断が必要な事項を確定する。
- [x] 数学状態とQR画像を外部APIへ送らない現在の設計、GitHub Pages等の配信基盤、保存される情報、既知の制限、対応環境を学生・教員向けに簡潔に案内する。
- [x] README、公開手順、授業シナリオ、トラブル時の代替操作を一つの配布資料群として相互リンクする。
- [x] **確認ゲート:** D-054として、初版候補`1.0.0-rc.1`、正式承認後`1.0.0`、MIT License、Copyright © 2026 Daichi Kitamura、アプリ内と詳細文書の公開案内を利用者が承認した。

6.6は版番号を`package.json`からアプリへ表示し、`CHANGELOG.md`、`LICENSE`、`THIRD_PARTY_NOTICES.md`、`USAGE_AND_PRIVACY.md`を追加した。学生はアプリのフッターから利用・プライバシー概要と詳細文書へ到達できる。初版候補のためGit tagとGitHub Releaseは作成しない。

### 6.7 フェーズ6統合確認と初版候補への引渡し

- [x] 代表例16件と0本・8本の境界例について、2D/3D、共有URL、QR、Reset、数値入力、直接操作、レスポンシブ表示を公開版で回帰確認する。
- [x] URL上限と互換fixture、ルート／サブパスのビルド、選定した配布先、対応環境、ライセンス・配布文書を自動テストと手動確認へ対応付ける。
- [x] `SPEC.md`、`ROADMAP.md`、`docs/DECISIONS.md`、`docs/PROJECT_STATUS.md`、公開・授業文書、実装、テスト、依存関係を横断して棚卸しする。
- [x] フェーズ6の終了条件を満たすことを利用者が確認し、フェーズ7「初版評価と棚卸し」へ引き渡す。フェーズ6完了は初版候補の完成を意味し、正式リリースと互換保証開始はフェーズ7の判断とする。

6.7の技術確認は`PHASE6_ACCEPTANCE.md`へ集約した。棚卸しで検出した2D代表例の旧v2リンクを全て現行v3へ更新し、文書URLが正本から生成したURLと完全一致するテストへ強化した。利用者が公開版の初版候補を確認し、フェーズ6完了とフェーズ7への引渡しを承認した。

想定順序は6.2→6.3→6.4→6.5→6.6→6.7とする。6.2の測定結果はスキーマを変える最後の機会となる6.3へ先行させ、6.3で共有形式を固めてから配布先・授業運用を確認する。nginxの実配置と正式リリースは、環境情報または利用者の明示的な判断なしに進めない。

## 9. フェーズ7の延期とフェーズ8の作業単位

授業実施が先であるため、フェーズ7「初版評価と棚卸し」は完了扱いにせず、実際の授業で評価できる時期まで延期する。延期中は正式リリース前の候補版という位置付けを維持し、現時点の版番号を`1.0.0-rc.1`とする。追加開発に伴う次のRC番号は別の作業単位で決めるが、`1.0.0`、Git tag、GitHub Release、D-051のv3共有URL互換保証開始は行わない。ただし、現行v3 fixtureと回帰テストは追加Lab開発中も維持する。

フェーズ8は「複数Lab基盤と基底・次元Lab」とする。シラバス第4〜7週の一次独立な最大個数、基底、次元、座標を、現在のspan・rank・一次結合教材から連続して学べるようにする。後続候補は線形写像、表現行列、固有値・固有空間、対角化・Cayley–Hamilton、内積・Gram–Schmidtの順を基本とし、各Labを別フェーズとして個別承認する。部分空間判定と連立一次方程式・RREFは、前半単元を補う独立候補として残す。

### 8.1 複数Lab化の仕様と状態設計

- [x] 現行教材を「ベクトル空間Lab」（識別子`vector-space`）、最初の追加教材を「基底・次元Lab」（識別子`basis-dimension`）とする。
- [x] アプリ共通ヘッダーからLabメニューを開く構成とし、Lab数の増加に弱い横一列のタブへ全Labを並べない。現行の2D/3Dセグメント型タブはベクトル空間Lab内に維持する。
- [x] 状態なしの通常URLは当面ベクトル空間Labを開き、共有URLは教材一覧等を経由せず対象Labへ直接到達させる。独立したトップ画面は8.1の必須範囲に含めない。
- [x] 各Labは`DefaultState`、`InitialState`、`CurrentState`、一時UI状態を所有する。Lab切替では同一セッション中の各Labの`CurrentState`を互いに変更せず、共有URLには現在Labの状態だけを含める。
- [x] Resetは現在Labの`CurrentState`だけをそのLabの`InitialState`へ戻す。ベクトル空間Lab内では、D-024・D-037の「現在次元だけをResetする」挙動を維持する。
- [x] 共有スキーマは`lab`と`v`の組を判別子とするLab別の直和型として検証する。現行`vector-space` v3を変更せず、`basis-dimension`は独立したv1から開始し、新Lab追加だけを理由に現行スキーマをv4へ上げない。
- [x] QR、コピー、テキスト保存、URL長制限、不正URLの安全なフォールバックは共通機能とし、Lab固有の状態検証・既定状態・復元処理と分離する。
- [x] 8.2では機能変更を伴わない共通シェルとベクトル空間Labの責務分離を先に行い、基底・次元の数学機能を同時に作り込まない。
- [x] **確認ゲート:** D-056として、フェーズ7の延期、フェーズ8の対象、複数Labのナビゲーション、状態所有、共有・Reset・版管理の境界を利用者が承認した。

### 8.1a 3Dターゲット表示と平面span吸着の補修

- [x] ターゲットドラッグ中にCSS2Dの`v`ラベルが移動経路へ蓄積する原因を、入れ子のプレビューグループ削除時にラベルDOMの削除イベントが届かないことと特定した。
- [x] ドラッグプレビューを作り直す前に、破棄対象のCSS2DラベルDOMを明示的に除去し、描画資源も従来どおり破棄する。
- [x] 3Dの選択集合がrank 2の平面を生成するとき、ターゲットドラッグ中だけ、表示幅相対の候補をその平面へ直交射影して吸着する。実装時の1%はD-060で3%へ改定し、数値入力は変更しない。
- [x] 吸着中と確定時に、平面を決める独立な2本と「一次結合で表現できます」を状態メッセージへ示す。吸着座標から一次結合幾何と解析をリアルタイム更新する。
- [x] 35テストファイル・272テスト、型検査、本番ビルドに成功し、ローカル実画面でドラッグ後の`v`ラベルが1個だけであること、斜め平面への吸着座標、不能から唯一解への切替を確認した。
- [x] **確認ゲート:** D-057として実装・自動検証・ローカル実画面確認を完了し、公開版で問題が解消したことを利用者が確認した。

### 8.2 共通アプリシェルと現行Labの分離

- [x] 共通ヘッダー、Labメニュー、共有・Resetの接続点と、Lab固有画面の境界を実装する。
- [x] 現行のベクトル空間機能をLab単位へ整理し、表示、操作、共有v3、Reset、レスポンシブ配置を変更しない。
- [x] PC・スマートフォンでLabメニューをキーボード・タッチ操作でき、現在Labを視覚以外の方法でも識別できるようにする。
- [x] **確認ゲート:** 公開版相当でベクトル空間Labの代表例とv3 URLが変更前と同じ状態へ復元されることを利用者が確認した。

8.2ではD-058として、共通`App`をヘッダー・Labメニュー・利用案内・フッターへ縮小し、現行2D/3D教材状態と画面を`VectorSpaceLab`へ移した。共有とResetは現在Labから共通操作部品へハンドラを渡す。まだ画面を持たない基底・次元Labはメニューで準備中と読み上げ可能に示し、8.4でLab画面を接続するまで選択不能とする。36テストファイル・276テスト、型検査、Pages用ビルド、ローカル実画面と固定v3 URLの復元に成功し、公開版を利用者が確認して8.2を完了した。

### 8.2a 3Dターゲットの原点・直線span吸着

- [x] D-057のrank 2平面吸着を、rank 0の原点とrank 1の生成直線へ拡張する。
- [x] rank 0では原点との距離、rank 1では生成直線への直交距離、rank 2では生成平面への直交距離を、現在の正投影表示幅に相対な共通距離と比較する。当初の1%はD-060で3%へ改定した。
- [x] 吸着中・確定時に原点、直線を決めるベクトル、平面を決める2本と、一次結合で表現可能であることをrank別に示す。
- [x] rank 3、距離外、数値入力、呼出側rankと選択集合の不整合では座標を吸着させない。
- [x] 36テストファイル・279テスト、型検査、Pages用ビルド、静的アセット、ローカル実画面のrank 1案内を確認した。
- [x] **確認ゲート:** 公開版でrank 0の原点とrank 1の直線への吸着範囲・操作感を利用者が確認した。

### 8.2b 3D共通吸着距離の調整

- [x] D-060として、画面平行面内でしか動かせない3D操作の難しさを踏まえ、3D専用の吸着距離を正投影表示幅の1%から3%へ広げる。
- [x] 通常ベクトルの平行・同一平面吸着と、ターゲットの原点・生成直線・生成平面吸着へ同じ3%を適用する。
- [x] この作業時点では2Dの通常ベクトルとターゲットを表示幅1%に維持した。D-069で2%へ改定したが、D-009の数学判定、数値入力、共有状態は変更しない。
- [x] 3D専用比率、表示幅10・100・4での比例値、2つの3Dドラッグ経路への接続を自動テストで固定する。
- [x] 36テストファイル・280テスト、型検査、Pages用ビルド、静的アセット検査に成功した。
- [x] **確認ゲート:** 公開版で通常ベクトルとターゲットの3%吸着範囲・操作感を利用者が確認した。異なるズームや複数候補で問題が見つかった場合はD-060を再評価する。

### 8.2c フェーズ8開始後の統合棚卸し

- [x] 8.1、8.1a、8.2、8.2a、8.2bの実装、利用者確認、決定D-056〜D-060、仕様、ロードマップ、現在地を横断照合する。
- [x] 3Dの通常ベクトルとターゲットが共通の表示幅3%を使い、この棚卸し時点の2Dは表示幅1%、D-009は`1e-10`を維持することを実装・テスト・文書で照合した。2DはD-069で2%へ改定した。
- [x] 共通アプリシェルと`VectorSpaceLab`の責務分離、現行`vector-space` v3の維持、`basis-dimension` v1を8.3以降に追加する境界を確認する。
- [x] D-059・D-060の確認待ち、仕様書の文書バージョン、フェーズ8の実施順から補修作業が抜けていた記述、D-058以前の旧UI責務表現を修正する。
- [x] **確認ゲート:** 全自動テスト、型検査、Pages用ビルド、静的アセット検査に成功し、次の作業単位を8.3「基底・次元の数学ロジック」として一意に引き渡す。

### 8.3 基底・次元の数学ロジック

- [x] 候補となるベクトルの組について、一次独立性、対象空間を生成するか、基底か、次元、一次独立な最大個数を判定する。
- [x] 基底でない理由を「一次従属」「対象空間を生成しない」の2条件へ分け、両方が問題となる場合も表現できるようにする。
- [x] 一つの基底例を抽出できるようにするが、それが唯一の基底であるとは表示しない。
- [x] `math-writing-rules.txt`と教科書に合わせ、基底記号、順序付きの組、座標ベクトルの表記をUI実装前に確定する。
- [x] 対象空間を全ベクトルの集合`S`が生成する`V = span(S)`、基底候補を`S`から選ぶ順序付きID列として定義し、8.4の選択UIと共有状態から独立したdomain APIにする。
- [x] 空集合、零ベクトル、2D・3D、一次独立だが生成不足、一次従属だが生成可能、2条件とも不成立、許容誤差、入力検証を単体テストで固定する。
- [x] 38テストファイル・298テスト、型検査、Pages用ビルド、4アセットの静的検査に成功し、現行ベクトル空間Labの回帰を維持する。
- [x] **確認ゲート:** D-062の対象空間・基底候補・失敗理由・基底例・数式表記を利用者が確認し、8.4の画面実装へ進む。

8.3では`analyzeBasisCandidate`と`extractBasisExample`を描画非依存のdomain APIとして追加した。対象空間の次元、一次独立な最大個数、基底例はいずれもD-009と同じrank判定から導出し、基底候補の順序は保持する。8.4の画面、`basis-dimension` v1共有状態、Labメニューの有効化はまだ行わない。

### 8.4 2D/3D基底エクスプローラ

- [x] 表示中のベクトルから順序付きの基底候補を選択・並べ替えし、一次独立性と生成条件を同時に操作・確認できるようにする。
- [x] 全ベクトル集合`S`、順序付き候補`\mathcal{B}`、対象空間`V`を画面で区別し、rankを使う2条件へ接続する。表示位置と情報量は8.4bで改訂した。
- [x] 2D/3D別のベクトル成分編集、矢先ドラッグ、候補span幾何、候補の2条件・失敗理由・一次独立な最大個数を同じ解析へ接続する。
- [x] 複数の基底候補を試せるようにし、自動抽出した基底例を「入力順から得た一例」と明記して正解を固定しない。
- [x] 基底・次元Labを共通メニューから選択可能にし、Lab切替後も各Labの教材状態を保持する。共有URLは8.7まで無効理由を示し、現行`vector-space` v3を誤って出力しない。
- [x] PCと390px幅、キーボード操作、WebGL代替説明、2D/3D切替、候補追加・除外、Resetをローカル実画面で確認する。
- [x] **確認ゲート:** D-063の画面構成、候補順序操作、2条件表示、2D/3D操作、Lab切替、共有無効表示を利用者が公開版で確認した。追加の表示・吸着補修は8.4b・8.4cで追跡する。

8.4では`BasisDimensionLab`を追加し、共通Labメニューの準備中項目を選択可能にした。2Dと3Dは独立状態を保持し、現在次元だけをResetする。基底候補の選択順、並べ替え、ベクトル成分、候補span、2条件、失敗理由、対象空間の次元、基底例をD-062の同じdomain解析から描画する。学生向け要約の配置と情報量は8.4bで改訂した。`basis-dimension` v1共有状態、座標エクスプローラ、代表例はそれぞれ8.7、8.5、8.7へ残す。

### 8.4a 基底・次元Labの2D平行吸着補修

- [x] 基底・次元Labの2D矢先ドラッグだけが、ベクトル空間Labの平行吸着処理を通らず座標を直接確定していた原因を特定する。
- [x] D-019と同じ`parallelSnapDistanceForViewWidth`と`snapDraggedVectorToParallel`を接続し、他のベクトルが張る直線へ直交射影する。当初の表示幅1%はD-069で2%へ改定した。
- [x] ドラッグ中は吸着対象の矢印と操作ハンドルを強調し、吸着座標から候補rank、灰色span、2条件、数値入力をリアルタイム更新する。ドラッグ終了、次元切替、Resetで一時強調を解除する。
- [x] 表示幅10で候補rankが2から1へ落ちる例、距離外、表示幅20で吸着範囲が比例する例、既存2D吸着回帰を自動テストで確認する。
- [x] **確認ゲート:** D-064として、基底・次元Labの2D吸着範囲と操作感がベクトル空間Labと等しいことを利用者が公開版で確認した。表示内容と原点吸着の追加修正は8.4bへ送る。

### 8.4b 基底候補の数式表記・判定要約・原点吸着の補修

- [x] カリグラフィー体の`\mathcal{B}`だけ専用フォントを残し、`\bm{a}_i`、`S`、`V`等をベクトル空間Labと同じ数式フォント・太さ・添え字規則へ統一する。
- [x] 基底候補カードを`S = \{\bm{a}_1, ...\}`、`\mathcal{B} = (\bm{a}_{i_1}, ...)`の2行とし、各成分をセミコロン表記ではなく`{}^t[x_1, ...]`で示す。
- [x] 判定カードの要約を「対象としている空間：`V = \mathbb{R}^n`」「現在選んでいるベクトルの組：`\mathcal{B} = (...)`」へ絞り、その直後に2条件を置く。
- [x] 基底例の注記を、他の基底が存在し得る可能性と`S`から選べる一次独立な最大個数を一続きに説明する文へ改める。
- [x] 平行・同一平面吸着より原点吸着を優先して零ベクトルを作れるようにし、ベクトル空間Labと基底・次元Labの共有処理へ反映する。2Dは当初の表示幅1%からD-069で2%へ改定し、3Dは表示幅3%を維持する。
- [x] PC幅・390px幅で数式フォント、2行要約、横はみ出し、ブラウザエラーを実画面確認し、2D・3D原点吸着を自動テストで固定する。
- [x] **確認ゲート:** D-065として、公開版で数式表記、判定カードの情報量、基底例の文言、原点吸着を利用者が確認した。原点優先規則の全Lab経路と3D平面spanの表示範囲は8.4cで補強する。

### 8.4c 原点優先吸着の全Lab統一・3D平面span境界の補修

- [x] 原点から吸着距離以内では他ベクトルが張る直線・平面への射影より零ベクトル化を優先する判定を、2D・3D共通の原点距離関数へ集約する。
- [x] ベクトル空間Labと基底・次元Labの2D・3Dが、それぞれ原点優先の共有吸着経路へ接続していることを回帰テストで固定する。
- [x] 3Dのrank 2 span平面をxy格子外周の92%へ収め、半透明面とは別に4辺の灰色輪郭を描いて、ズーム時も有限平面の縁を識別できるようにする。
- [x] 40テストファイル・317テスト、型検査、Pages用ビルド、静的アセット検査を確認する。
- [x] **確認ゲート:** D-066として、公開版の両Labで原点吸着とrank 2平面の大きさ・4辺の見え方を利用者が確認し、8.5へ進む。

### 8.5 座標エクスプローラ

- [x] 2D・3D別のターゲットベクトル`v`を数値入力、矢先ドラッグ、2D背景配置、3D背景配置で編集し、基底なら一次結合係数を`[v]_B`として唯一表示する。
- [x] 既存の一次結合解析へ接続し、表現不能、一次従属で係数が無数、基底でない組による当該ターゲットだけの一意表現を、基底に関する座標と区別する。
- [x] 現在の基底を比較用`B_0`として記録し、同じターゲットに対する現在の`[v]_B`と`[v]_{B_0}`を、基底の選択・順序を変えながら比較できるようにする。
- [x] 既定ターゲット、比較用基底、入力下書きを2D・3D別に保持し、Resetで現在次元だけを戻す。共有URL・QRは8.7まで追加しない。
- [x] 41テストファイル・328テスト、型検査、PC幅・390px幅・3D Canvasのローカル実画面を確認する。
- [x] **確認ゲート:** D-067として、公開版で座標の一意性、非基底の3分類、基底・順序による座標の変化、2D・3Dターゲット操作、Resetを利用者が確認した。

### 8.5a 基底・次元Labの詳細タブ・列ベクトル・係数表記の統一

- [x] 「基底候補」だけを常時表示し、「全ベクトルの集合」「基底・次元の判定」「基底に関する座標」を、ベクトル空間Labと同じARIA付き詳細タブへまとめる。タブ状態は2D・3D別に保持し、Resetで現在次元を「成分」へ戻す。
- [x] 成分カードをグラフ列からインスペクター列へ移し、広幅・高倍率相当・スクロール時にstickyなグラフ領域へカードが入り込まない配置へ改める。980px以下の1列化と390px幅の横はみ出しなしを維持する。
- [x] 成分入力と表示用列ベクトルの括弧を、ベクトル空間Labと同じ上下辺を持つ`[`・`]`へ統一する。
- [x] 一意な係数、非基底の一意な係数、無数解の2例を太字斜体`c`で表示し、基底の場合だけその`c`が`[v]_B`であると説明する。
- [x] 41テストファイル・330テスト、型検査、広幅スクロール・3タブ・390px幅のローカル実画面を確認する。
- [x] **確認ゲート:** D-068として、公開版でタブ構成、倍率・スクロール時の配置、角括弧、係数`c`の表記を利用者が確認した。

### 8.5b 2Dターゲット吸着と原点優先の全Lab統一

- [x] 基底・次元Labの2Dターゲット配置・矢先ドラッグを、選択中の基底候補が生成する原点または直線への共通吸着処理へ接続する。
- [x] 両Labの2D通常ベクトルとターゲットに共通する吸着距離を、表示幅の1%から2%へ広げる。D-009の数学判定、数値入力、3Dの表示幅3%は変更しない。
- [x] 2D・3Dのターゲットは、選択集合のrankにかかわらず吸着距離内の原点を直線・平面より優先する。3Dでは背景タップによる配置にも同じ規則を適用する。
- [x] 原点優先をrank 0〜3、2D直線吸着の接続、表示幅比例、3Dドラッグ・配置の接続について、41テストファイル・330テスト、型検査、Pages用ビルド、静的検査、ローカル画面で確認する。
- [x] **確認ゲート:** D-069として、公開版で基底・次元Labの2D rank 1ターゲット吸着、両Labの2D 2%の操作感、2D・3Dターゲットの原点優先を利用者が確認した。

### 8.6 多項式空間への段階的拡張

- [x] D-070として独立Labを増やさず、基底・次元Labの同じ状態を「数ベクトル／多項式」で切り替える。2Dを`R[x]_1`、3Dを`R[x]_2`へ対応させる。
- [x] 標準基底を`math-writing-rules.txt`どおり`(1, x, x^2)`の昇べき順とし、係数ベクトルとの双方向の対応、各`a_i = f_i(x)`を確認できるようにする。ターゲット表示は8.6a・D-071で一次結合モード中だけの`v = g(x)`へ改定した。
- [x] 2D係数空間の軸を`b_0, b_1`、3Dを`b_0, b_1, b_2`として、数ベクトル版のドラッグ・rank・基底・座標解析を再利用する。多項式のグラフ、文字式入力、3次以上は今回の範囲外とする。
- [x] 表示方式を2D・3D別に保持し、Resetで現在次元を既定の数ベクトル表示へ戻す。共有URLへの保存は8.7へ送る。
- [x] 多項式整形、零係数・符号・係数1、係数名、UI接続、2D/3D係数軸、Reset、390px幅を自動テスト・型検査・ローカル実画面で確認する。
- [x] **確認ゲート:** D-070として、公開版で数ベクトル／多項式切替、昇べき順、係数編集、2D/3D係数軸、基底・座標判定の一致、Reset、スマートフォン表示を利用者が確認した。

### 8.6a 一次結合モードと多項式表記の統一

- [x] D-071として基底・次元Labの2D・3D、数ベクトル・多項式を未配置ターゲットで開始し、既存Labと同じ「一次結合を調べる／一次結合モードを終了」操作へ統一する。
- [x] 一次結合モード中だけ「一次結合」タブを表示し、背景クリック・タップ、矢先ドラッグ、成分入力、消去を利用できるようにする。旧「基底に関する座標」カードは「一次結合」へ改称する。
- [x] 2D見出しを「基底候補の数ベクトルが生成する空間」「基底候補の係数が生成する係数空間」へ分ける。「多項式と係数ベクトル」は多項式表示だけの詳細タブへ移す。
- [x] 一般対応を数式フォントで示す。係数記号は8.6b・D-072で、多項式係数を`b_i`、一次結合係数を`c_i`として役割を分けた。
- [x] 42テストファイル・338テスト、型検査、Pages用ビルド、静的アセット検査、PC幅の2D/3D実画面で初期状態、モード、配置、消去、タブ、数式フォント、ブラウザ警告・エラーなしを確認する。
- [x] **確認ゲート:** D-070・D-071として、公開版で4表示の初期ターゲット非表示、一次結合モード、2D・3D配置とドラッグ、条件付きタブ、見出し、数式フォント、Resetを利用者が確認した。

### 8.6b 多項式係数記号・消去ボタン・描画ラベルの補修

- [x] D-072として一般対応を`f(x)=b_0+b_1x(+b_2x^2) ⇔ {}^t[b_0,b_1(,b_2)]∈R^n`へ修正し、多項式係数`b_i`と一次結合係数`c_i`を区別する。
- [x] 一次結合カードの「ターゲットを消去」を横書きで収まる独立行へ置き、狭い入力列での縦書き崩れを防ぐ。
- [x] 2D・3D図のASCII末尾数字を添え字として解釈し、通常ベクトルのラベルを`a_1, a_2, ...`として描画する。
- [x] 42テストファイル・339テスト、型検査、Pages用ビルド、4アセットの静的検査、PC幅の2D・3D実画面を確認する。
- [x] **確認ゲート:** D-070〜D-072として、公開版で多項式係数`b_i`と一次結合係数`c_i`の区別、消去ボタンの横書き、2D・3Dラベルの添え字を利用者が確認した。

### 8.7 共有・教材例・統合棚卸し

- [x] D-073として基底・次元Lab v1を定義し、次元、全ベクトル、順序付き候補、表示方式、一次結合、比較用基底、3DカメラをURL・QR・Resetで再現する。完全URL2048文字以内と受信状態8192文字以内を維持する。
- [x] 2D/3D、複数の基底、基底でない2種類の理由、座標の一意・非一意・表現不能を4件の代表例へまとめる。
- [x] 現行ベクトル空間Labのv3 fixtureと16件の代表例、PC・390px幅、静的配布を回帰確認する。基底・次元Lab v1も固定fixtureへ加える。
- [x] 仕様、決定、状態、授業シナリオ、実装、テスト、依存関係を統合棚卸しし、フェーズ8の実装範囲を閉じる。
- [x] 45テストファイル・352テスト、型検査、Pages用ビルド、4アセットの静的検査、PC・390px幅の2D/3D共有・Reset実画面を確認する。
- [x] **確認ゲート:** D-073として、公開版で基底・次元Labの2D/3D共有URL・QR、候補順、表示方式、ターゲット、比較用基底、3Dカメラ、共有時InitialStateへのReset、代表例を利用者が確認した。

実施順序8.1→8.1a→8.2→8.2a→8.2b→8.2c→8.3→8.4→8.4a→8.4b→8.4c→8.5→8.5a→8.5b→8.6→8.6a→8.6b→8.7はすべて利用者確認済みで、フェーズ8を完了した。8.1a・8.2a・8.2b・8.4a・8.4b・8.4c・8.5a・8.5b・8.6a・8.6bは利用者確認で判明した操作・表示補修、8.2cと8.7は統合棚卸しを含む。8.2は既存機能の責務分離、8.3は描画非依存の数学仕様、8.4は選択可能な2D/3D基底エクスプローラ、8.5は同じターゲットを複数の基底で比較する座標エクスプローラ、8.6は同じ係数状態を数ベクトルと多項式の2つの見方で結ぶ教材、8.7はLab固有共有・代表例・回帰確認である。

## 10. フェーズ9「線形写像Lab」の作業単位案

`syllabus.txt`では基底・次元に続く第9〜10週に線形写像、核、像、ランク、退化次数、次元定理を扱う。フェーズ8で整備したrank、生成する空間、一次結合、基底、座標を直接再利用でき、後続の表現行列・固有値・対角化の前提にもなるため、次の追加単元は「線形写像Lab」を推奨する。部分空間判定と連立一次方程式・RREFは前半の補助教材として有用だが、現在のシラバス順と既存Labからの連続性では線形写像を優先する。

### 9.1 実装前の教材設計と状態境界

- [x] 新Labの名称を「線形写像Lab」、識別子を`linear-map`とする。
- [x] 数ベクトル空間の`T: \mathbb{R}^n \rightarrow \mathbb{R}^m`と`T_{\bm{A}}(\bm{u})=\bm{A}\bm{u}`を中核とし、`n,m∈{2,3}`を段階的に扱う。高次元、任意基底、多項式写像はフェーズ9へ含めない。
- [x] 定義域`U`と終域`V`を別々の図として並べ、入力`\bm{u}`と像`T(\bm{u})`を同時に操作・確認する。広幅では左右、狭幅では上下に配置し、異なる次元の図を無理に同じ座標空間へ重ねない。
- [x] 行列`\bm{A}`、入出力次元、主入力ベクトル、線形性確認用の第2入力とスカラー、2D/3Dカメラ、表示項目をLab固有状態とする。共有は既存Labと独立した`(lab,v)=(linear-map,1)`とし、URL・QR・共有時InitialStateへのResetを9.6で接続する。
- [x] `math-writing-rules.txt`に従い、写像は`T`、行列による写像は`T_{\bm{A}}`、像は`T(\bm{u})`、核は`\mathrm{Ker}(T)`、像は`\mathrm{Im}(T)`、ランクは`\mathrm{rank}(T)`、退化次数は`\mathrm{null}(T)`とする。
- [x] **確認ゲート:** D-074として、教育目的、対象次元、定義域／終域の配置、状態・共有境界、表現行列Labへ送る範囲を利用者が承認した。
- [x] 仕様・決定・現在地の整合を46テストファイル・356テストと型検査で確認し、9.2の開始点を固定する。

9.1では設計境界だけを確定し、Lab画面、数学API、共有validatorはまだ追加しない。主操作は定義域の入力と行列の編集であり、像は常に導出値として表示する。終域側の像を直接ドラッグして逆像を暗黙に選ばせる操作は、逆像が存在しない・一意でない場合があるためフェーズ9の対象外とする。3Dカメラは定義域と終域で独立に保持し、手動2D表示範囲、開いている詳細タブ、入力途中値、ドラッグ途中値は共有しない。次は9.2で描画非依存の数学ロジックを実装する。

### 9.2 線形写像の数学ロジック

- [x] `m×n`行列と`n`成分入力の検証、`\bm{A}\bm{u}`、rank、零空間基底、列空間基底を`applyLinearMap`と`analyzeLinearMap`として描画非依存に実装する。
- [x] `\mathrm{Ker}(T_{\bm{A}})`、`\mathrm{Im}(T_{\bm{A}})`、`\mathrm{rank}(T_{\bm{A}})`、`\mathrm{null}(T_{\bm{A}})`を同じrank判定から導出し、`\mathrm{dim}(U)=\mathrm{rank}(T)+\mathrm{null}(T)`を検証する。
- [x] 零写像、恒等写像、回転、鏡映、せん断、射影、`2→3`単射、`3→2`全射を専用テストに含める。
- [x] D-009の相対許容誤差、有限値と絶対値`1,000,000`上限、既存の一次結合・零空間計算を再利用し、数学判定と操作用吸着閾値を混同しない。
- [x] 47テストファイル・379テスト、型検査、Pages用ビルド、静的アセット検査で既存Labを回帰確認する。
- [x] **確認ゲート:** D-075として、APIの入力・出力、核・像の基底、rank・nullity、単射・全射、次元定理、数値境界を利用者が確認し、9.3の2D画面実装へ進む。

9.2では状態や描画を追加せず、行列の各列を標準基底の像`T(\bm{e}_i)`として既存のベクトル集合解析へ渡す。像と数学分類は入力状態から毎回導出し、共有状態へ保存しない。零空間基底は既存の一次結合`\bm{A}\bm{c}=\bm{0}`、像の基底は入力順でrankが増える元の列から決定的に求める。

### 9.3 2Dから2Dへの最小縦断版

- [x] `T: \mathbb{R}^2\rightarrow\mathbb{R}^2`を最初の操作可能例とし、入力`\bm{u}`の数値入力・矢先ドラッグと像`T(\bm{u})`をリアルタイムに連動させる。
- [x] 行列`\bm{A}`の成分編集と、標準基底の像`T(\bm{e}_1),T(\bm{e}_2)`が`\bm{A}`の列であることを対応表示する。
- [x] 恒等・回転・鏡映・せん断・拡大縮小・rank 1への退化を、代表ベクトルと切替可能な格子の像で比較する。格子は情報量が多い場合に非表示へ切り替えられる。
- [x] PC・390px幅で、行列編集、入力と像の連動、ズーム・パン、ページスクロール、横はみ出し、ブラウザ警告を既存Labと同じ操作原則で確認する。
- [x] 50テストファイル・398テスト、型検査、Pages用ビルド、静的アセット検査で既存Labを回帰確認する。
- [x] **9.3a 表記補修:** 列を並べた行列を`\bm{A}=[T(\bm{e}_1),T(\bm{e}_2)]`と角括弧で表示する。
- [x] **9.3b 対応表示・モバイル補修:** `\bm{e}_1`と`T(\bm{e}_1)`、`\bm{e}_2`と`T(\bm{e}_2)`をそれぞれ同じ枠内へ列ベクトルで表示する。定義域の重複した操作説明を削除し、390px幅でも各「全体を表示」を見出しの右隣へ小さい幅で置く。50テストファイル・399テスト、型検査、Pages用ビルド、静的検査に成功した。
- [x] **確認ゲート:** D-076として、定義域／終域の配置、入力と像の連動、行列の列と標準基底像、代表例、格子の像、PC・スマートフォン操作を利用者が確認した。

9.3では3番目のLabとして最小の`2→2`画面を追加した。入力`\bm{u}`だけを定義域側で直接編集し、像`T(\bm{u})`は終域側の導出表示とする。定義域と終域は独立してズーム・パン・全体表示できる。共有URL、Resetの共有時InitialState、核・像の幾何、異なる入出力次元は後続作業単位の責務を維持する。

### 9.4 核・像の2D/3D可視化

- [x] `2→2`から`2→3`、`3→2`、`3→3`へ広げ、次元組ごとの状態を保持し、定義域側に`\mathrm{Ker}(T)`、終域側に`\mathrm{Im}(T)`を原点・直線・平面・全空間として表示する。
- [x] 入力`\bm{u}`を動かしたとき、核上では`T(\bm{u})=\bm{0}`となること、終域で像として現れる範囲が`\mathrm{Im}(T)`であることを図・次元・文章で同時表示する。
- [x] 列ベクトル`T(\bm{e}_i)`が生成する空間と`\mathrm{Im}(T)`を結び付け、ベクトル空間Labのspan幾何を`Ker(T)`・`Im(T)`という意味ラベル付きで再利用する。
- [x] 2D/3Dのカメラ、直接操作、原点・部分空間への吸着を既存共通部品から再利用する。定義域では入力だけを編集可能、終域では全矢印を導出値として編集不可にする。
- [x] 長方形・3次行列、標準基底3本、次元組別代表例、現在の次元組だけを戻すResetを実装し、`2→2`の変形格子を回帰維持する。
- [x] **9.4a 視認性・リアルタイム追従補修:** `\bm{u}`と`T(\bm{u})`を減光対象から除外し、3D入力ドラッグ中の像を確定前座標からリアルタイム更新する。`Ker(T)`を薄い青、`Im(T)`を薄いピンクの半透明幾何へ分け、3Dカード下部の重複説明を削除し、`2→2`格子像の既定をオフにする。
- [x] 50テストファイル・408テスト、型検査、Pages用ビルド、静的アセット検査に成功し、PC実画面で4次元組、Ker/Imラベル・色分け、2D/3D混在、3D導出矢印の編集禁止とドラッグ中追従を確認する。
- [x] **確認ゲート:** D-077として、4次元組の切替、核・像の青／ピンク半透明幾何、入力の核への吸着、3Dドラッグ中の像追従、標準基底像、長方形・3次行列編集、PC・スマートフォン操作を利用者が確認した。

9.4では格子像を`2→2`だけに維持し、3D格子全体の写像は追加しない。共有URLと共有時InitialState、次元組別3Dカメラの直列化は9.6の責務を維持する。D-077は利用者確認済みである。

### 9.5 線形性と次元定理の教材表示

- [x] 2つの入力`\bm{u},\bm{w}`とスカラー`c`について、`T(\bm{u}+\bm{w})=T(\bm{u})+T(\bm{w})`、`T(c\bm{u})=cT(\bm{u})`を小さな定義域→終域の対応図と式で比較できるようにする。
- [x] `\mathrm{dim}(U)=\mathrm{rank}(T)+\mathrm{null}(T)`を、定義域の次元、像の次元、核の次元と対応する整数・色分け区間で説明する。
- [x] 単射・全射・全単射は核・像から導く補助表示とし、シラバスと`math-writing-rules.txt`の表記を再確認して独立した手入力状態を持たせない。
- [x] 表現行列は折りたたみ可能な「標準基底の像が行列の列になる」予告に留め、任意の定義域基底・終域基底による表現行列は後続候補へ分離する（D-082案ではフェーズ11）。
- [x] **9.5a カード配置・線形性表示補修:** 4カードをグラフ下の全幅タブ領域へ統合して1枚だけ表示する。「写像の線形性の確認」タブではグラフと連動する`\bm{u}`、編集可能な`\bm{w}`、`c`を3枠で並べ、写像前後の各等式を1行で表示する。狭幅では短いタブ名と1列入力へ切り替える。
- [x] 50テストファイル・416テスト、型検査、Pages用ビルドに成功し、PC実画面でrank 1例、`3→2`、4タブ、`\bm{u}`連動、式の非折返し、390px幅の1列化と横はみ出しなしを確認する。
- [x] **確認ゲート:** D-078として、`\bm{w}`・`c`の入力、加法・スカラー倍の対応図、次元定理の色分け、単射・全射・全単射の補助説明、標準基底の折りたたみ予告、PC・スマートフォン表示を利用者が確認した。

9.5では線形性確認用の`\bm{w}`と`c`を次元組別のLab状態へ保持する。主図へ矢印を増やさず専用カード内の小さな対応図で比較し、次元定理のrank側を像と同じ薄いピンク、nullity側を核と同じ薄い青に対応させる。9.5aで4カードを全幅タブへ統合し、既存Labと同じ単一カード表示へ統一した。利用者がD-078を確認し、共有URL・共有時InitialStateへの追加を9.6で実装した。

### 9.6 共有・教材例・アクセシビリティ

- [x] `linear-map` v1として行列、入出力次元、入力ベクトル`\bm{u},\bm{w}`、スカラー`c`、格子表示、次元に応じた定義域・終域カメラをURL・QR・Resetで再現し、既存2スキーマのfixtureを維持する。
- [x] 手動2D表示範囲、開いている詳細タブ、入力下書き、ドラッグ中表示を共有せず、共有URL起動後のResetを共有時InitialStateへ戻す。
- [x] 恒等、回転、鏡映、せん断、rank 1射影、零写像、`2→3`単射、`3→2`全射を代表例として整理し、核・像・rank・nullity、単射・全射・全単射の期待値を固定する。
- [x] 数値入力だけでも同じ結論へ到達できるCanvas/WebGL代替説明、次元組・詳細カードのキーボードタブ操作、390px幅、色以外の識別を確認する。
- [x] 53テストファイル・437テスト、型検査、Pages用ビルド、4アセットの静的検査に成功する。
- [x] **確認ゲート:** D-079として、共有URL・QR、別タブ復元、共有時Reset、8代表例、キーボード操作、PC・390px幅を利用者が確認した。

9.6の共有URLは現在選択中の次元組だけを保存し、URLを開いたときに線形写像Labとその次元組を直接選ぶ。他の3次元組は既定状態で開始する。3Dカメラは3D側だけ必須とし、2D側は`null`へ固定する。教材例の正本は`src/teaching/linearMapScenarios.ts`、互換候補の正本は`tests/fixtures/share-url-linear-map-v1.json`とする。

### 9.7 統合確認と棚卸し

- [x] ベクトル空間Lab、基底・次元Lab、線形写像Labの状態分離、共有URL、QR、Reset、代表例を回帰確認する。
- [x] 仕様、決定、状態、授業シナリオ、共有互換、依存関係、表記を棚卸しし、表現行列Labへ渡す責務を確定する。
- [x] 3つのLab固有共有fixtureが対象Labだけを共有InitialStateとして選び、決定的に再生成できること、全28代表例と3件のQR生成を統合テストで固定する。
- [x] 54テストファイル・446テスト、型検査、Pages用ビルド、静的アセット検査で既存機能を回帰確認する。
- [x] ローカル実画面で3つのLabの切替・状態保持、現在LabだけのReset、共有URL・QR、rank 1例の次元定理、390px幅の1列配置・横はみ出しなし、ブラウザ警告なしを確認する。
- [x] **確認ゲート:** D-080として、学生が低次元例を操作し、入力と像、核と像、rankとnullity、次元定理の関係を説明・共有できることを利用者が公開版で確認した。

9.7の棚卸しでは、3つのLabが非表示中も個別のCurrentStateを保持し、共有URLには現在Labの状態だけを含め、共有起動時は対象LabだけをInitialStateへ差し替える責務を再確認した。共有スキーマは`vector-space` v3、`basis-dimension` v1、`linear-map` v1のまま変更せず、代表例は16件・4件・8件の計28件である。依存関係、第三者通知、数式表記、シラバス第9〜10週との対応に不整合はなかった。

フェーズ9の後に、既存3Labへ0・1次元空間を補う基礎拡張を挿入する。表現行列Labへ渡すのは、0〜3次元へ一般化したrank・一次結合・基底・線形写像の数学API、1D数直線、0D一点表示、任意の定義域基底・終域基底、基底の順序、各`T(\bm{u}_i)`の終域基底に関する座標を列に並べる表現行列、標準基底の場合との比較、基底変換である。フェーズ9は標準基底の像が現在の行列`\bm{A}`の列になるところまでとし、この境界を越える状態・UI・共有スキーマは先行実装しない。

フェーズ9の実施順序9.1→9.2→9.3→9.4→9.5→9.6→9.7はすべて利用者確認済みで、フェーズ9を完了した。2Dから2Dへの縦断版を先に作り、異なる入出力次元と3D、線形性、次元定理、共有へ段階的に広げた。D-080で3つのLabの統合確認と棚卸しも完了した。

後続候補は、フェーズ10「0・1次元空間の基礎拡張」、フェーズ11「表現行列・基底変換Lab」、フェーズ12「固有値・固有空間Lab」、フェーズ13「対角化・Cayley–Hamilton Lab」、フェーズ14「内積・Gram–Schmidt Lab」の順を基本とする。第1〜3週を補う「部分空間判定Lab」「連立一次方程式・RREF Lab」は独立した補助候補として残し、シラバス順を遡って優先する授業上の理由が生じたときに再評価する。

## 11. フェーズ10「0・1次元空間の基礎拡張」の詳細作業単位案

既存3Labは2D・3Dの幾何表示を中核としてきたが、1次元空間では「非零ベクトル1本で空間全体を生成する」「2本以上は必ず一次従属になる」「線形写像は1個の係数による拡大縮小になる」という定義と幾何の対応を数直線上で直接示せる。0次元空間は通常の編集対象には乏しい一方、空集合が生成する空間、空の組が基底になること、次元定理、単射・全射の境界例を正確に扱う教材価値がある。

一方、現実装の`VectorDimension`、3Labの状態、共有validator、2D/3D分岐、線形写像の4次元組は`2 | 3`を前提としている。したがって単純なタブ追加ではなく、中核数学・描画・状態・共有を横断する中〜大規模の基礎改修として扱う。1Dは全3Labで通常操作できる第一級の表示とし、0Dは一点表示と定義上必要な固定状態を持つ制約付き教材モードとする。0Dへ2D/3Dと同じ編集量を持ち込まず、境界概念を明確にすることを優先する。

### 10.1 教育範囲、表記、状態境界の承認

- [x] `syllabus.txt`第1〜10週、`math-writing-rules.txt`、既存3Labの数学型・描画・状態・共有を照合し、追加効果と改修範囲を調査する。
- [x] フェーズ10・11の番号、D-081・D-082、対象範囲、非対象、文書間の現在地を回帰へ追加し、55テストファイル・454テスト、型検査、Pages用ビルド、静的アセット検査に成功する。
- [x] 1Dを全3Labで通常操作できる空間、0Dを制約付きの境界教材とする非対称な範囲を確認する。
- [x] 0Dを「零ベクトル空間`V=\{\bm{0}\}`」と表示し、必要な箇所だけ`\mathbb{R}^0`を補助表記する。0次元の座標を空の列、基底を空の組`()`、生成集合を`\emptyset`として扱う。
- [x] 1Dは数ベクトル`\mathbb{R}^1`と定数多項式空間`\mathbb{R}[x]_0`を対応させ、0Dでは多項式表示を設けない境界を確認する。
- [x] 既存3Labの0D/1D状態を2D/3Dと分離保持し、利用者が選んでいない次元を暗黙変換しない。0Dの固定値、数学的派生値、ドラッグ途中状態は保存対象にしない。
- [x] **確認ゲート:** D-082として、フェーズ10の挿入、1Dの通常操作、0Dの制約付き表示、空の基底・空集合の扱い、線形写像の次元選択、共有版の更新、後続フェーズの繰下げを利用者が承認した（2026-09-03）。

### 10.2 0〜3次元に対応する数学ロジック

- [x] 数学上の空間次元を`VectorSpaceDimension = 0 | 1 | 2 | 3`へ広げ、rank、一次結合、零空間、基底抽出、座標、線形写像、核・像、rank-nullityを描画から独立して扱う。描画・状態・共有で使う`VectorDimension = 2 | 3`は10.3以降まで分離して維持する。
- [x] 0Dでは唯一のベクトル、空集合のspan、空の組の一次独立性、`\mathrm{span}(\emptyset)=\{\bm{0}\}`、`\dim(\{\bm{0}\})=0`、空の組が唯一の基底であることを明示的なテストで固定する。
- [x] 1Dでは零ベクトルと非零ベクトル、複数ベクトル、一次結合の解なし・一意・無数、基底候補、比較基底を既存の相対許容誤差で解析する。
- [x] `T:U\rightarrow V`の`\dim U,\dim V∈\{0,1,2,3\}`を扱い、`m×0`・`0×n`の空行列、0Dからの唯一の写像、0Dへの零写像、rank 0、nullity、単射・全射・全単射を定義どおり返す。
- [x] 既存2D/3Dのrank、一般解、スナップ、線形写像解析結果を変更しない回帰を固定し、全56テストファイル・470テスト、型検査、Pages用ビルドに成功する。
- [ ] **確認ゲート:** 0D・1Dの代表例と0Dを含む線形写像について、定義、rank、基底、核・像、次元定理、単射・全射の結果を利用者が確認する。

### 10.3 0D一点表示と1D数直線の共通描画

- [ ] 1Dを水平な数直線として描き、原点、正負方向、目盛、矢印、ラベル、span、ターゲットを既存の数式フォント・色・線種で表示する。
- [ ] 1Dの矢先ドラッグ、数値入力、原点最優先吸着、ホイール・ピンチ・ボタンによるズーム、パン、「全体を表示」、タッチとページスクロールの分離を実装する。吸着距離は2Dと同じ表示幅2%を初期値とし、実機確認で再調整できるよう共通定数から分離する。
- [ ] 0Dは原点だけを持つ一点表示とし、固定の零ベクトル、生成する空間、編集不能の理由を図・数式・文章で示す。見かけだけ2D座標面を縮小した表示にはしない。
- [ ] 1D数直線と0D一点表示を3Labで再利用できる描画責務に分離し、PC・390px幅、キーボード、タッチ、Canvas/SVG代替説明を確認する。
- [ ] **確認ゲート:** 1D数直線の操作感、0D一点表示の理解しやすさ、2D/3Dとの切替、モバイル表示を利用者が確認する。

### 10.4 ベクトル空間Labの0D・1D拡張

- [ ] 次元切替を0D・1D・2D・3Dへ広げ、各次元のInitialState・CurrentState・入力下書き・一次結合モードを分離保持する。
- [ ] 1Dでベクトルの追加・削除・成分編集・矢先ドラッグ・span選択・一次独立判定・一次結合ターゲットを提供する。非零ベクトル1本で数直線全体、零ベクトルだけなら原点を生成することを即時表示する。
- [ ] 0Dでは固定の零ベクトル空間と`\mathrm{span}(\emptyset)`を中心にし、複数の同一零ベクトルを追加する通常編集は提供しない。空集合と`\{\bm{0}\}`の違い、その生成する空間が同じであることを説明する。
- [ ] 1Dの一次結合で、非零ベクトル1本なら任意ターゲットに唯一解、複数の生成元なら無数解、全生成元が零なら非零ターゲットを表せないことを、係数と数直線で示す。
- [ ] **確認ゲート:** 0D・1Dのspan、一次独立・一次従属、一次結合、次元切替、Resetを利用者が確認する。

### 10.5 基底・次元Labの0D・1D拡張

- [ ] 1Dで候補集合、選択順、基底の2条件、最大一次独立本数、ターゲット座標、比較基底を既存タブ構造へ接続する。
- [ ] 1Dの多項式表示を`\mathbb{R}[x]_0`として追加し、定数多項式`f(x)=b_0`と係数ベクトル`[b_0]`を対応させる。
- [ ] 0Dでは`V=\{\bm{0}\}`、基底`\mathcal{B}=()`、`\dim(V)=0`を示し、空の基底が一次独立かつ`V`を生成することを2条件から説明する。0Dの多項式モード、ターゲット編集、比較基底は設けない。
- [ ] 「基底は必ず少なくとも1本のベクトルを含む」という誤解を避ける代表例を用意し、空の座標ベクトルを無理に通常の括弧描画へ押し込まず文章と数式で説明する。
- [ ] **確認ゲート:** 1Dの基底・座標・定数多項式と、0Dの空の基底・次元0を利用者が確認する。

### 10.6 線形写像Labの0D・1D拡張

- [ ] 現在の4個の次元組タブを、定義域と終域をそれぞれ0D・1D・2D・3Dから選ぶ2段の小さなセレクタへ再構成し、16組をボタンとして一列に並べない。
- [ ] 1Dを含む写像では1D数直線と既存2D/3D図を組み合わせ、`1→1`の拡大縮小・零写像、`1→2/3`の埋め込み、`2/3→1`の線形汎関数を編集・可視化する。
- [ ] 0Dを含む写像では行列成分を編集させず、存在する唯一の写像を固定表示する。`0→m`の単射性、`n→0`の全射性、`0→0`の全単射性を核・像・rank・nullity・次元定理から説明する。
- [ ] 次元組ごとの状態を切替後も保持しつつ、0Dの空ベクトル・空行列・固定カメラを通常入力欄へ表示しない。3Dカメラは3D側だけ、2D/1D表示範囲は共有対象外とする既存原則を維持する。
- [ ] **確認ゲート:** 1Dを含む編集可能な写像、0Dを含む固定写像、16次元組の選択UI、核・像・次元定理、PC・スマートフォン表示を利用者が確認する。

### 10.7 共有状態、教材例、アクセシビリティ

- [ ] 0D・1Dを表せる新しい共有版を3Labそれぞれに定義する。正式リリース前の互換保証は開始しないが、既存`vector-space` v3、`basis-dimension` v1、`linear-map` v1は可能な範囲で読み込み、固定fixtureの意味を維持する。
- [ ] 0D・1Dの現在状態、線形写像の次元組、1D多項式表示をURL・QR・共有時InitialStateへのResetで復元する。1D手動表示範囲、入力下書き、選択中の詳細タブ、ドラッグ途中状態は共有しない。
- [ ] 1Dのspan・基底・一次結合、0Dの空の基底、`1→1`、`2→1`、`1→2`、`0→m`、`n→0`、`0→0`を代表例として整理する。
- [ ] 数直線や一点表示を見られなくても、成分、生成する空間、rank、基底条件、核・像、単射・全射を追える読み上げ要約を用意する。
- [ ] **確認ゲート:** 0D・1Dの共有URL・QR、別タブ復元、共有時Reset、キーボード、390px幅を利用者が確認する。

### 10.8 統合確認と棚卸し

- [ ] 0D・1D・2D・3Dと既存3Labの状態分離、共有URL、QR、Reset、代表例を統合回帰する。
- [ ] 数学型、空配列・空行列、0D表記、1D描画、スナップ、タッチ、共有版、固定fixture、授業シナリオ、依存関係、第三者通知を棚卸しする。
- [ ] 2D/3Dの操作感、28代表例、3共有fixture、Pages静的配信に回帰がないことを確認する。
- [ ] フェーズ11へ、0〜3次元の数学API、1D数直線、0D境界表示、定数多項式空間、次元選択UIを引き渡す。表現行列の記号と基底変換の向きは先行決定しない。
- [ ] **確認ゲート:** 学生が0D・1D・2D・3Dの違いをspan・基底・線形写像・次元定理から説明できることを利用者が公開版で確認する。

フェーズ10の推奨実施順序は10.1→10.2→10.3→10.4→10.5→10.6→10.7→10.8とする。1Dを通常操作、0Dを定義上の固定境界として分けることで教育効果を保ち、0Dへ不要な編集UIを増やさない。数学ロジックと共通描画を先に完成させてから3Labへ順に接続し、最後に共有版と既存2D/3D回帰をまとめて確認する。

## 12. フェーズ11「表現行列・基底変換Lab」の詳細作業単位案

`syllabus.txt`では第10〜11週に、基底による線形写像の行列表示を理解し、低次元の具体例から表現行列を求めることを目標とする。フェーズ8の順序付き基底・座標と多項式係数表示、フェーズ9の写像・標準基底像・定義域／終域連動、フェーズ10の0D/1D基盤を統合し、「同じ線形写像`T`でも、定義域と終域の基底および順序が変われば表現行列は変わる」ことを中心課題とする。

実装単位は、線形写像Labの詳細タブ追加ではなく第四の「表現行列・基底変換Lab」を推奨する。既存線形写像Labは標準基底と核・像・次元定理へ集中させ、新Labは2組の順序付き基底、像の座標、表現行列の列構成、基底変更前後の比較へ集中させる。数ベクトルに加えて`\mathbb{R}[x]_0`、`\mathbb{R}[x]_1`、`\mathbb{R}[x]_2`上の線形写像を扱い、多項式そのものへの写像と、選んだ基底による係数ベクトル間の行列表示を区別する。内部の数学・0D/1D/2D/3D描画・多項式整形・共有基盤は再利用するが、Lab状態と共有スキーマは分離する。

### 11.1 教材範囲、記号、状態境界の承認

- [x] シラバス第10〜11週、`math-writing-rules.txt`の表現行列、フェーズ8・9の基底・座標・線形写像実装を照合し、詳細ロードマップ案を作る。
- [x] フェーズ9完了と当初の表現行列Lab計画の文書整合性を回帰へ追加し、55テストファイル・452テスト、型検査、Pages用ビルド、静的アセット検査に成功する。
- [ ] Lab名を「表現行列・基底変換Lab」、識別子を`representation-matrix`とする推奨案を利用者が確認する。
- [ ] 数ベクトル空間の`T: \mathbb{R}^n\rightarrow\mathbb{R}^m`、`n,m∈{1,2,3}`と、`\mathbb{R}[x]_0`〜`\mathbb{R}[x]_2`上の線形写像を対象とする。0D表現行列は境界例に限定し、高次元、一般の多項式構文解析、非線形な多項式写像、関数グラフ、固有値・対角化を含めない。
- [ ] 定義域の順序付き基底と終域の順序付き基底の名称、標準座標で`T`を定義する行列と表現行列の文字、表現行列を添え字付きで略記するか長い説明名と`\bm{A}`だけにするかを決め、`math-writing-rules.txt`へ追加する。
- [ ] 基底変換行列の「どの基底の座標から、どの基底の座標へ変換するか」を表す記号と乗算方向を決める。逆向き行列との混同を避け、画面上では変換元・変換先を文章でも併記する。
- [ ] Lab固有状態を、入出力次元、各空間の数ベクトル／多項式種別、写像`T`の標準座標または標準単項式基底での定義、定義域基底、終域基底、入力、表示設定、2D/3Dカメラとする。多項式、表現行列、各像、座標、基底判定は派生値として保存しない。
- [ ] **確認ゲート:** D-081として、独立Lab、数ベクトルと多項式空間の範囲、対象次元、表記、基底変換の向き、共有境界、後続単元との境界を利用者が承認する。

11.1では画面・数学API・共有validatorを実装しない。特に行列文字と添え字記法は授業で反復して使うため、仮の記号で先行実装しない。数値表示は既存と同じ小数・有効数字方針を維持し、分数・根号の記号復元は対象外とする。

### 11.2 表現行列と基底変換の数学ロジック

- [ ] 定義域基底`(\bm{u}_1,\ldots,\bm{u}_n)`と終域基底`(\bm{v}_1,\ldots,\bm{v}_m)`を順序付きで検証し、一次従属、ベクトル数不足、次元不一致を理由別に返す。
- [ ] 各`T(\bm{u}_i)`を終域基底の一次結合として解き、その一意な座標ベクトルを第`i`列に並べて表現行列を構成する。逆行列を直接前提にせず、既存の一次結合・基底座標APIを再利用する。
- [ ] 任意の入力`\bm{x}`について、標準座標で`T(\bm{x})`を求める経路と、基底座標を表現行列へ作用させて終域基底座標を得る経路が一致することを検証する。
- [ ] 恒等写像を異なる2基底間で表した場合を基底変換として扱い、同じ基底なら単位行列、基底順序の交換なら対応する置換、逆方向なら互いに逆行列となることを検証する。
- [ ] 1D/2D/3Dの入出力、非標準基底、順序交換、恒等写像、零写像、退化写像、不正な基底、許容誤差付近、多項式の微分・`x`倍を描画非依存テストで固定する。
- [ ] **確認ゲート:** 表現行列の各列、座標での作用、基底変換の方向、失敗理由を利用者が数値例で確認する。

### 11.3 数ベクトル`2→2`最小縦断版

- [ ] 第四のLabとして共通Labメニューへ追加し、まず`T: \mathbb{R}^2\rightarrow\mathbb{R}^2`だけを操作可能にする。
- [ ] 定義域図と終域図を既存線形写像Labと同じく広幅で左右、狭幅で上下に置き、各図に標準座標上の順序付き基底、入力`\bm{x}`、像`T(\bm{x})`を区別して表示する。
- [ ] 「写像と基底」「表現行列の作り方」「座標での作用」「基底変換」の全幅タブ領域を2図の下へ置き、1枚ずつ段階的に読めるようにする。
- [ ] 各列について`T(\bm{u}_i)`を終域基底の一次結合として表示し、その係数列が表現行列の同じ列へ入ることを色・番号・数式・文章で対応付ける。
- [ ] 基底の選択順を変更したときに列または座標が変わる一方、標準座標上の写像`T`と像`T(\bm{x})`は変わらないことを常時示す。
- [ ] 基底候補を一次従属にした場合は直前値を暗黙に使わず、どちらの基底条件が崩れたかを示して表現行列を未確定にする。数値入力と矢先ドラッグの両方を許可する。
- [ ] PC・390px幅、キーボード、タッチ操作、ズーム・パン、ページスクロール、横はみ出し、数式の折返しを確認する。
- [ ] **確認ゲート:** 非標準基底の`2→2`例で、各列の導出、入力座標から像座標までの2経路、基底順序変更を利用者が確認する。

### 11.4 数ベクトルの1D/2D/3D入出力次元への拡張

- [ ] `n,m∈{1,2,3}`の9次元組へ広げ、次元組ごとの写像、2基底、入力、カメラを切替後も保持する。0Dを含む表現行列はフェーズ10の境界説明を参照できる補助例に留める。
- [ ] 2Dの直接操作と表示幅2%の吸着、3Dの画面平行面ドラッグと表示幅3%の原点・平行・同一平面吸着を再利用し、ドラッグ中も基底判定、像、表現行列をリアルタイム更新する。
- [ ] 定義域基底と終域基底を同じ色だけに依存せず、空間名、添え字、形、数式カードで識別する。3Dでは基底・像・入力を同時表示してもラベルが矢先へ重ならない配置を再利用する。
- [ ] 2D/3Dカメラと「全体を表示」を両空間で独立させ、基底変更によって視点や手動2D範囲を勝手に変えない。
- [ ] 長方形表現行列の行数が終域次元、列数が定義域次元に一致することを、図・列構成・読み上げ要約で示す。
- [ ] **確認ゲート:** 1D/2D/3Dの9次元組、基底の直接編集、長方形表現行列、3D視点、モバイル操作を利用者が確認する。

### 11.5 座標の可換関係と基底変換エクスプローラ

- [ ] 入力`\bm{x}`から`T(\bm{x})`へ進む標準座標の経路と、入力の基底座標から表現行列を経て像の基底座標へ進む経路を、過密な主図ではなく専用カード内の対応図で示す。
- [ ] 両経路の途中値を列ベクトルで表示し、最終的に同じ`T(\bm{x})`へ到達することを数値と日本語で説明する。等号・近似記号は既存の丸め規則に従う。
- [ ] 恒等写像を選ぶ「基底変換」表示では、同じベクトルの2つの座標を並べ、変換元→変換先の行列と逆向きの行列を切り替えて比較する。
- [ ] 基底の順序だけを入れ替える例、斜交基底、標準基底との変換、変換して戻すと元の座標へ戻る例を用意する。
- [ ] 固有値・固有ベクトルや対角化は説明予告に留め、特定基底で表現行列が対角になる理由の実装はフェーズ12〜13へ送る。
- [ ] **確認ゲート:** 学生が「同じベクトル・同じ写像」と「それを表す座標・行列」の違い、変換方向と逆変換を説明できることを利用者が確認する。

### 11.6 多項式空間上の線形写像と表現行列

- [ ] `\mathbb{R}[x]_0`、`\mathbb{R}[x]_1`、`\mathbb{R}[x]_2`を、それぞれ標準単項式基底`(1)`、`(1,x)`、`(1,x,x^2)`により1D・2D・3D係数空間と対応させる。
- [ ] `f(x)=(b_1(x),\ldots,b_n(x))[f]_{\mathcal{B}}`と`T(f(x))=(c_1(x),\ldots,c_m(x))[T(f)]_{\mathcal{C}}`を示し、多項式そのものへの`T`と、係数ベクトルに作用する表現行列を同一視しない説明を置く。
- [ ] 基底を変更しても多項式`f(x)`と写像`T`は同じまま、`[f]_{\mathcal{B}}`、`[T(f)]_{\mathcal{C}}`、表現行列が変わることを、多項式表示と係数空間の矢印で同期して示す。
- [ ] 微分`D:\mathbb{R}[x]_2\rightarrow\mathbb{R}[x]_1`、`x`倍`M_x:\mathbb{R}[x]_1\rightarrow\mathbb{R}[x]_2`、平行移動`T(f)(x)=f(x+1)`、恒等写像による基底変換を代表例にする。全例が線形であることを数学APIで確認する。
- [ ] 既存の多項式整形と係数空間描画を再利用し、文字式入力、一般式の構文解析、関数グラフ、3次以上、`f\mapsto f^2`のような非線形写像は対象外とする。
- [ ] **確認ゲート:** 同じ多項式・同じ線形写像に対して、基底により係数ベクトルと表現行列が変わること、標準座標経路と基底座標経路が一致することを利用者が確認する。

### 11.7 共有状態、URL、QR、Reset

- [ ] フェーズ10終了時の3スキーマと独立した`representation-matrix` v1を定義し、現在の次元組、各空間の数ベクトル／多項式種別、写像、定義域基底、終域基底、入力、表示設定、3Dカメラを厳密に検証する。
- [ ] 手動2D表示範囲、詳細タブ、入力下書き、hover、drag preview、表現行列などの派生値は共有しない。
- [ ] 共有URLから第四のLabと保存した次元組を直接開き、その状態をInitialStateとする。Resetは共有時InitialStateへ戻し、他の次元組と他Labを変更しない。
- [ ] 完全URL2048文字、受信状態8192文字、未知版・未知フィールド・過大値の拒否、QRコードPNG生成、公開サブパス維持を確認する。
- [ ] フェーズ10終了時の3Lab固定fixtureを変更せず、第四の固定fixtureを追加する。
- [ ] **確認ゲート:** 1D/2D/3Dと多項式空間の共有URL・QR、別タブ復元、共有時Reset、他Labの状態分離を利用者が確認する。

### 11.8 代表例、授業導線、アクセシビリティ

- [ ] 標準基底同士、同じ非標準基底、異なる2基底、基底順序交換、`1→1`、`2→3`埋め込み、`3→2`射影、恒等写像による基底変換、基底でない候補、多項式の微分・`x`倍を、重複を抑えた代表例として整理する。
- [ ] 各例へ「写像は同じか」「基底はどれか」「各列は何を表すか」「変換の向きはどちらか」という観察課題と期待値を持たせる。
- [ ] 数値入力、表現行列の列、座標の2経路、基底不成立理由を図なしでも追える読み上げ要約を用意する。タブ、次元組、基底順序変更をキーボードで操作できるようにする。
- [ ] フェーズ10終了時の代表例・3共有fixture、3LabのPC・390px幅、Canvas/WebGL代替導線、静的配布を回帰確認する。
- [ ] **確認ゲート:** 教員が代表例をURL・QRで配布し、学生が列構成、座標経路、基底変換を説明してResetで配布状態へ戻せることを利用者が確認する。

### 11.9 統合確認と棚卸し

- [ ] 4つのLabの状態分離、共有URL、QR、Reset、代表例を統合回帰する。
- [ ] 仕様、決定、状態、数学API、2D/3D描画、授業シナリオ、共有互換、依存関係、第三者通知、数式表記を棚卸しする。
- [ ] 表現行列の記号、基底順序、変換方向、標準座標との関係がすべての画面・文書・テストで一致することを確認する。
- [ ] フェーズ12「固有値・固有空間Lab」へ渡す責務を、線形変換`T: U→U`、固有値・固有ベクトル・固有空間、実数体、対角化を先行しない境界として記録する。
- [ ] **確認ゲート:** 低次元の代表例を操作・共有し、同じ写像に対する複数の表現行列と基底変換を説明できることを利用者が公開版で確認する。

フェーズ11の推奨実施順序は11.1→11.2→11.3→11.4→11.5→11.6→11.7→11.8→11.9とする。記号と変換方向を11.1で確定してから数学APIへ進み、数ベクトル`2→2`の縦断版で教材表現を確認してから1D・3D・多項式・共有へ広げる。多項式対応は任意追加ではなく、数ベクトルと同じ座標・表現行列の原理を抽象対象で確認する中核単位とする。

## 13. 文書更新と棚卸し

各作業単位の終了時に、少なくとも次を確認する。

- `docs/PROJECT_STATUS.md`: 完了事項、検証結果、次の一手を更新する。
- `docs/DECISIONS.md`: 新しい決定、変更、却下理由を記録する。
- `SPEC.md`: 要件または受入条件が変わった場合だけ更新する。
- `ROADMAP.md`: 順序、範囲、確認ゲートが変わった場合に更新する。
- D-030に従い、検証済みの変更をmainへcommit・pushし、GitHub Pagesのデプロイ結果を確認する。

文書全体の棚卸しは、各フェーズ終了時、または小さな作業単位を 3 個完了するごとに行う。利用者から依頼があった場合は、この周期を待たずに実施する。棚卸しでは、仕様と実装、受入条件とテスト、ロードマップと現在地、文書間リンク、古くなった保留事項を照合する。

## 14. 初版完了の目安

初版候補は、`SPEC.md` の CORE-01〜CORE-07 とAC-01〜AC-45を満たし、静的配信で2D/3Dの代表例を共有・復元できる状態としてフェーズ6で完成した。授業実施が先であるため、正式な初版完了、`1.0.0`、v3互換保証開始の判断は延期中のフェーズ7まで保留する。フェーズ8以降の追加Labは初版候補と同じアプリへ追加するが、正式リリース判断を暗黙に代替しない。
