# プロジェクト状況

最終更新: 2026-08-25

## 現在地

- 完了したフェーズ: 0「要件と進め方の確立」、1「状態・数学ロジックの基礎」、2「2D の最小縦断版」、3「2D 教材版の完成」、4「2D 一次結合エクスプローラ」、5「3D教材版」、6「共有と配布の仕上げ」
- 現在地: 初版候補`1.0.0-rc.1`。フェーズ7は授業実施まで延期し、フェーズ8の8.2と追加補修D-057〜D-060を利用者確認済み。8.2cの統合棚卸しを完了
- 完了・利用者確認済みの作業単位: 0.1、0.2、1.1、1.2、1.3、2.1〜2.7、3.1〜3.3、4.1〜4.6、5.1〜5.8、6.1〜6.7、8.1、8.1a、8.2、8.2a、8.2b、8.2c
- 完了した統合棚卸し: 2.7「フェーズ2統合確認」、フェーズ3終了時、フェーズ4終了時、5.8「フェーズ5統合確認」、6.7「フェーズ6統合確認」、8.2c「フェーズ8開始後の統合棚卸し」
- 次の確認ゲート: 8.3で基底・次元の数学ロジック、基底でない理由の分類、教科書準拠の基底・座標表記を仕様化して確認する
- 配布・公開: GitHub Pagesへ公開済み。本番URLは `https://d-kitamura.github.io/linear-algebra-visual-lab/`。mainへのpushごとにActionsがテスト・ビルド・デプロイする
- Git 運用: D-030により、Codexが検証後にmainへcommit・pushし、利用可能な範囲でActionsとPagesの反映を確認する

## 決定済み・試行採用の方針

- D-001〜D-008 は 2026-08-19 に提案どおり承認された。
- D-009 の相対許容誤差 `1e-10` は2Dの操作テスト後も維持し、直接操作の補助は D-019 と分離した。
- D-010 の共有状態 v1 と安全上限は利用者確認済みの試行採用とした。
- D-011 として、GitHub Pages または学内 nginx を公開候補として記録した。
- D-012 の 2D 初期表示範囲 `[-5, 5] × [-5, 5]` と固定例は利用者確認済みの試行採用とした。
- D-013 として、2D の原則的な自動スケールと、ホイール／ピンチによる手動ズームを決定した。
- D-014 の自動追従と手動表示の切替、ドラッグ中の扱いは利用者確認済みの試行採用とした。表示範囲の共有方針はD-023で置換した。
- D-015 の `math-writing-rules.txt` は利用者確認済みの数学用語・数式表記基準とした。
- D-016 の数値入力、自動範囲、適応目盛、初期の数式フォントは利用者のブラウザ操作確認済みとした。
- D-017 の手動表示モード、ホイール・ピンチ・パン・画面ボタン、倍率安全範囲は利用者のブラウザ操作確認済みとした。
- D-018 の先端ドラッグは利用者確認済みであり、平行に合わせにくい問題は D-019 で解決した。
- D-019 の画面幅 `1%` の平行スナップは利用者の操作確認済みとした。
- D-020 のspan選択、rank 0・1・2の幾何表現、選択集合と全ベクトルの判定分離、既定の幾何表示オンは利用者確認済みとした。
- D-021として、常時表示の2要約、3タブの詳細インスペクター、条件付きsticky座標面、タブレット・スマートフォンの1列配置を試行採用し、利用者確認済みとした。
- D-022として、URL欄、コピー、テキスト保存、閉じる操作を持つレスポンシブな共有URLダイアログを決定・実装し、利用者確認済みとした。
- D-023として、2D手動表示範囲を共有せず、復元時に自動範囲を再計算し、共有状態v1を維持する方針を決定・実装し、利用者確認済みとした。
- D-024として、`state`パラメーター、有効・不正URLのInitialState、既定例へのフォールバック、InitialStateへのResetを決定・実装し、利用者確認済みとした。
- D-025として、GitHub Actionsがmainをpnpmでテスト・ビルドし、`dist/`をGitHub Pagesへ公開する方式を決定・実装した。利用者が初回公開と本番操作を確認済みである。
- D-026として、2Dのベクトル集合を0〜8本で編集し、追加時は既定座標 `(1, 0)`、span選択済み、自動命名、表示順の固定色とする方針を決定・実装した。デプロイ先で利用者確認済みである。
- D-027として、未確定入力・共有URLエラーの案内、スキップリンク、数式読み上げ、タッチ領域、5つの2D授業シナリオ、共有URLによる授業手順、3Dへの再利用境界を決定・実装した。利用者確認済みである。
- D-028として、SVG座標面全体がモバイルの先端ドラッグ・パン・ピンチを所有し、座標面外ではページスクロール・ページズームを維持する方針を決定・実装した。実端末で利用者確認済みである。
- D-029として、生成する空間のrank 0・1・2の幾何表示を、ベクトル色から独立した灰色系へ変更した。利用者確認済みである。
- D-030として、各作業単位の検証と文書更新後にCodexがmainへのcommit・pushとPagesデプロイ確認まで担当する日常運用へ変更した。
- D-031として、3D教材版の前にフェーズ4「2D一次結合エクスプローラ」を挿入した。既存span選択、共有するターゲット、Reset、小数係数、折り畳み一般式、画面幅1%吸着、原点基準の平行四辺形、無数解の2例と一般解を採用した。
- D-031の共有状態v2として、`linearCombination.visible` と次元に一致する `linearCombination.target`（未配置は `null`）を追加した。旧v1は厳密に検証して非表示・未配置を補い、新しいURLは常にv2で生成する。
- D-032として、正式リリース前の共有URLには後方互換性を保証せず、正式リリース以後に保証を開始する。現行v1読込はv2の出力データとURL長へ影響しないため残すが、今後の設計を制約しない。
- D-033として、一次結合結果を不能・唯一解・不定で表示し、係数は有効数字6桁、丸めがある式だけ近似記号とする。一般のターゲット公式は既定で折り畳み、平行四辺形は選択がちょうど2本で表現可能な場合だけ描く。
- D-034として、通常ベクトルを `a₁`〜`a₈`、選択集合を `S`、列行列を `A`、ターゲットを `v` へ統一した。主式と係数列の転置横書きを併記し、不定の2例を常時表示、一般解だけを折り畳み、特解・同次解方向の導出理由と展開した一次結合を加えた。
- D-035として、一次結合の4代表例をターゲット付き共有URLとして追加し、4.5の統合確認とフェーズ4終了を記録した。
- D-036として、共有URLをブラウザ内でQRコード化し、ダイアログ表示と時刻付きPNG保存へ対応した。外部QR生成サービスは使わない。
- D-037として、2D/3Dのセグメント型タブと状態分離、現在次元だけの共有・Reset、右手座標系、z軸上向きの正投影、カメラ共有、数値編集先行、操作モードと軸拘束による直接編集試作、2項平行四辺形と3項平行六面体試作を決定した。
- D-038として、Three.jsの固定3D表示、正投影と4視点・全体表示、回転・ズーム・パン、オンデマンド描画、遅延ロード、レスポンシブ・高DPI・WebGL失敗・リソース解放を決定・実装した。
- D-039として、0〜8本の3D数値・集合編集、rank・span解析、自動3D範囲、共有状態v3のカメラ方向・注視点・上方向・拡大率、v1・v2移行、共有URL・QR・Reset復元を決定・実装した。
- D-040として、rank 0の原点強調、rank 1の太い破線、rank 2の格子なし両面半透明平面、rank 3の半透明空間・実線の外枠Box、表示切替と未選択ベクトル減光を決定・実装した。
- D-041として、3Dターゲットのx・y・z数値入力、2D共通の一次結合解析、2項の平行四辺形、3項第1係数例の平行六面体試行、退化判定、4本以上の数式限定表示、自動範囲・共有・Resetへの統合を決定・実装した。
- D-042として操作モードとx・y・z軸拘束ハンドルを初回試作したが、利用者評価でモード切替の負担が判明したためD-043で置換した。
- D-043として、矢先／背景の開始位置による自動操作分離、画面平行面ドラッグ、画面距離ヒット領域、元位置・ガイド・新矢印・3成分プレビュー、終了時確定を決定・試作実装した。
- D-043の統一操作は利用者確認で操作しやすいと評価され、本採用とした。
- D-044として、3D通常ベクトルのドラッグ中だけ、他の1本が張る直線への平行スナップを優先し、次に独立な2本が張る平面への同一平面スナップを適用することを決定・実装した。当初の表示幅1%はD-060で3%へ改定した。
- D-044のスナップは利用者確認で良好と評価され、採用した。
- D-045として、span対象ベクトルのドラッグ中にプレビュー座標からrankを再計算し、灰色のspan形状、グラフ内ラベル、操作中の状態表示をリアルタイム更新することを決定・実装した。
- D-046として、一次結合モード中の短い背景クリック・タップを、原点を通り現在の画面に平行な面上の3Dターゲット配置とすることを決定・実装した。8px超の背景移動は視点回転、2点操作はズーム・パン、通常ベクトルの矢先操作はベクトル編集を優先する。
- D-046のワンタップ配置は利用者確認で問題なしと評価され、採用した。
- D-047として、配置済み3Dターゲットの矢先を画面平行面内でドラッグし、ターゲット矢印、平行四辺形・平行六面体、係数倍ベクトルとラベルをドラッグ中にリアルタイム更新することを決定・実装した。
- D-045とD-047はPC・スマートフォンの操作確認で問題なしと評価され、5.6を完了した。
- D-048として、rank 0〜3、一次独立・一次従属、一次結合の不能・唯一解・不定を網羅する3D代表例7件を、3Dカメラ付きの共有状態v3、本番URL、期待結果、授業手順とともに定義した。
- Canvasを補助的な可視化と明示する代替説明を3D表示へ関連付け、WebGL失敗時も数値入力、解析カード、共有URL、Resetを利用できることを画面内に案内する。
- D-048の代表例、端末回転、Canvas内外の操作、URL・QR・Reset復元、平行六面体の追従は公開版のPC・スマートフォンで利用者確認済みとした。
- D-049として、5.8の2D回帰、責務境界、置換した試作、再評価条件、文書・依存関係の統合棚卸しを完了し、フェーズ5を終了した。
- D-050として、完全共有URLの授業用上限を2048文字、受信状態の防御上限を8192文字として分離した。代表例の現行v3生成は286〜612文字、8本・3D境界状態は1309文字でQR生成にも成功したため、圧縮は採用しない。
- D-051として、v3を最初の正式保証版、本アプリ提供期間中は期限なし、現在までの有効v3も対象とする方針を利用者が承認した。固定v3 fixtureと互換表を追加し、正式リリースと保証開始はフェーズ7まで行わない。
- D-052として、GitHub Pagesを初版の正規配布先、nginx実配置を任意とした。ルート・任意サブパス・Pagesの実ビルドと静的アセット検証、Actionsのartifact検証、nginx設定ガイドを追加した。
- 初版は TypeScript、Vite、React の完全クライアントサイド静的アプリとする。
- 共有状態は検証済みの UTF-8 JSON をパディングなしの Base64URL 形式で格納する。

## 作業単位 2.5 の変更

- 各ベクトルの編集欄に「spanの対象に含める」チェックを追加し、空集合を含む任意の部分集合を選択できるようにした。
- 選択集合を `S`、選択した列ベクトルを並べた行列を `A` と定義し、`rank(A)`、`dim(span(S))`、一次独立・一次従属、幾何学的な形を一つのカードで同期表示する。
- rank 0は原点の二重マーカー、rank 1は原点を通る太い破線、rank 2は座標面全体の薄い面と斜線パターンで表示する。
- パンで原点が表示外にある場合を含め、原点を通る直線と現在の表示矩形との交差区間を計算して描画する。
- 幾何表示のオン・オフを追加し、オフにしても選択集合と解析結果を維持する。既定例は2本を選択し、表示オンとした。
- 幾何表示中は未選択ベクトルを薄くし、チェック、形、線種、パターン、日本語ラベル、説明文を併用して色だけに依存しないようにした。
- 全ベクトルを列に持つ行列 `A` の一次独立・一次従属カードを別に保ち、選択集合の判定と明確に区別した。
- 既存の共有状態 v1 の `spanSelection` と `visualization.showSpan` を利用し、共有スキーマは変更していない。
- 座標面内の「生成する空間」ラベル枠を文字列長に応じた可変幅とし、「2次元座標平面全体」が枠をはみ出さないようにした。
- 選択集合の次元・形と全ベクトルのrank・判定を常時表示する要約へ分離し、要約から対応する詳細を開けるようにした。
- 3つの詳細カードを「ベクトル編集」「生成する空間」「全ベクトル」のタブへまとめ、既定では編集だけを表示するようにした。
- PCの十分な高さでは座標面をsticky表示し、タブレット以下では座標面、要約、タブ、詳細の1列配置へ切り替えるようにした。
- タブへARIA属性と左右矢印・Home・Endキー操作を追加した。選択タブは共有対象外の一時的UI状態とした。

## 作業単位 2.6 の変更

- 2D Lab上部に「共有URLをエクスポート」とResetを追加した。未確定の不正な座標文字列がある間はエクスポートを無効にする。
- 現在のoriginと公開サブパスを使い、既存クエリーとハッシュを除いた `?state=...` の共有URLを生成する。閲覧中URLとブラウザ履歴は変更しない。
- ネイティブなモーダルダイアログに、選択可能なURL欄、「クリップボードにコピー」「テキストで保存」「閉じる」を配置した。狭い画面ではボタンを縦並びにする。
- Clipboard APIが使えない場合やコピーに失敗した場合は、URLを選択して手動コピーを案内する。テキスト保存はURLと末尾改行だけをUTF-8でダウンロードする。
- 共有URLからベクトル、span選択、幾何表示を復元する。手動表示範囲と詳細タブは共有せず、表示は常に自動範囲から開始する。
- 2.6時点では`state`なしを既定例、有効な2D v1を復元結果、不正・重複・未対応または3D状態を既定例とした。D-037以後は有効な3D状態も3DのInitialStateとして受け入れる。
- ResetはInitialStateへ戻し、入力欄、自動表示、ベクトル編集タブ、ドラッグ・吸着・エクスポートの一時状態を同期する。
- URL構築と読込を `src/sharing/shareUrl.ts`、アプリ初期化を `src/state/appInitialization.ts` へ分離し、描画と独立してテスト可能にした。

## 作業単位 2.7 の統合棚卸し（2026-08-19）

- 2.2〜2.6で積み上げた利用者のPC・マウス・キーボード・タッチ相当の操作確認と、自動テスト、型検査、静的ビルドを統合してフェーズ2の終了条件を確認した。
- D-009の相対許容誤差 `1e-10`、D-012の固定例と初期表示範囲、D-013の自動スケールと手動ズーム、D-014の自動／手動表示の切替を再評価し、利用者の指示どおり現状を維持した。共有に関するD-014の旧方針だけはD-023を正とする。
- 実装、テスト、`SPEC.md`、`ROADMAP.md`、`docs/DECISIONS.md`、本書、`README.md`、`math-writing-rules.txt`、`syllabus.txt` を横断して照合した。
- 空のベクトル集合を共有状態から復元した場合、全ベクトルの判定を「空集合は一次独立です」と明示し、行列 `A` を「列を持たない空行列」と読み上げるようにした。数学ロジックのrank 0判定は変更していない。
- Reset時に、以前生成した共有URLの一時状態も消去するようにし、D-024の「エクスポート状態を消去する」と実装を一致させた。
- 数学用語と表記は、列ベクトル、ベクトル基本文字だけのボールド斜体、添え字数字の立体レギュラー、`rank(A)`、`dim(span(S))`、一次独立・一次従属、集合と列順の区別について基準と一致している。
- シラバス第1〜7週のベクトル空間、一次独立、rank、基底に対する2Dの中核教材を成立させた。線形写像以降と3Dは後続フェーズで扱う。
- 以上により2.7とフェーズ2を完了し、フェーズ3は作業単位案の確認後に着手する。

## 作業単位 6.1 の変更

- `.github/workflows/deploy-pages.yml` を追加し、mainへのpushとmainを選んだ手動実行だけを公開起点とした。
- `packageManager`に従うpnpm 11、Node.js 24、`pnpm install --frozen-lockfile`、`pnpm test`、公開用base pathでの `pnpm build` を順番に実行する。
- GitHub Pagesに必要な最小権限だけを付与し、`dist/`だけをPages artifactとして `github-pages` 環境へデプロイする。
- GitHub公式・Vite公式が示すActionsをcommit SHAで固定し、pnpm公式のpnpm 11向けsetup Actionを使用した。
- `docs/GITHUB_PAGES.md` に処理の意味、初回のGitHub操作、本番確認、通常更新、設定変更時の見直し事項を記録した。
- ローカルでロックファイル整合、93テスト、型検査、公開用ビルドに成功した。生成HTML、JavaScript、CSSを `/linear-algebra-visual-lab/` 以下で静的配信し、すべてHTTP 200になることを確認した。
- 利用者がPages設定、Actionsの成功、本番画面・再読み込み、共有URLの生成・復元、Reset、コピー、テキスト保存を確認し、現在の6.1を完了した。
- フェーズ4挿入により、当時5.1と呼んでいたこの作業単位は現在6.1である。実装内容と完了状態は変わらない。

## 作業単位 3.1 の変更

- `src/state/vectorCollection.ts` に描画非依存の追加・削除処理を分離した。0本を許可し、共有上限8本では追加を拒否する。
- 追加時は `(1, 0)`、span選択済みとし、既存ID・表示名の双方と衝突しない最小番号で `a3` / `a₃` のように命名する。削除で空いた番号は再利用できる。
- 編集カードへ本数表示、追加、各ベクトルの削除、上限案内、空集合の案内を追加した。8色の固定パレットを表示順に割り当て、3.1では名前と色を編集しない。
- 削除時は対応するspan選択、入力途中状態、ドラッグ・吸着状態も除き、追加ボタンへフォーカスを戻す。行列は8列で横スクロールでき、空集合の座標面説明も整えた。
- 共有状態v1は変更せず、編集後の共有URL復元とInitialStateへのResetを維持した。
- 利用者がデプロイ先で表示、0〜8本の操作、解析、共有URLを確認し、3.1を完了した。

## 作業単位 3.2・3.3 の変更

- 未確定入力の総数と影響を画面上部へ表示し、「入力欄を確認」でベクトル編集タブの最初の該当欄へ移動できるようにした。各成分は固有のエラー説明を持ち、数学状態には直前の有効値を維持する。
- 共有URLの読込・生成エラーに固有の見出しと閉じる操作名を付け、不正URLの理由と既定例へのフォールバックを明示した。上限8本の案内も状態変化として読み上げられる。
- 教材操作領域へのスキップリンクを追加し、主要ボタン・入力・選択操作を44px以上へ拡大した。数値添え字は「添え字」と明示して読み上げ、既存のタブ・dialog・色以外の識別を再点検した。
- `src/teaching/twoDimensionalScenarios.ts` に空集合、零ベクトル、平行な2本、一次独立な2本、3本以上の一次従属の状態、学習目的、期待結果を定義した。
- `docs/TEACHING_SCENARIOS.md` に各代表例の本番共有URL、期待結果、観察点、配布から編集・再共有・Resetまでの授業手順を記録した。文書の全リンクと状態定義の一致も自動テストする。
- 3Dで再利用するベクトル・数学・共有・集合編集・教材シナリオと、2D固有のSVG・ジェスチャー・平行スナップ・初期化ゲートを棚卸しした。
- スマートフォン確認で、背景矩形と先端操作領域だけの `touch-action: none` ではブラウザが途中からページ操作を優先する問題が見つかった。D-028としてSVG座標面全体へ指定を広げ、学生向け操作案内も更新した。
- 利用者がD-028の再デプロイ後にスマートフォンで期待どおり操作できることを確認した。
- D-029として、rank 0の原点、rank 1の太い破線、rank 2の背景・斜線、座標面内ラベルを中立的な灰色系へ統一した。ベクトルの8色と形・線種・説明は変更していない。

## 作業単位 4.1 の変更

- `src/domain/linearCombination.ts` に、選択ベクトルを列に持つ `A` とターゲット `v` について `Ac = v` を解析する描画非依存ロジックを追加した。
- D-009と同じ列ごとの正規化と相対許容誤差を使い、`rank(A)` と `rank([A | v])` から表現不可能・一意・無数を分類する。
- 表現可能な場合は特解を返し、無数の場合は自由係数の数、正規化した零空間基底、特解と零空間方向から作る異なる第2例を返す。
- 空集合と零ターゲットは空の係数列による一意な表現、零ベクトルと零ターゲットは任意係数による無数の表現として区別する。
- 2D専用にせず3Dにも対応させ、フェーズ5の3D一次結合エクスプローラから同じ関数を再利用できるようにした。
- 15件の専用テストで標準基底、非直観的な一次独立対、span外、一次従属対、3本、上限8本、1本、零ベクトル、空集合、3D、許容誤差、入力検証を固定した。

## 作業単位 4.2 の変更

- `ShareStateV2` に `linearCombination: { visible, target }` を追加し、アプリのCurrentState、既定例、教材シナリオ、URL初期化、集合編集を現行v2へ統一した。4.2ではターゲット操作UIを追加せず、共有可能な基盤だけを接続した。
- `target` は現在の次元に一致する有限座標または `null` とし、ベクトル座標と同じ絶対値上限 `1,000,000`、`-0` の正規化、未知フィールド拒否を適用する。`visible` と `target` は独立して保持する。
- 旧v1の厳密なスキーマ検証を残し、正常なv1は `{ visible: false, target: null }` を補ったv2へ移行する。エクスポートと教材シナリオの本番リンクは常にv2を生成する。
- 有効なv2 URLはターゲットを含めてInitialStateとなり、既存のResetがそのInitialState全体を復元する。ベクトル追加・削除は一次結合状態を失わない。
- v1移行、2D/3Dターゲット、`null`、不正な型・次元・上限、決定的エンコード、公開サブパス、URL初期化、教材文書のv2リンクを自動テストで固定した。

## 作業単位 4.3 の変更

- 座標面ヘッダーに「一次結合を調べる」モードを追加し、モード中はターゲット `v` の数値入力欄と消去操作を表示する。`v` は通常ベクトルと区別できる深い青の破線矢印、ボールド斜体の基本文字として描画する。
- 背景の短いクリック・タップはターゲット配置、一定距離を超える1点移動はパン、2点操作はピンチとして区別した。ターゲット先端と通常ベクトル先端は独立したポインターキャプチャでドラッグできる。
- ターゲットの配置・先端ドラッグ・2成分の数値入力を共有状態と同期する。未配置は両欄が空の正規状態で、片方だけ未確定なら直前の有効ターゲットを保ち、訂正までエクスポートを停止する。
- 自動表示では表示中のターゲットも範囲計算へ含め、ドラッグ中は従来どおり表示範囲を固定する。非表示でも保持しているターゲットは表示範囲へ含めない。
- `src/state/targetSnapping.ts` に、rank 0の原点とrank 1の生成直線へ画面幅1%以内で吸着する描画非依存ロジックを追加した。rank 2では吸着せず、数値入力にも吸着を適用しないため、D-009の数学判定と直接操作補助を分離している。
- ローカルのブラウザ自動操作はWindows短縮パスへの権限制限で接続できなかった。代わりにViteの入口・React変換HTTP 200、型検査、単体テスト、公開用ビルドを確認し、実操作はデプロイ先での利用者確認ゲートに残す。

## 作業単位 4.4 の変更

- 一次結合モードを開くと解析欄へ専用の「一次結合」タブを追加し、自動的に選択する。共有URLの初期状態またはReset後にモードが表示中なら、このタブを初期表示する。
- `Ac = v` を係数行列と拡大係数行列のrankとともに示し、「不能（解なし）」「唯一解」「不定（解が無数）」を、一次結合で表現できるか・表し方が一意かという日本語へ対応付けた。
- 表現可能なら係数列と展開した一次結合を表示する。数は有効数字6桁へ整形し、丸めが生じる式だけ等号を近似記号へ切り替える。
- D-034で不定の異なる2例を初期表示へ変更し、自由係数の数と一般解だけを折り畳む。一般解には、自由係数0から得る特解、`An_j = 0` の零空間方向、`A(c_p + Σt_jn_j) = v` の理由、展開した一次結合を示す。
- 2本が一次独立なら、一般のターゲット `{}^t[a, b]` に対する2×2逆行列の係数公式を別の折り畳みに表示する。開閉状態は共有対象外である。
- 選択がちょうど2本で表現可能なら、特解の `c₁a₁` と `c₂a₂` を原点から描き、平行な対辺を補う。一次従属では退化した平行四辺形として説明し、3本以上では描画しない。
- `v = Ac = (a₁, …, aₙ)[c₁; …; cₙ]`、`c = {}^t[c₁, …, cₙ]`、係数記号列と値列、記号による一次結合と値を代入した一次結合を段階的に表示する。数式項は0〜8本と長い値に応じて折り返す。
- D-034の利用者指摘を反映し、主式・係数列・直下の係数転置表示を同じ青色、数式フォント、文字サイズへ統一した。一般ターゲット公式では、説明中の `{}^t[a, b]` は維持し、逆行列の積の右端だけを縦の列ベクトルへ変更し、逆行列係数を上下分数で表示する。
- ローカルブラウザで未配置・不能・唯一解・不定・3本選択、折り畳み、デスクトップと幅390px相当を確認した。D-034改訂後は390px CSSビューポートで `scrollWidth` が本文幅を超えず、一般解を展開しても数式カード内に収まることを実測した。追加の表示確認では主式・係数列・転置表示の計算済み色、フォント、20.48pxの文字サイズが一致し、一般式の列ベクトルと上下分数も確認した。

## 作業単位 4.5・フェーズ4終了時の統合棚卸し（2026-08-20）

- 利用者が4.4の分類、係数、一般解、一般ターゲット公式、平行四辺形を確認し、問題なしとした。4.5に列挙した共有復元、Reset、レスポンシブ配置、アクセシビリティ、PC・タブレット・スマートフォンでの一連の操作も、確認可能な範囲ですべて問題なしとした。
- `LINEAR_COMBINATION_TEACHING_SCENARIOS` に、1本で一意、一次従属な2本で不能、一次独立な2本で唯一解、3本で不定となる4つのターゲット付き状態を追加した。既存5例と合わせ、本番共有URLから授業と回帰確認に直接使える。
- 4例ごとにベクトル集合のrank・一次独立性、`rank(A)`・`rank([A | v])`・解分類、共有URLの往復、文書リンクとの一致を自動検証した。画面の進捗表示も「フェーズ4完了」へ更新した。
- 3Dで再利用する責務を、`analyzeLinearCombination`、ターゲットを含む共有状態、不能・唯一解・不定、特解・2例・自由係数・零空間一般解、教材シナリオ構造とした。2D固有の責務を、SVG座標面、2Dポインター変換、直線・原点への吸着、原点基準の平行四辺形、2×2逆行列の一般ターゲット公式とした。
- `SPEC.md`、`ROADMAP.md`、`README.md`、`docs/DECISIONS.md`、本書、`docs/TEACHING_SCENARIOS.md`、実装、テスト、`math-writing-rules.txt`、`syllabus.txt` を横断して照合した。ROADMAPとSPECに残っていた「不定では一例を既定表示」というD-034以前の記述を、異なる2例の常時表示へ修正した。
- 19ファイル・161テスト、TypeScript型検査、静的ビルドに成功した。以上により4.1〜4.5の実装・利用者確認・統合棚卸しが揃い、フェーズ4を完了した。

## 追加作業 4.6 の変更

- 共有URLダイアログのURL欄の上へ、現在の共有URLを表すQRコードを追加した。QR生成は `qrcode` 1.5.4を使ってブラウザ内だけで行い、共有URLを外部サービスへ送信しない。
- QR画像は768×768 PNG、誤り訂正レベルM、余白4モジュール、高コントラスト配色とした。画面表示と保存に同じデータを使い、「QRコードを保存」から時刻付きPNGファイル名でダウンロードする。
- ボタン順を「クリップボードにコピー」「QRコードを保存」「テキストで保存」「閉じる」とした。デスクトップは4列、640px以下は1列で表示する。
- QR画像に代替テキストを付け、生成中・失敗を読み上げ可能にした。生成中はQR保存だけを無効にし、容量超過等で失敗してもURLのコピー、テキスト保存、閉じる操作は維持する。
- PNG署名を含むQR生成、空URL拒否、時刻付きファイル名、URL欄より前の表示、保存ボタン順、代替テキスト、生成状態、狭い画面の1列化を自動テストで固定した。
- ブラウザ自動確認は、Browserスキルの信頼済みプラグインパス判定でローカル画面へ接続できず実施できなかった。21ファイル・168テスト、TypeScript型検査、静的ビルドは成功しており、実機の読取・保存・表示を公開版での利用者確認ゲートに残す。
- 利用者が公開版でQRコードのスマートフォン読取、同じ教材状態の復元、PNG保存、PC・スマートフォン表示を確認し、すべて問題なしとした。4.6を完了した。

## フェーズ5設計決定と作業単位5.1

- 利用者がROADMAP 5.0の推奨案をすべて承認し、D-037として正式決定した。右手座標系では、初期視点上のx軸を右向き、y軸を左下（手前）から右上（奥）、z軸を鉛直上向きの正方向とする。
- Lab上部へ「2D座標平面」「3D座標空間」の2択セグメント型タブを追加した。ARIA tab、左右矢印・Home・Endキー、640px以下の短縮ラベルに対応した。
- 2Dと3DのInitialState・CurrentStateを分けた。3D専用既定例は`a₁ = {}^t[2, 0, 1]`、`a₂ = {}^t[0, 2, 1]`、`a₃ = {}^t[1, 1, 2]`で、3本ともspan対象、rank 3・一次独立とした。
- 3D共有URLを未対応エラーにせず、その状態を3DのInitialStateとして3Dタブを初期選択する。2D共有URLは2Dを初期選択し、非共有側は専用既定例を使う。
- エクスポートは現在の次元だけを保存し、Resetは現在の次元だけを同次元のInitialStateへ戻す。切替時に座標を変換せず、2DのCurrentStateと未確定入力・表示範囲・詳細タブ等の一時UIを維持する。
- 5.1では3D側を説明付きプレースホルダーとし、固定3D描画とカメラは5.2へ残した。22ファイル・174テスト、TypeScript型検査、静的ビルドに成功した。利用者がデプロイ先のPC・スマートフォンで確認し、問題なしとして5.1を完了した。

## 作業単位5.2の変更

- `three` 0.185.1と`@types/three` 0.185.4を追加し、Reactから分離した`VectorSpace3D`描画ランタイムを作成した。数学座標x・y・zをThree.jsのワールド座標へそのまま割り当て、カメラupをz正方向とした。
- xy平面の適応補助格子、正方向の矢じりと負方向の破線を持つ3軸、原点、3D既定状態の固定ベクトルと矢じりを表示した。ラベルは基本文字だけボールド斜体、添え字を立体レギュラーとし、近接ラベルを左右へずらした。
- `OrthographicCamera`と`OrbitControls`を使い、マウス・タッチの回転、ズーム、パンと、正面・右・上・等角・全体表示を追加した。Resetでは3D状態とともに等角視点へ戻す。
- 描画はcontrolsのchange、プリセット、リサイズ時だけ行い、連続アニメーションループを設けない。3Dモジュールは3Dタブの初回選択まで遅延ロードし、読み込み後は非表示時もマウントしてカメラ状態を保持する。初期2D用JavaScriptは約286 kB、3Dの遅延チャンクは約553 kBとなった。
- `ResizeObserver`、devicePixelRatio最大2、PC・狭い画面のCanvas高さ、Canvas内の`touch-action: none`、WebGL初期化失敗・context lost案内、geometry・material・controls・rendererの破棄を追加した。
- ローカルEdgeのSwiftShader WebGLで幅1440pxの3D初期表示を確認し、x正方向が右、y正方向が右上（奥）、z正方向が上に見えること、固定ベクトル・ラベル・補助格子・4視点と全体表示ボタンの配置を目視確認した。ヘッドレスEdgeは狭幅で最小レイアウト幅を持つため、スマートフォン相当の操作感はデプロイ先の利用者確認へ残す。
- 24ファイル・186テスト、TypeScript型検査、静的ビルドに成功した。Three.jsの遅延チャンクが500 kBを超えるVite警告はあるが、初期2Dチャンクから分離されており、ビルド失敗ではない。

利用者がデプロイ先のPC・スマートフォンで確認し、問題なしとして5.2を完了した。

## 作業単位5.3の変更

- 3D専用の入力途中状態と詳細タブを追加し、0〜8本のx・y・z数値入力、追加・削除、自動命名、8色、span選択を既存の`addDefaultVector`、`removeVector`、`updateSpanSelection`へ接続した。未確定入力中は直前の有効値を維持し、3Dエクスポートを停止する。
- 全ベクトルと選択集合へ`analyzeVectorSet`を適用し、`rank(A)`、`dim(span(S))`、一次独立・一次従属を2Dと同じ数式規則で表示した。3Dのrank 0・1・2・3を原点、直線、原点を通る平面、3次元座標空間全体として説明する。span幾何描画は5.4へ残した。
- 共有状態をv3へ更新し、3Dカメラの単位方向、注視点、単位上方向、拡大率を厳密に検証・丸めて保存する。v1・v2はカメラ未指定のv3へ移行し、2Dではcameraを`null`とする。新規URL、QRコード、テキスト保存、InitialState、Resetを同じv3状態へ接続した。
- ベクトル編集による3Dシーン再構築でもカメラを維持し、全ベクトルの最大絶対座標と25%余白から表示範囲を再計算する。near/far、適応格子、矢じり、ラベルを範囲へ追従させた。
- Browserスキルを用いたローカル実画面確認で、a₃の第3成分を2→1としてrank 3→2、4本目の追加・削除、上視点の共有状態v3生成、別タブでのベクトル・カメラ復元、正面へ変更後のResetによる上視点復元を確認した。確認URLは548文字で、QRコード生成も成功した。390×844pxで主要操作が1列に収まることを確認した。
- 24ファイル・193テスト、TypeScript型検査、本番ビルドに成功した。3Dの遅延チャンクが約553 kBで500 kBを超えるVite警告は5.2から継続するが、初期2Dチャンクから分離されており、ビルド失敗ではない。
- 利用者が3Dの共有URLによるベクトル設定・カメラ状態の保存を確認した。rank 3の結果カードは平面状に見える格子記号から、3次元全体を認識しやすい3面構成の立方体アイコンへ変更した。5.3時点では3Dベクトル先端の直接編集を未実装とし、5.6でD-042を試作後、D-043の画面平行面操作へ改修した。

利用者が共有状態を問題なしと確認し、立方体アイコンの追加修正と5.4着手を同時に依頼した。アイコンはサイコロの目を除き、半透明の手前3面と奥側の破線を持つ立方体へ更新した。これを5.3の完了確認とする。

## 作業単位5.4の変更

- 描画から独立した`createSpaceSpanGeometry`を追加した。rank 1は非零選択ベクトルの方向と表示立方体との交点、rank 2は正規化したベクトル組のうち最大外積を持つ組から法線・正規直交基底・有限平面寸法を計算し、rank 0・3も判別共用体として返す。
- rank 0は原点の半透明球と外側ワイヤーフレーム、rank 1は円柱片による太い破線、rank 2は格子なし・両面の半透明平面、rank 3は薄い半透明グレーの立方体と実線の外枠Boxとして描いた。各形状へ「生成する空間」の日本語ラベルを付けた。通常の座標読取りに使うxy補助格子はspan形状とは独立して維持する。
- 3Dの「生成する空間」カードへ共有可能な表示オン・オフを追加した。表示中は未選択ベクトルとラベルを薄くし、選択ベクトルをspan形状より前面へ出す。表示オフでもspan選択、rank、次元、一次独立性を維持する。
- Browserスキルによる実画面確認で、既定rank 3、a₃変更によるrank 2、1本選択のrank 1、空集合のrank 0、表示オン・オフを確認した。xy平面を正面から見た真横表示でも、灰色線、座標空間内ラベル、カードが「原点を通る平面」を示し、コンソール警告・エラーはなかった。
- 立方体アイコンは目を除き、手前の上・正面・右面を半透明、奥側の稜線を薄い破線とした。
- 利用者の初回確認で、span専用格子が視認性を下げることが分かった。旧MATLAB版`vplot.m`のrank 3表示を参照し、rank 2・3のspan専用格子を削除、rank 3を半透明空間と外枠Boxへ変更した。
- Browserスキルによる調整後の実画面確認で、rank 2が独自格子のない半透明板、rank 3が薄い半透明空間と実線の外枠Boxとして表示されること、選択ベクトル・軸・ラベルが判別できること、390×844px相当でも解析内容へ到達できることを確認した。コンソール警告・エラーはなかった。
- Browserスキルによる追加確認で、選択ベクトルの座標を`1,000,000`まで拡大しても自動範囲とrank 2平面が維持されること、390×844px相当の狭幅でも3D表示・spanカード・表示切替へ到達できることを確認した。
- 24ファイル・198テスト、TypeScript型検査、本番ビルドに成功した。調整後も再実行してすべて成功した。3Dの遅延チャンクは約558 kBで500 kBを超えるVite警告が継続するが、初期2Dチャンクから分離されており、ビルド失敗ではない。デプロイ確認はコミット・プッシュ後に行う。

利用者が調整後のrank 2・3表示を確認し、問題なしとして5.4を完了した。

## 作業単位5.5の変更

- 3D描画上部へ「一次結合を調べる」を追加し、3D詳細列にも専用の「一次結合」タブを追加した。5.5時点ではターゲット`v`をx・y・zの数値入力と消去で編集し、奥行き規則が未決定だったCanvasクリック配置は見送った。未確定入力は直前の有効値を保ち、3Dエクスポートを停止する。Canvas配置は後述のD-046で追加した。
- 2Dの`analyzeLinearCombination`と結果カードを3Dへ再利用した。不能・唯一解・不定、係数例1・2、自由係数による一般解、rankを同じ教科書用語・数式規則で表示する。2D専用の一般ターゲット公式だけは3Dに表示しない。
- 描画から独立した`createSpaceCombinationGeometry`を追加した。2項は原点基準の平行四辺形、3項は第1係数例の`c₁a₁`、`c₂a₂`、`c₃a₃`を3辺とする半透明の平行六面体を構成し、各寄与ベクトルとターゲット`v`を表示する。係数を乗じた辺のrankが項数未満なら退化形として説明する。
- 3本の不定における第2例と一般解は数式カードだけに示す。4本以上は単一の幾何形状へ対応させず、係数・一般解と「数式で確認」の案内だけを表示する。ターゲットと全幾何頂点を3D自動表示範囲へ含めた。
- 共有スキーマはv3のまま変更せず、既存の3Dターゲット・表示フラグ・カメラを共有URL、QRコード、InitialState、Resetへ接続した。共有状態で一次結合タブを初期選択する。
- Browserスキルによるローカル実画面確認で、3本の唯一解と非退化平行六面体、係数0を含む退化形、2本の平行四辺形、一次従属集合の不定・不能、4本の数式限定表示を確認した。ターゲット付き共有URLを別タブで復元し、編集後のResetが共有時のターゲットと一次結合タブへ戻ることも確認した。確認URLは648文字で、390×844pxでも入力・結果・4本以上の案内へ到達でき、コンソール警告・エラーはなかった。
- 利用者確認で通常ベクトル名と係数付き寄与ベクトル名が矢じりへ重なる問題が見つかった。ラベルの世界座標方向オフセットを廃止して基準点を矢先へ固定し、ラベル矩形が基準点を含まない4方向の画面座標配置へ変更した。同じ終点の`a_i`と`c_i a_i`は反対側へ置き、背景を62〜66%の白系半透明色へ薄くして縁取り・文字影を残した。等角・上視点と390px幅で、矢じりがラベルに覆われず、平行六面体と背後の線が透けて見えることを確認した。
- ラベル調整後を含む25ファイル・204テスト、TypeScript型検査、本番ビルドに成功した。3Dの遅延チャンクが500 kBを超えるVite警告は継続するが、初期2Dチャンクから分離されており、ビルド失敗ではない。

利用者がラベル調整後を含む5.5の表示・操作を確認し、問題なしとして5.5を完了した。平行六面体は現行の試行表示を維持する。

## 作業単位5.6の変更

- 初回試作では「視点を操作」「ベクトルを操作」のモード、選択ベクトル、x・y・z軸拘束ハンドル、±0.1パネルを実装した。利用者確認でモード切替が操作の流れを妨げると分かり、このUIをD-043で削除した。
- 改修後は通常ベクトルの矢先から始めたドラッグだけをベクトル移動、矢先以外から始めたドラッグを従来の視点回転として自動分離する。OrbitControlsのズーム・パンと視点プリセットは維持する。
- ドラッグ開始時の矢先を通り、カメラ視線へ垂直な画面平行面を固定した。正投影rayと操作面の交点差を初期座標へ加え、斜め視点では3成分を同時に、上・正面・右視点では対応する座標平面方向を変更できる。
- 各矢先へ色付きの小球とワイヤーフレーム球を表示した。ヒット半径はマウス15px、ペン19px、タッチ24pxの画面距離とし、重なる候補は画面距離とカメラ距離で選ぶ。pointerdown時に操作所有者を固定し、ベクトル移動中はカメラと表示範囲を固定する。
- ドラッグ中は元の矢印、画面の上下・左右に対応する破線ガイド、元位置からの移動線、新しい矢印、現在の3成分をプレビューする。自由移動座標は小数6桁・絶対値`1,000,000`以内とし、D-044で吸着した場合は従属関係を保つ直交射影値へ置き換えてから既存状態へ反映し、数値入力、span、rank、一次結合、自動範囲を再計算する。
- 専用選択状態や操作面は共有せず、共有状態v3を変更していない。厳密な座標とキーボード代替は既存の数値入力で行う。Resetは従来どおりInitialStateの確定座標とカメラを復元する。
- BrowserスキルでモードUIがないこと、1行の矢先／背景案内、二重矢先マーカー、デスクトップと390×844px相当の表示を確認した。自動テストでは画面平行面上の変位、3成分同時変化、座標上限、Pointer Eventsの所有分離、数値状態接続を確認した。
- 26ファイル・210テスト、TypeScript型検査、本番ビルドに成功した。3D遅延チャンクは約570 kBで500 kBを超える既知のVite警告が継続するが、初期2Dチャンクから分離され、ビルド失敗ではない。
- 利用者がD-043の統一操作を確認し、「非常に操作しやすい」と評価したため本採用とした。そのうえで、3本を厳密に同一平面上へ置く直接操作には吸着が必要と判明した。
- D-044として描画非依存の3D吸着計算を追加した。ドラッグ中の座標から他の各非零ベクトルが張る直線までを比較し、表示幅相対の距離内なら最寄り直線へ射影する。平行候補がない場合だけ、D-009で独立な他の各2本が張る最寄り平面へ射影する。平行を優先し、零・従属ペアを除外する。当初の1%はD-060で3%へ改定した。
- 吸着中はガイドと移動線を緑系に変え、矢先へ画面幅相対のリングを表示し、状態表示へ対象ベクトル名と「平行」または「同一平面上」を出す。数値入力、ターゲット、rank許容誤差、共有状態v3には適用しない。
- ローカルブラウザで3D案内、座標入力、正面視点、rank 3の更新を確認した。自動テストでは直線・平面への直交射影、平行優先、閾値外、零・退化候補、安全上限後のrank 2、表示幅相対の接続を確認した。
- 利用者がD-044のスナップを確認し、良好な操作感として採用した。一方、吸着した瞬間にはまだ確定前のため灰色のspanが立体のままであり、従属関係の成立と幾何表示に時間差があることが判明した。
- D-045として、確定spanとドラッグ中spanを別のThree.jsグループへ分離した。span対象をドラッグすると確定形状を隠し、吸着適用後の座標とD-009からrank 0〜3のプレビュー形状を生成する。キャンセル時は元の形状を復元する。
- rank 0・3は方向に依存しないため同じrankが続く間は再生成せず、rank 1・2だけ向きの変化へ追従して更新する。グラフ内ラベルと操作中の状態表示も同期し、右側カード・数値入力・共有状態は終了時確定を維持する。
- 自動テストでは、3本目が独立な2本の平面へ入ったときのプレビューrank 3→2、span非選択ベクトルでは更新しないこと、確定・プレビューグループの切替接続を確認した。
- D-046として、一次結合モード中の3D背景を短くクリック・タップすると、ポインター位置の正投影rayと原点を通る現在の画面平行面との交点へターゲット`v`を配置・再配置できるようにした。正面・右・上の視点では対応する座標平面上へ配置される。
- 背景移動が8pxを超えた場合は配置候補を破棄して従来の視点回転を続け、2点操作はズーム・パン、通常ベクトルの矢先から始めた操作はベクトル編集を優先する。配置座標は小数6桁・絶対値`1,000,000`以内とし、既存の数値入力、解析、共有URL、QRコード、Resetへ同じターゲット状態として接続した。
- 自動テストでは、3D交点座標の丸め・上限と不正値、8px判定、原点通過面、操作案内、状態接続を確認した。
- 利用者がD-046のワンタップ配置を確認し、問題なしとして採用した。続いてD-047として、ターゲットの青い矢先へ通常ベクトルと同じ画面距離ヒット領域を追加し、開始時の矢先を通る画面平行面内で直接移動できるようにした。
- 確定ターゲット・係数幾何とドラッグ用プレビューを別グループへ分離した。ドラッグ中の3成分から一次結合を再計算し、表現可能な2本・3本では平行四辺形・平行六面体、係数倍矢印・ラベル・ターゲットを毎フレーム更新する。表現不能ではターゲットだけを追従させる。
- 3成分入力、解析カード、共有状態v3は終了時に一度だけ確定し、キャンセル時は元の表示へ戻す。3D専用解析モジュールを遅延チャンク側に置き、2D初期バンドルへの増加を回避した。
- 利用者が公開版のPCとスマートフォンでD-045のspanリアルタイム切替、D-047のターゲット矢先ドラッグ、係数幾何の追従、背景・通常ベクトル操作との分離を確認し、問題なしとして5.6を完了した。

## 作業単位5.7の変更

- `src/teaching/threeDimensionalScenarios.ts`へ、空集合、零ベクトル、1本が生成する直線、同一平面上の3本、一次独立な3本で唯一解、平行な2本で不能、同一平面上の3本で不定となる7例を追加した。既存の教材シナリオ型は次元共通ファイルへ分離した。
- 7例でrank 0〜3、一次独立・一次従属、一次結合の不能・唯一解・不定を網羅する。正面・右・上・等角のカメラを割り当て、全状態を共有状態v3の厳密な検証へ通す。
- `docs/TEACHING_SCENARIOS.md`を2D・3D共通の授業ガイドへ拡張し、各例の期待結果、本番共有URL、視点変更、直接編集、吸着、span変化、ターゲット配置・ドラッグ、係数幾何、URL・QR配布、Resetの観察手順を記録した。
- 3D表示へCanvasの代替説明を関連付けた。WebGL初期化失敗または描画コンテキスト喪失時も、数値入力、解析カード、共有URL、Resetを利用できることをエラー表示に明記した。
- 既存のレスポンシブ2列／1列、Canvasの`touch-action: none`、視点プリセット、フォーカス可能なCanvas、画面距離ヒット領域、線種・ラベル・数式カードによる色以外の識別を再点検し、端末回転と主要ブラウザの実機確認手順を授業ガイドへまとめた。
- 利用者が公開版のPC・スマートフォンで代表例10〜16、端末回転、Canvas外スクロール、URL・QR・Reset復元、代表例14の平行六面体とターゲット追従を確認し、すべて問題なしとして5.7を完了した。

## 作業単位5.8・フェーズ5終了時の統合棚卸し（2026-08-24）

- 2D/3D切替と次元別InitialState・CurrentState、数値入力、集合編集、カメラ、rank 0〜3のspan、一次結合、通常ベクトルとターゲットの直接操作、共有URL、QR、Resetを、過去の利用者確認、全自動テスト、公開版のデスクトップ・タブレット・スマートフォン相当表示で再照合した。
- 公開版で2Dの`a₁`第1成分を7、3Dを9へ変更して切り替え、両状態が独立して残ることを確認した。2DのReset後は2Dだけ2へ戻り3Dは9を維持し、3DのResetで3Dも2へ戻った。3D共有ダイアログはカメラ付きv3 URLとQRを生成した。
- CSS幅820×1024と390×844では3D表示とインスペクターが同幅の1列となり、文書幅は各viewport以下だった。2D SVG・3D Canvasはいずれも`touch-action: none`、3D Canvasはフォーカス可能で、ブラウザの警告・エラーログはなかった。
- 2DのSVG、ズーム・パン・ピンチ、平行スナップ、ターゲット配置・吸着、平行四辺形、9件の代表例、共有URLに回帰がないことを確認した。3D追加コードは初回3D選択まで遅延ロードされ、2D初期バンドルと状態を分離している。
- フェーズ5完了時点では、3Dの責務を、次元共通数学の`src/domain`、共有状態v3・URL・QRの`src/sharing`、次元別状態と吸着の`src/state`、Three.js描画・操作の`src/visualization`、UI接続の`src/app`、7件の代表例の`src/teaching`へ分離していることを確認した。D-058以後、Lab固有UI接続は`src/labs/vector-space`、共通シェルは`src/app`が担当する。数学状態は描画オブジェクトから逆算しない。
- D-042の操作モード・軸拘束はD-043へ、D-041のターゲット数値入力限定はD-046へ置換済みで、旧UI・旧状態は実装に残っていない。span専用格子と4本以上の単一係数幾何は教育的な過密・一意対応不能を理由に採用していない。
- `SPEC.md` 0.52、`ROADMAP.md`、`README.md`、`docs/DECISIONS.md`、本書、`docs/TEACHING_SCENARIOS.md`、`math-writing-rules.txt`、`syllabus.txt`、実装、テスト、`package.json`、`pnpm-lock.yaml`、GitHub Actionsを横断した。進行中・確認待ち・3Dジェスチャー未決定・先端ドラッグ未実装・5.7の未来形という古い記述を現状へ修正した。
- 実装依存はReact、Three.js、QRコード生成だけで、型・ビルド・テスト用依存とlockfileが一致している。Three.jsは遅延チャンク約577kBとして500kB警告が続くが、初期2Dから分離され、現行端末の操作確認では問題がないため既知の再評価条件として維持する。
- D-009の数学許容誤差、操作専用スナップ比率、共有URL実用長、学内端末・ブラウザ、WebGL/GPU、nginx、数式描画、極端な座標は、現仕様を変更せず実運用で問題が見つかった場合の再評価事項とした。
- 以上により、3D代表例を操作・共有・復元できるという終了条件を満たし、フェーズ5を完了した。

## フェーズ3終了時の統合棚卸し（2026-08-19）

- 利用者がD-027の入力案内、アクセシビリティ、5つの授業シナリオと、D-029のrank 1・2における灰色系span表示を確認し、いずれも問題なしとした。D-028のスマートフォン実機確認と合わせ、3.2・3.3の確認ゲートをすべて満たした。
- 実装、15ファイル・117テスト、`SPEC.md` 0.28、`ROADMAP.md`、`docs/DECISIONS.md`、本書、`README.md`、`docs/GITHUB_PAGES.md`、`docs/TEACHING_SCENARIOS.md`、`math-writing-rules.txt`、`syllabus.txt` を横断して照合した。
- 0〜8本の集合編集、未確定入力、共有URL、Reset、モバイルジェスチャー、span幾何表示、5つの代表例について、承認済み仕様と実装・テストの不整合は見つからなかった。共有状態v1と依存関係は変更していない。
- 学生向けの列ベクトル、ベクトル基本文字だけのボールド斜体、添え字数字の立体レギュラー、`rank(A)`、`dim(span(S))`、一次独立・一次従属、集合と列順の区別は `math-writing-rules.txt` と一致している。
- `syllabus.txt` の第1〜7週に対応する2D教材の骨組みは、授業で共有・操作・Resetできる品質に達した。第8週の中間試験と第9週以降の拡張項目は現在の初版範囲と矛盾しない。
- 2Dと3Dで再利用する状態、rank解析、共有、集合編集、教材シナリオの境界は明文化済みである。フェーズ4で一次結合も共通責務として確立し、D-037で3D描画、カメラと直接編集、直線・平面・全空間、カメラ共有の基本方針を決定した。
- 古くなっていたD-027・D-029の確認待ち、フェーズ3の進行中表示、旧Git担当分担を解消した。日常更新はD-030のcommit・push・デプロイ確認運用へ移行した。
- 以上によりフェーズ3を完了した。

## 検証結果

- 自動テスト: 27 ファイル、242 テスト成功
- 数学テスト: rank、span の次元、一次独立・一次従属の代表例と境界例を確認
- 一次結合テスト: 2D/3Dの解なし・一意・無数、特解、異なる2例、自由係数、零空間基底、空集合、零ベクトル、D-009許容誤差、入力検証を確認
- 共有テスト: v1・v2からv3への移行、2D/3Dターゲット、3Dカメラ、未配置、エクスプローラ表示、日本語表示名、決定的エンコード、往復、厳密なv1・v2・v3スキーマ、安全上限、公開サブパス、`state`の一意性、不要クエリー・ハッシュ除去、テキスト内容・QRコードPNG・各ファイル名を確認
- 初期化テスト: URLなし、有効v1・v2・v3の2D、有効v2・v3の3D、不正状態について次元別InitialState、初期選択次元、ターゲット・カメラ保持、フォールバックを確認
- 2D 状態テスト: 固定例が共有状態スキーマを満たし、rank 2・一次独立となることを確認
- 3D表示テスト: 基準半径と余白付き範囲、縦横アスペクト比、等角・正面・右・上のカメラ姿勢、正投影、3軸、数式ラベル、タッチ操作、オンデマンド描画、リサイズ、高DPI、WebGL喪失、破棄、遅延ロード、Reset接続を確認
- 3D直接編集テスト: 画面平行面上の3次元変位、斜め視点の3成分同時変化、座標上限、矢先／背景の自動分離、固定操作面、Pointer Events、ドラッグプレビュー、全3成分の状態接続を確認
- 3Dターゲット配置テスト: 短い背景クリック・タップ、原点通過の画面平行面との交点、8px超の視点ドラッグとの分離、座標の丸め・安全上限、通常ベクトル編集の優先、一次結合状態への接続を確認
- 3Dターゲット直接編集テスト: 画面距離ヒット領域、画面平行面ドラッグ、平行六面体の再計算、span外での表現不能、専用プレビューグループ、終了時確定、キャンセル復元、背景・通常ベクトルとの操作分離を確認
- 入力テスト: 小数・指数表記、空欄、入力途中、16進表記、非有限値、`-0`、座標上限を確認
- 描画座標テスト: 原点、y 軸反転、自動範囲、15%余白、適応目盛、目盛表記、ズーム中心、画面中央ズーム、パン、ドラッグ座標変換と丸め、矢じりを確認
- 平行スナップテスト: D-009 との閾値分離、画面距離の内外、直交射影、反対方向、零ベクトル、座標安全上限、表示幅10・100・4での比例変化を確認
- ターゲット吸着テスト: rank 0の原点、rank 1の直交射影、rank 2、距離内外、零方向、極小の非零方向、表示幅に応じた距離、入力検証を確認
- span選択テスト: 表示順の維持、選択の追加・解除、空集合・1本・2本でrank 0・1・2になることを確認
- ベクトル集合編集テスト: 既定値、spanへの追加、ID・表示名の衝突回避、3D用既定値、8本上限、削除と空集合、一次結合・カメラ状態の保持、共有状態v3の往復を確認
- span幾何テスト: 原点を通る直線の表示矩形への切り出しと、パンにより交差しない場合を確認
- 数式表記テスト: Unicode 添え字、TeX 風のアンダースコア添え字、添え字なしのベクトル名を基本文字と添え字へ分解できることを確認
- 一次結合表示テスト: 教科書用語の不能・唯一解・不定、有効数字6桁、負のゼロ、丸めと近似判定、科学表記、入力検証を確認
- 教材シナリオテスト: 2Dのspan 5例・一次結合4例と、3Dのspan 4例・一次結合3例について、rank・一次独立性、一次結合の2つのrank・解分類、3Dカメラ、本番サブパスの共有URL往復、文書リンクと状態定義の一致を確認
- モバイルジェスチャーポリシーテスト: SVG座標面全体とターゲット先端の `touch-action: none`、短いpointerupだけのターゲット配置、単一点パン・2点ピンチとの分離、グラフ外のページズームを禁止しないviewport設定を確認
- span配色テスト: rank 0・1・2の座標面内幾何表示が中立色パレットを使い、赤橙色のUIアクセントへ再依存していないことを確認
- TypeScript の型検査: 成功
- 静的ビルド: 成功
- ローカル静的配信確認: GitHub Pages相当の `/linear-algebra-visual-lab/` で入口、JavaScript、CSSがHTTP 200

## 2.6着手前の棚卸し（2026-08-19）

- 実装、テスト、`SPEC.md`、`ROADMAP.md`、`docs/DECISIONS.md`、本書、`README.md`、`math-writing-rules.txt`、`syllabus.txt` を横断して照合した。
- 共有状態v1のフィールドと安全上限、既定例、D-009の許容誤差、D-017の表示範囲、D-019の画面幅1%スナップ、D-020のspan選択、D-021のレスポンシブ境界値は実装と文書で一致している。
- コードとテストに、現時点の承認済み仕様からの逸脱は見つからなかった。この棚卸しでソースコードの修正は行っていない。
- 文書側では、READMEとロードマップの古い現在地、D-021の確認待ち、D-009・D-012〜D-014・D-018の古い再評価記述、重複した仕様書の節番号、未決定のまま残っていた状態モデル・ディレクトリ・受入条件、Git操作状況の誤記を修正した。
- `math-writing-rules.txt` と現行画面を再照合し、列ベクトル、ベクトル基本文字だけのボールド斜体、添え字数字の立体レギュラー、`rank(A)`、`dim(span(S))`、一次独立・一次従属、集合と列順の区別に不整合がないことを確認した。
- `syllabus.txt` の第1〜7週のベクトル空間・一次独立・rank・基底、第8週の中間試験、第9〜15週の拡張候補は、初版と将来拡張のスコープ記述に矛盾しない。
- 再検証として9ファイル・84テスト、TypeScript型検査、本番ビルド、ローカルHTTP 200を確認した。

## 3.3利用者確認前の統合棚卸し（2026-08-19）

- 3.1のデプロイ先確認をD-026、ロードマップ、README、本書へ反映し、古い「3.1確認待ち」を解消した。
- 実装、`SPEC.md` 0.25、D-027、ロードマップ、教材シナリオ文書、数学表記規則を照合し、未確定入力、読み上げ名、代表例の期待結果に不整合がないことを確認した。
- 5つの文書内本番URLを状態定義から生成したURLと自動照合し、共有状態v1を変更していないことを確認した。追加依存関係もない。
- 2Dと3Dで再利用する責務と2D固有の責務を明文化し、当時の後続3Dフェーズ（現在のフェーズ5）で判断するカメラ・直接編集・平面表示・共有対象を分離した。
- 13ファイル・113テスト、TypeScript型検査、GitHub Pages用base pathでの本番ビルド、入口・JavaScript・CSSのHTTP 200を確認した。

## フェーズ8開始後の統合棚卸し（2026-08-25）

- 8.1、8.1a、8.2、8.2a、8.2bとD-056〜D-060を、実装、テスト、`SPEC.md`、`ROADMAP.md`、`README.md`、`docs/DECISIONS.md`、本書、`docs/TEACHING_SCENARIOS.md`に対して横断照合した。
- D-059・D-060が利用者確認後も確認待ちだった記述、変更履歴0.65に対して0.62だった仕様書の文書バージョン、補修作業8.1a・8.2a・8.2bが抜けていたフェーズ8実施順、D-058以前の旧UI責務表現を修正した。
- 3Dの通常ベクトルとターゲットは`spaceSnapDistanceForViewWidth`の表示幅3%を共有し、2Dは従来の表示幅1%、数学判定D-009は`1e-10`を維持していることを確認した。
- D-058の共通アプリシェルと`VectorSpaceLab`の責務分離、現行`vector-space` v3の維持、`basis-dimension` v1を8.3以降に追加する境界に不整合はなかった。
- 棚卸し結果を固定する文書整合テストを追加し、37テストファイル・283テスト、型検査、Pages用ビルド、4アセットの静的検査に成功した。約578kBの3D遅延チャンクに対する既知の500kB警告は、初期2Dから分離された再評価条件として維持する。
- D-061・作業単位8.2cとして本棚卸しを記録した。フェーズ8は進行中であり、次の作業単位は8.3「基底・次元の数学ロジック」である。

## 次に行うこと

フェーズ7「初版評価と棚卸し」は、授業実施が先であるため完了扱いにせず延期した。正式リリース、`1.0.0`、Git tag、GitHub Release、v3共有URLの正式な互換保証開始も延期する。追加Lab開発中は正式リリース前の候補版という位置付けと現行v3 fixtureの回帰確認を維持し、現時点の版番号は`1.0.0-rc.1`とする。次のRC番号は追加実装の節目で別途決める。

フェーズ8「複数Lab基盤と基底・次元Lab」の8.2をD-058として実装し、公開版を利用者が確認して完了した。共通`App`はヘッダー、Labメニュー、利用案内、フッターを担当し、現行の2D/3D状態、初期化、共有v3、Reset、解析、描画は`VectorSpaceLab`が所有する。共有とResetは共通`LabActionControls`へ現在Labのハンドラを接続する。まだ画面を持たない基底・次元Labは準備中の非操作項目とし、空の状態やスキーマを先行実装していない。

8.2後の追加補修D-059では、D-057の3Dターゲット平面吸着をrank 0の原点とrank 1の生成直線へ拡張した。原点との距離、直線・平面への直交距離を同じ表示幅相対距離と比較し、吸着後の座標から一次結合の幾何と解析をリアルタイム更新する。rank 3、距離外、数値入力、不整合rankは対象外とする。

D-060では、3Dの画面平行面内操作ではrank 1の生成直線へ1%以内まで近づけることが難しいという利用者評価を受け、3D専用の共通吸着距離を正投影表示幅の3%へ拡大した。通常ベクトルの平行・同一平面吸着と、ターゲットの原点・直線・平面吸着の2経路が同じ`spaceSnapDistanceForViewWidth`を使う。2Dは従来の1%を維持する。36テストファイル・280テスト、型検査、Pages用ビルド、静的アセット検査に成功し、公開版の操作感に問題がないことを利用者が確認した。

8.2cの棚卸しまで完了したため、次は8.3「基底・次元の数学ロジック」へ進む。基底候補の一次独立性と生成条件、基底でない理由、次元と一次独立な最大個数、基底例の抽出を描画非依存で実装する。その前に、基底記号、順序付きの組、座標ベクトルの学生向け表記を`math-writing-rules.txt`と教科書へ合わせて確定する。8.4の画面、`basis-dimension` v1共有状態、Labメニューの有効化は8.3へ混在させない。

8.2前の追加補修D-057では、3Dターゲットドラッグ中に`v`ラベルが移動経路へ蓄積する問題を修正し、rank 2の平面spanへ表示幅相対距離内で吸着するようにした。当初の1%はD-060で3%へ改定した。公開版でラベル蓄積の解消と平面吸着の操作感に問題がないことを利用者が確認済みである。

6.7の最終自動検証は34テストファイル・264テスト、TypeScript型検査、GitHub Pages用ビルド、4アセットのbase path検証が成功した。ルート`/`、任意サブパス`/class/tools/vector/`、Pages`/linear-algebra-visual-lab/`の実ビルドも成功した。公開版ではv3代表例16件のrank・分類、8本・3D境界URL1309文字、QR、共有状態からのReset、`1.0.0-rc.1`と利用案内を確認した。

6.2の実測では、当時の文書に固定した旧版を含む代表例16件は268〜612文字、現行v3の新規生成は286〜612文字だった。6.7で配布文書の全16件をv3へ更新した。画面から到達可能な8本・3D・最大座標・カメラ・ターゲットの境界状態は1309文字で、誤り訂正MのQRコードPNGへ変換できた。完全URL2048文字とデコード防御上限8192文字を分離し、圧縮は導入しない。

6.3でD-051の互換契約と固定v3 fixtureを整備したが、D-032どおり正式リリース前URLの保証開始はまだ行わない。D-056によりフェーズ7自体を授業実施まで延期したため、正式リリースと互換保証の開始もそれまで行わない。学内nginxの実配置、Git tag、GitHub Releaseも明示的な判断なしには実施しない。

## 再開時の最短コンテキスト

### 文書の優先順位

1. 決定済み要件と受入条件は `SPEC.md` を正本とする。
2. 決定の理由、試行値、再評価条件は `docs/DECISIONS.md` を参照する。
3. 作業順と確認ゲートは `ROADMAP.md`、直近の現在地と検証結果は本書を参照する。
4. 学生向けの用語・記号は `math-writing-rules.txt` に合わせる。

文書と実装が食い違う場合は、どちらかを暗黙に正とせず差分を記録して確認する。

### 現在の状態構成

- `src/state/defaultState.ts` の `DEFAULT_2D_SHARE_STATE` が2D既定例で、`a₁ = (2, 1)`、`a₂ = (-3, 2)`、両方をspan対象、幾何表示オン、一次結合エクスプローラ非表示・ターゲット未配置である。`DEFAULT_3D_SHARE_STATE`は3本の一次独立な3Dベクトルを持つ3D専用既定例である。
- `src/sharing/shareState.ts` の厳密な `ShareStateV3` が、次元、ベクトル、span選択、`showSpan`、一次結合エクスプローラ、3Dカメラを保持する。旧v1・v2を検証してv3へ移行し、`src/sharing/shareUrl.ts` が現行v3のURL構築と各バージョンの読込、テキスト内容・ファイル名を担当する。
- `src/sharing/shareQrCode.ts` が外部通信なしのQRコードPNG生成と時刻付きPNGファイル名を担当する。QR画像自体は共有状態へ保存しない。
- `src/labs/vector-space/VectorSpaceLab.tsx` は2Dと3Dの共有可能状態・入力途中状態・詳細タブを別々に保持し、現在次元だけをエクスポート・Reset対象にする。両次元のrank・span・一次結合解析を派生値として計算し、3D描画モジュールは3Dタブ初回選択時に遅延ロードする。ターゲット・ベクトルの入力途中文字列、2D手動表示範囲、ドラッグ中表示、吸着表示、詳細タブ、解の詳細と公式の折り畳み状態は共有しない。
- `src/state/appInitialization.ts` がURLなし・有効な2D/3D URL・不正URLを次元別InitialStateと初期選択次元へ変換する。Reset UIは現在次元のInitialStateだけを使い、他方のCurrentStateを変更しない。
- `src/state/vectorCollection.ts` がD-026の追加・削除、既定座標、自動命名、span選択の同期を担当する。
- `src/teaching/twoDimensionalScenarios.ts` が5つのspan代表例と4つの一次結合代表例、期待する数学結果を保持する。
- D-023により2D手動表示範囲は共有対象外とする。D-039により現行スキーマはv3となり、3Dカメラを共有する。D-032により正式リリース前の互換性は保証しないが、現行v1・v2読込はURL長へ影響しないため残している。
- D-056により複数Labの共有状態は`lab`と`v`の組で判別する。現行`vector-space` v3を維持し、後続の`basis-dimension`は独立したv1として追加する。D-058で共通シェルと現行Labを分離済みであり、基底・次元の状態と画面は8.3以降で追加する。

### 主な実装責務

- `src/domain/vectorSet.ts`: D-009のrank・span次元・一次独立判定。既定相対許容誤差は `1e-10`。
- `src/domain/linearCombination.ts`: D-031・D-034の `Ac = v`、拡大行列rank、特解、異なる係数例、零空間基底による一般解。
- `src/sharing/shareState.ts`: D-010の安全上限と決定的Base64URL変換、D-031の一次結合状態、D-039の共有状態v3と3Dカメラ、厳密なv1・v2読込と移行。
- `src/state/`: 既定状態、数値入力、D-019の2D平行スナップ、D-031のターゲット吸着、D-044の3D平行・同一平面スナップ、span選択、D-026のベクトル集合更新。
- `src/state/spaceVectorSnapping.ts`: D-044・D-060の3D通常ベクトル吸着と、3D共通吸着距離。現在の正投影表示幅3%を通常ベクトルとターゲットの両方へ渡す。
- `src/state/spaceTargetSnapping.ts`: D-057・D-059の3Dターゲットspan吸着。rank 0は原点、rank 1は生成直線、rank 2は生成平面へ直交射影し、D-060の表示幅3%以内だけ吸着する。
- `src/teaching/`: 授業用の共有可能な状態、学習目的、期待結果。
- `src/visualization/`: 2D座標変換、自動範囲、ズーム・パン・ピンチ、通常ベクトルとターゲットのドラッグ、ターゲット配置、span幾何表示、2Dの2項平行四辺形。D-038〜D-047・D-057の`VectorSpace3D`、3D表示範囲、共有カメラ変換、3D span・2項平行四辺形・3項平行六面体、画面平行面ドラッグ、3D吸着、spanリアルタイムプレビュー、3Dターゲット配置・矢先ドラッグ・係数幾何プレビュー、CSS2Dプレビューラベル破棄、Three.jsリソース管理。
- `src/app/App.tsx`、`LabMenu.tsx`、`LabActionControls.tsx`、`App.css`: D-058の共通ヘッダー、現在Labと準備中Labを示すレスポンシブなLabメニュー、共有・Reset接続点、利用案内、フッターと共通スタイル。
- `src/labs/vector-space/VectorSpaceLab.tsx`: D-021の要約・タブ、D-037の次元切替、2D/3DのCurrentState・InitialState・一時UI、共有v3、Reset、D-038以降の3D編集・解析・描画接続を所有する現行Lab画面。

### フェーズ2完了時点で確定した事項

- 手動表示範囲を共有せず、共有状態v1を維持する。
- クエリーパラメーター名は `state` とし、公開サブパスを維持する。エクスポートURLには他のクエリーとハッシュを持ち込まない。
- URLなし、有効2D、不正・未対応状態についてInitialStateと利用者向け案内をD-024のとおり扱う。
- Resetは共有可能状態、入力欄、自動表示、編集タブ、一時操作をInitialStateへ同期する。

ベクトル追加・削除、QR生成、3D、公開作業はフェーズ2の範囲に含めなかった。ベクトル追加・削除はフェーズ3の3.1、初回公開は6.1、QR生成は追加作業4.6として実装・利用者確認済みである。3Dはフェーズ5の5.1〜5.8として実装・利用者確認・統合棚卸しを完了した。

## 数学用語・数式表記の確認

- `math-writing-rules.txt` を全文確認した。教科書と授業で用いる語・記号を、今後の学生向け画面の基準とする。
- `(2, 1)` のような横並び座標を列ベクトル表示へ変更した。
- 引数のない `rank` を、列ベクトルを並べた行列 `A` の `rank(A)` へ変更した。
- `span の次元` を、対象集合を明示した「生成する空間の次元」と `dim(span({...}))` の対応へ変更した。
- 「一次独立」「一次従属」は既に教科書の用語と一致している。
- 内部コードの `rank`、`spanDimension`、`spanSelection` 等は学生向け表記ではないため、意味が一致する限り変更を必須としない。

## 既知のリスクと後続確認

- D-014 の手動ズーム後の維持、「全体を表示」による自動追従への復帰、ドラッグ終了後の再調整は利用者確認済みである。D-023により手動表示は共有しない。
- 現段階の数式は依存追加なしで組版した。複雑な数式が必要になる段階で MathML または数式描画ライブラリを比較する。
- D-016 の15%余白、目盛密度、入力途中の扱い、数式フォントの見た目は、利用者のブラウザ操作で問題なしと確認された。
- D-017 のホイール・ピンチ感度、倍率上下限、パン、原点が表示外にある場合の見え方は、利用者の操作確認で問題なしとされた。異なる端末で問題が出た場合は再評価する。
- D-018 の先端操作領域、ドラッグ座標の丸め、自動再フィット、手動表示維持は問題なしと確認された。
- D-019 の画面幅 `1%` の平行スナップ距離、ヒステリシスなしの操作感は利用者確認で問題なしとされた。
- D-020 のチェック配置、既定で幾何表示をオンにする方針、rank別の表現、選択集合と全ベクトルのカード分離は利用者確認で問題なしとされた。長い座標面内ラベルの枠幅は可変幅へ修正した。
- D-021 の常時要約、3タブ、画面高さ `820px` 以上でのsticky、幅 `980px`・`640px` での配置切替は利用者確認済みである。異なる実端末で問題が出た場合に再評価する。
- D-022〜D-024のクリップボード、ファイルダウンロード、フォーカス、別タブ復元、Resetは利用者確認済みである。公開先固有の制約は配布前に再確認する。
- D-025のGitHub Actionsと初回公開は利用者確認済みである。リポジトリ名またはカスタムドメインを変更する場合はbase pathを再設定する。
- D-026はデプロイ先で利用者確認済みである。
- D-027は入力案内、キーボード操作、狭い画面、5つの代表例を含めて利用者確認済みである。
- D-028はスマートフォン実端末で利用者確認済みである。
- D-029はデプロイ先のrank 1・2の視認性を含めて利用者確認済みである。
- D-009 の相対許容誤差 `1e-10` は維持する。直接操作で平行に合わせにくい問題は D-019 の操作専用スナップで解決し、数学的判定と分離する。
- D-010の8192文字は受信状態の防御上限、D-050の2048文字はアプリが生成する完全URLの授業用上限である。LMS等の外部サービスへ貼り付ける場合は6.5で実環境を確認する。
- GitHub Pages のプロジェクトサイトでは、ビルド時にリポジトリ名のサブパスを `APP_BASE_PATH` へ指定する必要がある。
- nginxを採用する場合は、D-050の2048文字の完全URLを許容するリクエスト行設定を6.4の手順で確認する。
- HTTP配信ではClipboard APIが利用できない場合があるため、HTTPSを推奨し、手動コピーとテキスト保存を代替手段として維持する。
- D-039により3Dカメラ状態を共有状態v3へ実装済み。D-050により完全URLの授業用上限は2048文字に決定した。
- D-053により対応環境は固定ブラウザ版ではなく必要能力、確認済み範囲、代替導線で示す。学内端末、教室ネットワーク、投影距離は各授業前のチェック対象とする。
- D-054により初版候補は`1.0.0-rc.1`、正式承認後は`1.0.0`とする。本体はMIT License、Copyright © 2026 Daichi Kitamuraとし、アプリ内概要・詳細利用案内・第三者通知を公開する。
- D-055によりフェーズ6の技術確認と棚卸しを完了し、全16件の教材文書URLを現行v3へ統一した。正式リリース関連の操作はフェーズ7まで行わない。
- D-056としてフェーズ7を授業実施まで延期し、フェーズ8を「複数Lab基盤と基底・次元Lab」とした。共通Labメニュー、Lab別の状態所有、現在Labだけの共有・Reset、`(lab, v)`による共有スキーマ判別、現行`vector-space` v3と新規`basis-dimension` v1の分離を決定した。
- D-057として、3Dターゲットプレビューの入れ子CSS2DラベルDOMを明示的に破棄し、`v`の移動経路への蓄積を修正した。D-059でターゲット吸着をrank 2の生成平面からrank 0の原点・rank 1の生成直線へ拡張し、D-060で3D通常ベクトルとターゲットに共通する吸着距離を表示幅3%へ変更した。
- D-058として、共通アプリシェルと現行ベクトル空間Labを実ファイルで分離した。Labメニューは現在Labを視覚・支援技術の双方で示し、基底・次元Labは画面実装まで準備中とする。共有・Resetは現在Labが状態を所有したまま共通操作部品へ接続する。
