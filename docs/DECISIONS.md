# 意思決定記録

## 1. 運用方法

この文書は、仕様を実装可能な形へ具体化するときの判断を記録する。各項目は次の状態を取る。

- **提案中**: Codex の推奨案。利用者の承認前であり、仕様として確定していない。
- **試行採用**: 小さな実装とテストで評価する初期値。利用者の確認や授業上の評価で変更できる。
- **運用候補**: 利用者が想定している運用方式。環境情報を確認して最終決定する。
- **決定**: 利用者が承認し、`SPEC.md` と実装へ反映する。
- **置換**: 後の判断で変更された。新しい決定への参照と変更理由を残す。
- **却下**: 採用しない。再検討を避けるため理由を残す。

## 2. 実装開始前の決定

D-001〜D-008 は、2026-08-19 に利用者が提案どおり一括承認した。

### D-001 初版の範囲

- 状態: **決定**（2026-08-19）
- 推奨: 初版は旧 MATLAB 教材相当の中核、すなわち 2D/3D ベクトル、span、一次独立・一次従属、共有 URL に限定する。
- 理由: シラバス全体へ広げる前に、共通となる状態・数学・描画・共有の設計を授業で評価できる。基底以降の Lab を作り直すリスクを下げる。
- 影響: 基底、線形写像、固有値、内積等はロードマップのフェーズ 8 で個別に判断する。

### D-002 アプリの技術構成と配布方式

- 状態: **決定**（2026-08-19）
- 推奨: TypeScript、Vite、React を使用する完全クライアントサイドの静的 Web アプリとする。サーバー API、認証、データベースは初版に含めない。
- 理由: 状態に応じて複数の UI と描画を同期する用途に適し、自動テストと静的ビルドを組みやすい。学内 Web サーバーには静的ファイルだけを配置できる。
- 条件: 学内サーバーの公開パス、HTTPS、URL 書き換え可否をフェーズ 6 の残作業より前に確認する。

### D-003 描画技術

- 状態: **決定**（2026-08-19）
- 推奨: 2D は React から制御する SVG、3D は Three.js を使用する。D3 と math.js は、必要性が明確になるまで追加しない。
- 理由: 2D のベクトル、軸、ラベル、直線は SVG で十分に表現でき、DOM としてアクセシビリティも補いやすい。3D の視点操作と半透明平面は Three.js の既存機能を利用できる。依存ライブラリを必要最小限に保てる。

### D-004 ベクトルの入力方式

- 状態: **決定**（2026-08-19）
- 推奨: 数値入力と先端ドラッグの両方を同期して提供する。最初の 2D 縦断版では数値入力を先に実装し、次の作業単位でドラッグを追加する。3D の直接編集はカメラ操作との競合を試作後に判断する。
- 理由: 数値入力は授業例の正確な再現に必要で、ドラッグは変化の観察に有効である。別々の作業単位にすると、それぞれを確認しやすい。

### D-005 span と一次独立の表示

- 状態: **決定**（2026-08-19）
- 推奨: span 対象は各ベクトルのチェック操作で選ぶ。可視化に加え、日本語を主とした短い判定文、rank、span の次元を常時対応付けて表示する。面積・体積・行列式は初版の必須表示にしない。
- 理由: 色や形だけで意味を推測させず、「選択したベクトル数」と「独立な最大個数」と「span の次元」の関係を直接観察できる。行列式は正方形の場合に限られ、最初の概念説明を複雑にする。

### D-006 共有状態と URL 形式

- 状態: **決定**（2026-08-19）
- 推奨: バージョン付き JSON スキーマを定義し、Base64URL 形式の一つのクエリーパラメーターへ格納する。初版では圧縮しない。少なくとも次元、ベクトルの ID・表示名・座標、span 選択、教材上意味のある表示切替を含める。3D のカメラは、教員が意図した見え方の再現に必要かを 3D 実装時に判断する。一時的なパネル開閉、hover、ドラッグ中状態は含めない。
- 理由: スキーマを一箇所で検証でき、フィールド追加時に版を識別できる。初期段階では可読性とデバッグ容易性を圧縮より優先する。
- 未解決: 学内サーバーまたは利用サービスが許容する URL 長。

### D-007 Reset の意味

- 状態: **決定**（2026-08-19）
- 推奨: `DefaultState`、`InitialState`、`CurrentState` を区別し、Reset は共有 URL を含むページ読み込み時の `InitialState` へ戻す。アプリ既定例へ戻る操作が必要なら別名で提供する。
- 理由: 教員が配布した例を学生が操作した後、同じ授業例へ簡単に戻れる。

### D-008 対応環境とアクセシビリティの基準

- 状態: **決定**（2026-08-19）
- 推奨: 初版は現行の Chrome、Edge、Firefox、Safari の直近主要版を対象とし、PC、タブレット、スマートフォンで閲覧可能にする。操作の完全同等性は、2D ではキーボード・マウス・タッチ、3D では少なくともマウス・タッチを目標とする。色だけに依存しない識別、明示的なラベル、キーボードで変更できる数値入力、十分なタッチ領域を必須とする。
- 理由: シラバス上の教材として端末を限定しすぎず、視覚表現を利用できない状況でも数学的状態を確認できるようにする。
- 未解決: 学内で実際に管理されている端末・ブラウザの情報。

### D-009 rank 判定の数値方針

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 1.2）
- 方針: 初版の内部計算は JavaScript の有限な倍精度浮動小数点数を使用する。各ベクトルを最大絶対座標で個別に正規化した後、部分ピボット付き消去法で rank を求める。
- 許容誤差: 既定の相対許容誤差は `1e-10` とし、数学ロジックの呼び出し時に変更可能にする。
- 意味: ベクトルの単純な定数倍で判定が変わりにくく、正規化後のピボットが許容誤差以下なら、教材上はほぼ一次従属として扱う。
- 規約: 空集合は rank 0、span の次元 0、一次独立とする。0 ベクトルを一つでも単独で含む集合は一次従属となる。
- 保留: 有理数・無理数の厳密計算と `1/3`、`√2` 等の表示は初版の rank 判定に含めず、教材上の必要性を確認して別途判断する。
- 操作確認結果: 2D の直接操作後も `1e-10` を維持する。ドラッグだけで完全な平行へ合わせにくい問題は、数学的判定閾値を緩めず D-019 の操作専用スナップで解決した。許容誤差付近を学生へどう説明するかは引き続き教材設計上の確認事項とする。
- 2.7棚卸し: 利用者が現状維持を確認した。既定値 `1e-10` と D-019 との分離を変更しない。

### D-010 共有状態 v1 のスキーマと安全上限

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 1.3）
- スキーマ: `v`、`lab`、`dim`、`vectors`、`spanSelection`、`visualization.showSpan` だけを受け付ける。未知のフィールドは暗黙に保持せず拒否する。
- エンコード: 検証・正規化した状態を UTF-8 JSON に変換し、パディングなしの Base64URL とする。圧縮は行わない。
- デコード: Base64URL、UTF-8、JSON、スキーマの順に検証し、例外を外へ漏らさず成功または分類済みエラーを返す。
- 安全上限: ベクトル 8 本、ID 32 文字、表示名 40 文字、座標絶対値 `1,000,000`、エンコード文字列 8192 文字とする。
- 理由: 初版の授業例には十分な余裕を持たせつつ、改変 URL による過大なメモリ・描画負荷を早期に拒否できる。状態を正規化することで同じ状態から決定的な文字列を生成できる。
- 注意: 8192 文字はアプリの防御上限であり、学内サーバー、LMS、スライド等で利用可能な URL 長を保証する値ではない。
- 再評価条件: 2D の実例を共有 URL にして、対象ブラウザ、教材スライド、LMS 等で往復確認した後に上限と圧縮の必要性を再確認する。

### D-011 公開方式

- 状態: **運用候補**（2026-08-19）
- 候補: GitHub Pages のプロジェクトサイト、または学内 Web サーバー上の nginx で静的成果物を配信する。
- 共通方針: サーバー API を必要としない静的アプリを維持し、共有状態は同じページのクエリーパラメーターで表現する。公開先のサブパスを前提に、アセットとアプリ内リンクをベースパス対応にする。
- GitHub Pages: 通常のプロジェクトサイトは `/<repositoryname>/` 以下で公開されるため、Vite の `base` を公開パスに合わせる必要がある。GitHub Actions によるビルドと `dist/` の公開を候補とする。
- nginx: ルートまたはサブパスのどちらで配信するかを確認する。将来クライアント側ルーティングを導入する場合は、`try_files` 等による `index.html` へのフォールバックが必要になる。
- URL 長: nginx の既定構成では大きなリクエストヘッダーバッファが 8 KiB で、リクエスト行は一つのバッファを超えられない。D-010 の 8192 文字は状態文字列だけの防御上限なので、完全な URL としてそのまま通る保証はない。
- 最終決定時に必要な情報: 公開 URL、ルートかサブパスか、HTTPS、GitHub Pages のカスタムドメイン有無、nginx 設定を変更できる権限、学内ネットワーク外からの利用可否。

### D-012 2D 座標面の初期表示

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 2.1）
- 初期例: 旧教材で扱いやすい代表例として、`a₁ = (2, 1)`、`a₂ = (-3, 2)` を表示する。記号はD-034で `v` から `a` へ変更した。
- 表示範囲: x 軸・y 軸ともに `-5` から `5` までの正方形とし、整数単位の目盛線を表示する。
- 識別: 各ベクトルは矢印上の名前、一覧上の番号・名前・座標で対応付け、色だけに依存しない。
- 理由: 数値入力を入れる前に、座標変換、視認性、既存の rank 判定との接続を独立して確認できる。
- 操作確認結果: 数値入力、動的な自動スケール、手動表示、先端ドラッグを実装して利用者が確認した。`[-5, 5] × [-5, 5]` は固定上限ではなく、自動追従時の最小基準範囲として維持する。
- 2.7棚卸し: 利用者が現状維持を確認した。基準範囲と初期例を変更しない。

### D-013 2D 表示範囲の自動・手動操作

- 状態: **決定**（2026-08-19）
- 自動操作: ベクトルを操作・編集して現在の表示範囲を超える場合を含め、原則としてベクトルが見える範囲へ自動的にスケーリングする。
- 手動操作: PC では少なくともマウスホイール、タッチ端末では少なくともピンチ操作により、利用者が表示範囲を拡大・縮小できるようにする。
- 補助操作: ホイールやピンチを使えない利用者のため、キーボードで操作できる拡大、縮小、「全体を表示」の画面ボタンを併設する。
- 理由: 数値入力可能範囲と可視範囲を固定値 `[-5, 5]` に結び付けず、大小の異なる例を同じ教材画面で観察できるようにする。
- 具体化: 自動追従と手動操作の切替、倍率の上下限、ズーム感度、パンは D-014・D-017 で試行採用し、利用者確認済みである。共有時は D-023 により手動表示範囲を含めず、復元時に自動範囲を再計算する。
- 2.7棚卸し: 利用者が現状維持を確認した。自動スケールと手動ズームの原則を変更しない。

### D-014 自動追従モードと表示範囲の共有

- 状態: **試行採用・利用者確認済み**（2026-08-19）
- モード: 初期状態は自動追従とし、全ベクトルの終点と原点が余白付きで収まる正方形範囲を計算する。`[-5, 5] × [-5, 5]` を自動追従時の最小範囲とする。
- 手動優先: ホイール、ピンチ、パン、拡大・縮小ボタンを使った後は手動表示を維持し、その後のベクトル編集で表示範囲を勝手に変更しない。「全体を表示」で自動追従へ戻す。
- ドラッグ中: 自動追従モードでも、先端ドラッグ中は座標変換を安定させるため表示範囲を固定し、操作終了後に再計算する。
- 共有: 当初は手動表示の中心と倍率を共有候補としたが、D-023 により共有対象外へ変更した。復元時はベクトルから自動表示範囲を再計算する。
- スキーマ: 表示範囲を追加しないため、現行の厳密な共有状態 v1 を維持する。
- 理由: 自動追従が手動操作を打ち消すことと、ドラッグ中の表示倍率変化による操作の不安定化を避けつつ、共有 URL の再現性を保つ。
- 再評価条件: 数値入力、手動ズーム、パン、先端ドラッグを実際に操作し、自動追従との切替や共有時の再現が授業用途に適するかを確認する。

2.7の統合棚卸しで利用者が現状維持を確認した。自動／手動の切替とドラッグ中の規則は変更せず、共有部分だけはD-023の「手動表示を共有しない」方針を正とする。

### D-015 教材内の数学用語・数式表記

- 状態: **試行採用・利用者確認済み**（2026-08-19、利用者提示）
- 基準文書: リポジトリ直下の `math-writing-rules.txt` を、学生向け画面・説明・例題で使用する数学用語と数式表記の基準とする。利用者による追記・修正を反映できる生きた文書として扱う。
- 適用範囲: 画面に表示する数式、ラベル、説明文、アクセシブルな読み上げ文、授業用例を対象とする。TypeScript の型名、プロパティ名、テスト名等の内部識別子は対象外とする。
- フェーズ 2 の主要規則: 数ベクトルは列ベクトルとする。ベクトル記号は基本文字だけをボールド斜体小文字とし、添え字の数字は立体レギュラーとする。一次結合・一次関係・一次独立・一次従属は「一次」を優先する。「ベクトルが生成する空間」を主表現とし、`span` と「張る空間」を対応する補助語として示す。
- `rank` と `dim`: `rank` は列ベクトルを並べた行列または写像を引数として明示し、`dim` はベクトル空間を引数として明示する。例えば `S = {a₁, a₂}`、`A = [a₁ a₂]` を定義して `dim(span(S)) = rank(A)` の対応を示す。
- 集合と組: 順序に依存しない対象は集合、基底や列の順序を扱う対象は組として区別する。`span` の既定表記ではベクトル集合を波括弧で明示する。
- 描画方法: TeX 風の意味と見た目を再現するが、HTML/CSS、MathML、数式描画ライブラリのどれを採用するかは、必要な式の複雑さとアクセシビリティを確認して別途決める。生の TeX コマンドを学生向け画面へそのまま表示しない。
- 棚卸し: 各作業単位の画面確認と各フェーズ終了時に、画面文言を `math-writing-rules.txt` と照合する。

### D-016 数値入力と自動スケールの初期挙動

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 2.2）
- 入力形式: 10 進整数、小数、指数表記を受け付ける。空欄、入力途中、16 進表記、非有限値は確定済みの数学状態へ反映しない。
- 入力上限: 共有状態と同じく各成分の絶対値を `1,000,000` 以下とする。`-0` は `0` に正規化する。
- 入力途中: 入力欄の文字列と `CurrentState` の数値を分離し、不正な文字列を画面に残して訂正可能にしながら、描画と判定には直前の有効値を用いる。
- 自動範囲: 原点中心の正方形とし、`[-5, 5] × [-5, 5]` を最小範囲とする。最大絶対成分に 15% の余白を加えて全ベクトルを収める。
- 目盛: 表示幅を約 10 区間に分けることを目標に、`1`、`2`、`5` と 10 の累乗から目盛間隔を選ぶ。大きい値は指数表記にする。
- 数式表示: OS が提供する `Cambria Math` 等の数式用フォントを優先する。ベクトルの基本文字と行列をボールド斜体、ベクトルの添え字数字と `rank`、`dim`、`span` を立体レギュラーで表示する。現段階では外部数式描画ライブラリを追加しない。
- 再評価条件: 小数・指数表記・上限付近の入力、自動倍率の変化、画面幅の違いを操作し、余白、目盛密度、入力確定方式、数式の見た目を確認する。

### D-017 2D 手動表示操作の初期値

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 2.3）
- モード: ページ読み込みと「全体を表示」後は自動追従、ホイール、ピンチ、パン、拡大・縮小ボタンの使用後は手動表示とする。手動表示中の数値編集では表示範囲を維持する。
- ズーム中心: ホイールはポインター位置、ピンチは2点の中点を数学座標上の固定点として倍率を変える。画面ボタンは現在の表示中心を基準にする。
- 画面ボタン: 拡大は表示幅を `0.8` 倍、縮小は `1.25` 倍とする。ネイティブなボタンとしてキーボード操作とフォーカス表示を提供する。
- ホイール感度: 1イベントの係数を `exp(deltaY × 0.0015)` とし、極端なイベント値は計算前に抑える。
- パン: 座標面の背景をドラッグした距離に対応して表示中心を移す。ベクトル自体は作業単位 2.4 までポインター操作の対象にしない。
- 安全範囲: 正方形表示の半幅を `0.1` 以上 `2,000,000` 以下、中心座標の絶対値を `2,000,000` 以下に制限する。
- 範囲外表示: パンまたはズームにより原点やベクトルが表示外へ出ることを許可し、描画を座標面内へクリップする。「全体を表示」で復帰できる。
- 再評価条件: マウスホイール、トラックパッド、ピンチ、背景ドラッグ、画面ボタンを実機で操作し、感度、上下限、スクロール競合、軸・目盛の視認性を確認する。

### D-018 2D ベクトル先端ドラッグの初期挙動

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 2.4。平行操作の課題は D-019 で解決）
- 入力手段: マウス、ペン、タッチを Pointer Events で統一して扱う。キーボードでは既存の数値入力欄を同じ数学状態への代替操作とする。
- 操作領域: 見た目の先端点とは別に、半径 `18` の透明な SVG 操作領域を設ける。hover またはドラッグ中はベクトル色の薄い円で対象を示す。
- ジェスチャー分離: 先端の操作領域はベクトル編集専用、座標面の背景はパン・ピンチ専用とし、どちらかの操作中にもう一方を開始しない。先端ドラッグ中のホイールズームも抑止する。
- 表示範囲: 自動表示ではドラッグ開始時の範囲を終了まで固定して終了後に再計算する。手動表示では編集前後の範囲を維持する。
- 座標値: グリッドへのスナップは行わない。表示上の1ピクセルに相当する数学座標を基準に10の累乗の丸め幅を選び、数値入力欄が不要に長い小数にならないようにする。
- 安全上限: 数値入力および共有状態と同じく各成分の絶対値を `1,000,000` 以下に制限し、`-0` を `0` に正規化する。
- 再評価条件: マウスとタッチ相当の操作で、先端のつかみやすさ、丸め幅、自動再フィット、手動表示の維持、ほぼ平行な位置での D-009 の判定変化を確認する。

利用者の操作確認では全体として問題なかったが、D-009 の `1e-10` にドラッグだけで座標を合わせ、一次従属へ変化させることは難しかった。数学的な判定閾値を緩めず、D-019 の操作補助を追加する。

### D-019 ドラッグ中の平行スナップ

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 2.4）
- 適用範囲: ベクトル先端のドラッグ中だけに適用する。数値入力には適用せず、入力値を暗黙に変更しない。
- 操作閾値: ドラッグ中のベクトル `v` の先端と、他の非零ベクトル `w` が張る直線との垂直距離 `|det(v, w)| / ||w||` を用いる。基準表示範囲 `[−5, 5] × [−5, 5]` では距離 `0.1`、一般には現在の表示幅の `1%` 以下で吸着する。表示幅100では `1`、表示幅4では `0.04` となり、画面上の吸着幅を一定に保つ。
- 吸着位置: 閾値内に複数の候補がある場合は画面上の垂直距離が最も近いベクトルを選び、そのベクトルが張る直線へ先端を直交射影する。同方向と反対方向の両方を対象とする。
- 零ベクトル: ドラッグ中のベクトルが零ベクトルの場合は吸着しない。零ベクトルは方向を持たないため、吸着先からも除外する。
- 視覚表示: 吸着中はドラッグ先端の操作円を強調し、吸着先ベクトルを太い破線で表示する。
- 安全上限: 射影後に座標上限を超える場合は、平行関係を壊さないよう両成分を同じ倍率で縮小する。
- D-009 との分離: rank 判定の相対許容誤差 `1e-10` は変更しない。スナップ後の座標を完全な平行として `CurrentState` へ反映し、その状態を通常の D-009 で判定する。
- ヒステリシス: 現時点では不要との利用者判断により、吸着開始と解除に同じ距離を用いる。
- 再評価条件: 基準範囲、拡大表示、縮小表示で画面上の吸着幅が同程度に感じられるか、吸着時の跳び、境界から外れるときの安定性、視覚表示を実際に操作して確認する。

利用者の操作確認では、画面幅 `1%` の吸着距離、表示倍率に対する相対性、ヒステリシスなしの操作感に問題はなかった。

### D-020 2Dで生成する空間を選択・表示する初期方式

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位 2.5）
- 選択操作: 各ベクトルの編集欄にネイティブなチェックボックスを設け、マウス、タッチ、キーボードで `spanSelection` を変更する。選択順ではなく画面のベクトル順で集合を表示する。
- 空集合: 全ベクトルのチェックを外すことを許可する。空集合は rank 0、一次独立であり、生成する空間は原点だけからなる零部分空間とする。
- 幾何表示: rank 0 は原点の二重マーカー、rank 1 は原点を通る太い破線、rank 2 は座標面全体の薄い面と斜線パターンで表す。座標面内の日本語ラベルとカード内の説明を併用する。
- 選択の識別: 幾何表示中は `spanSelection` に含まれない矢印の不透明度を下げる。選択チェック、線種、パターン、ラベルを併用し、色だけに依存しない。
- 表示切替: `visualization.showSpan` は幾何表示だけをオン・オフし、選択集合と解析結果は維持する。既定例では2本を選択し、幾何表示をオンにする。
- 数学表示: D-034により、選択集合を `S`、選択した列ベクトルを並べた行列を `A` とし、`rank(A)` と `dim(span(S))` を表示する。全ベクトルのカードでも、そのカードに表示中の全列を同じ規則で `A` とする。
- 状態共有: 既存の共有状態 v1 にある `spanSelection` と `visualization.showSpan` をそのまま用いるため、スキーマ変更は行わない。
- 再評価条件: 空集合、1本、平行な2本、一次独立な2本を切り替え、選択操作、初期表示、幾何表現、説明の分かりやすさを確認する。

利用者の操作確認では基本的な表示と操作に問題はなかった。「2次元座標平面全体」の座標面内ラベルが固定枠をはみ出したため、文字列長に応じた可変幅へ修正した。

### D-021 2D Labのグラフと詳細カードの配置

- 状態: **試行採用・利用者確認済み**（2026-08-19、作業単位2.5後のレイアウト改善）
- 問題: 編集、選択span、全ベクトルの3カードを右列へ縦積みすると、全ベクトルの判定が初期表示範囲外になり、カード追加や内容増加にも耐えにくい。
- 常時要約: 選択集合について形と `dim(span(S))`、全ベクトルについて一次独立・一次従属と `rank(A)` を常時表示する。各要約は対応する詳細タブを開くボタンも兼ねる。
- 詳細: 「ベクトル編集」「生成する空間」「全ベクトル」の3タブから一度に1パネルを表示する。既定は操作頻度が高い「ベクトル編集」とする。
- PC: 十分な横幅では座標面とインスペクターの2列を維持する。幅に加えて画面高さが `820px` 以上の場合だけ座標面をsticky表示する。右列だけの独立スクロールは設けない。
- タブレット: 横幅 `980px` 以下では座標面を上、要約と詳細を下に置く1列構成とし、要約は2列で表示する。
- スマートフォン: 横幅 `640px` 以下では要約も1列にし、タブ名を「編集」「span」「全体」へ短縮する。sticky表示は使用しない。
- アクセシビリティ: ARIAのtablist・tab・tabpanelを用い、ネイティブボタン、選択状態、フォーカス順に加えて左右矢印、Home、Endキーでタブを移動できるようにする。
- 共有状態: 選択中の詳細タブは教材の数学状態ではない一時的UI状態であり、共有URLへ含めない。
- 拡張方針: 新しい詳細は既存の意味カテゴリへ整理し、タブ数を無制限に増やさない。比較に必要な結果は常時要約へ残す。
- 再評価条件: PC・タブレット・スマートフォン相当の幅、低いPC画面、キーボード操作で、情報の見つけやすさ、グラフとの比較、stickyの挙動を確認する。

利用者の表示・操作確認では、常時要約、3タブ、座標面との比較、レスポンシブ配置を含めて問題はなく、良好な表示と確認された。端末固有の問題が見つかった場合は再評価する。

### D-022 共有URLエクスポートダイアログ

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位2.6）
- 起点: 2D Lab上部の「共有URLをエクスポート」ボタンからモーダルダイアログを開く。グラフだけでなく教材状態全体の操作であるため、表示範囲ツールバーとは分ける。
- 内容: 2.6時点では、読み取り専用で選択可能な共有URL欄を表示し、その下に「クリップボードにコピー」「テキストで保存」「閉じる」の3ボタンを置く。PCでは横並び、狭い画面では縦並びとした。D-036でQR表示・保存を加えた4ボタンへ拡張した。
- コピー: Clipboard APIが利用できる場合はボタンでコピーし、成功をダイアログ内へ通知する。HTTPS要件、権限、ブラウザ差等で失敗した場合はURLを選択し、手動コピーを案内する。
- テキスト保存: URLと末尾改行だけをUTF-8テキストへ入れ、日時を含む `linear-algebra-visual-lab-url-YYYYMMDD-HHmm.txt` としてダウンロードを開始する。
- 閉じ方: 「閉じる」、Escape、背景クリックで閉じ、キーボードフォーカスをダイアログ内に保って終了後は呼出元へ戻す。
- 入力エラー: 画面に未確定の不正な成分文字列がある間は、見えている入力と共有内容の不一致を避けるためエクスポートを無効にする。
- 当時の範囲外: アプリ内QR生成は2.6へ含めなかった。D-036によりフェーズ5前の追加作業4.6として後から採用した。

### D-023 2D表示範囲を共有しない方針

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位2.6。D-014の共有方針を一部置換）
- 共有対象: 現行v1の次元、ベクトルID・表示名・座標・順序、span選択、`visualization.showSpan`を共有する。
- 共有対象外: 自動／手動表示モード、手動表示の中心・倍率、選択中の詳細タブ、入力途中の文字列、hover・drag・吸着等の一時状態、rank等の再計算可能な派生値は共有しない。
- 復元表示: 共有URLを開くと常に自動表示モードで開始し、「全体を表示」と同じ規則で原点と全ベクトルが収まる範囲を計算する。
- エクスポート時: 現在のパン・ズーム状態は変更せず、URLにも埋め込まない。
- スキーマ: 表示範囲を追加しないため共有状態はv1を維持し、v2とマイグレーションは不要とする。

### D-024 共有URLの読込、エラー、InitialState、Reset

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位2.6）
- URL形式: 現在のoriginと公開サブパスを維持し、クエリーパラメーター名を `state` とする。エクスポートURLから既存クエリーとハッシュを除き、状態を1件だけ格納する。エクスポートしても閲覧中ページのURLとブラウザ履歴は変更しない。
- 通常起動: `state` がなければ `DefaultState` を `InitialState` とする。
- 共有起動: 2.6時点では有効な2D v1を復元した。D-037以後は有効な2D/3D状態を該当次元の`InitialState`とし、その次元を初期選択する。
- 不正URL: 空、重複、壊れた状態、未対応版は安全に拒否する。アプリは停止せず `DefaultState` を `InitialState` として表示し、復元できなかった理由を閉じられる案内で示す。2.6時点で拒否していた3D状態はD-037で対応状態へ変更した。
- Reset: 2D単独時は`CurrentState`を`InitialState`へ戻し、入力欄、自動範囲、詳細タブ、ドラッグ・吸着・エクスポートを同期した。D-037以後は現在次元だけへ同じ原則を適用し、他方の次元を変更しない。閲覧中URLは変更しない。
- 配布: GitHub Pages等のサブパスを維持する。クリップボードの安定利用にはHTTPSを推奨し、HTTP環境では手動コピーとテキスト保存を代替手段とする。

### D-025 GitHub Pagesへの初回公開と自動デプロイ

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、フェーズ3着手前に実施し、現在の番号では作業単位6.1）
- 初回公開先: GitHub Pagesのプロジェクトサイト `https://d-kitamura.github.io/linear-algebra-visual-lab/` とする。学内nginxは将来の代替公開先として残す。
- 公開元: mainのソースファイルをそのまま配信せず、GitHub ActionsがViteで生成した `dist/` だけをPages artifactとして公開する。PagesのSourceは「GitHub Actions」とする。
- 起動条件: mainへのpushで自動実行し、障害調査や再実行のためActions画面からの手動実行も許可する。
- ビルド: リポジトリの `packageManager` 指定に合わせてpnpmを用い、ロックファイルに従って依存関係をインストールする。自動テスト成功後、`APP_BASE_PATH=/linear-algebra-visual-lab/` を指定して本番ビルドする。
- デプロイ: `dist/index.html` とハッシュ付き静的アセットを一つの成果物としてアップロードし、`github-pages`環境へデプロイする。`dist/`をmainへコミットしない。
- 共有URL: 実行時の `window.location.href` を基準とする現行方式を維持し、本番originと `/linear-algebra-visual-lab/` を自動的に共有URLへ反映する。
- 初回公開時の担当分担: Codexはローカルのワークフロー、文書、ビルド検証を担当し、利用者はGitHub上のSettings、コミット、push、Actions実行結果と公開画面を確認する。日常更新のcommit・push担当は、フェーズ3終了時にD-030で変更した。
- 再評価条件: リポジトリ名、公開先、カスタムドメインを変更した場合はbase pathを見直す。GitHub Actionsの参照バージョンは実装・棚卸し時に公式の現行版を確認する。

ローカル検証ではpnpm 11.19.0によるロックファイル整合、11ファイル・93テスト、TypeScript型検査、公開用base pathでのViteビルドに成功した。`dist/index.html` が `/linear-algebra-visual-lab/assets/` 以下のJavaScriptとCSSを参照し、入口と両アセットが同サブパスの静的配信でHTTP 200になることを確認した。その後、利用者がcommit・mainへのpush・Pages設定を行い、GitHub Actionsの成功、本番画面と再読み込み、共有URLの生成・別タブ復元、Reset、コピー、テキスト保存がすべて正常であることを確認した。

### D-026 ベクトル集合の追加・削除

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位3.1）
- 本数: 2Dの表示中ベクトルを0本から共有状態v1の上限8本まで追加・削除できる。上限では追加操作を無効にし、空集合も正規の数学状態として扱う。
- 追加時の値: 2Dでは `(1, 0)` を既定座標とし、追加したベクトルを生成する空間（span）の対象へ含める。将来3Dへ同じ処理を拡張する場合の既定座標は `(1, 0, 0)` とする。
- 識別子と表示名: D-034により `a1` / `a₁` のような正の整数添え字を使い、既存IDまたは既存表示名と衝突しない最小の番号を割り当てる。削除で空いた番号は次回追加時に再利用できる。3.1ではIDと表示名を利用者が編集する機能を設けない。
- 色: 表示順に8色の固定パレットを割り当て、共有状態へ色を保存しない。3.1では色を利用者が編集する機能を設けない。
- 削除: 確認ダイアログを挟まず即時に削除し、同じIDをspan選択と入力途中状態からも除く。誤操作からはResetでInitialStateへ戻せる。削除後のキーボードフォーカスは追加ボタンへ移す。
- 状態と共有: 現行の共有状態v1を変更せず、追加・削除後のベクトル列とspan選択をエクスポート・復元する。Resetはページ読み込み時のInitialStateを本数を含めて復元する。
- 再評価条件: 8本時の編集・行列表示、狭い画面、削除操作、既定値と自動命名の操作感を利用者が確認し、必要があれば3.2以降で調整する。

利用者がデプロイ先で0〜8本の追加・削除、表示、解析、共有URLの生成・復元を確認し、すべて問題なしとした。

### D-027 2D教材版の入力案内・アクセシビリティ・授業シナリオ

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位3.2・3.3）
- 未確定入力: 空欄、不完全な数値、範囲外を各成分の入力欄に対応付けて説明し、数学状態には直前の有効値を維持する。画面上部に未確定数と「入力欄を確認」を表示して最初の該当欄へ移動できるようにし、訂正までエクスポートを無効にする。
- 共有URLエラー: 読込と生成を区別した見出し、理由、固有の「閉じる」読み上げ名を持つalertとして示す。不正URLでは既定例へ安全にフォールバックする既存方針を維持する。
- アクセシビリティ: 教材操作領域へのスキップリンク、数値添え字を「添え字」と明示する読み上げ、入力欄ごとのエラー説明、主要操作の44px以上の高さ、ネイティブdialogと既存のキーボード操作を採用する。色に加えて番号、名前、形、線種、説明文を引き続き併用する。
- 授業シナリオ: 空集合、零ベクトル、平行な2本、一次独立な2本、3本以上の一次従属を正式な2D代表例とする。状態、学習目的、期待結果を `src/teaching/` に置き、本番共有URLと授業内手順を `docs/TEACHING_SCENARIOS.md` に記録する。
- 共有とReset: 各代表例の本番URLが同じ状態へ往復し、編集後のエクスポートと別タブ復元、Resetによる配布時InitialStateへの復帰を一連の授業手順とする。
- 3Dへの境界: ベクトル・数学解析・共有状態・集合編集・教材シナリオ構造を再利用し、SVG座標面、2Dジェスチャー、平行スナップは2D固有とする。D-037で3Dの描画・カメラ・直接編集・span・カメラ共有の基本方針を決定した。
- 再評価条件: デプロイ先のPCと狭い画面で、未確定入力からの復帰、Tab移動、共有ダイアログ、5つの代表例を利用者が確認する。

利用者がD-027の内容をデプロイ先で確認し、入力案内、アクセシビリティ、授業シナリオを含めて問題なしとした。

### D-028 モバイル座標面のジェスチャー所有

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位3.2の実機修正）
- 発見事項: スマートフォンでは、矢印先端のドラッグと座標面のピンチが一瞬動いた後、ページ全体のスクロールまたはズームへ移行した。背景矩形と先端操作領域だけに `touch-action: none` を指定しており、SVG座標面全体ではブラウザ既定ジェスチャーを抑止できていなかった。
- 方針: SVG座標面全体へ `touch-action: none` を適用し、その内側で開始した1本指パン、2本指ピンチ、矢印先端ドラッグを教材操作として完結させる。Pointer Eventsとpointer captureによる既存処理は維持する。
- ページ操作: グラフ外のページスクロールとページ全体のアクセシビリティ上のピンチズームは維持する。`user-scalable=no` や `maximum-scale=1` は指定しない。学生向け案内で、ページスクロールは座標面の外側から行うことを明示する。
- 再評価条件: iOS SafariまたはChrome、Android Chrome等の実端末で、先端ドラッグ、1本指パン、2本指ピンチ、グラフ外スクロール、グラフ外ページズームを確認する。

利用者が再デプロイ後のスマートフォンで期待どおり操作できることを確認し、D-028を完了した。

### D-029 生成する空間の中立色

- 状態: **決定・実装済み・利用者確認済み**（2026-08-19、作業単位3.3の視認性修正）
- 発見事項: 生成する空間の暗いピンクと、1番目のベクトルの赤橙色が近く、特にrank 1の太い破線と重なる場合にベクトルが見づらかった。
- 意味上の方針: 生成する空間は特定のベクトルに属さないため、8色のベクトルパレットから独立した中立的な灰色で表す。
- 座標面の表現: rank 0の原点マーカー、rank 1の太い破線、rank 2の薄い背景と斜線、座標面内ラベルを灰色系へ統一する。破線は濃い灰色 `#5d686f`・不透明度0.64、斜線は中間灰色、背景は淡い灰色を基準とする。
- 維持する識別: rank別の形、破線、斜線、ラベル、説明文を維持し、色だけに依存しない。ベクトルの色、線幅、矢じり、span未選択ベクトルの薄表示は変更しない。
- 再評価条件: rank 1で直線と重なる各色のベクトル、およびrank 2の背景上の全ベクトルをデプロイ先で確認する。

利用者がD-029の灰色系表示をデプロイ先で確認し、内容と視認性を問題なしとした。

### D-030 日常開発のcommit・push・デプロイ確認

- 状態: **決定済み**（2026-08-19、フェーズ3終了時の運用変更）
- 担当: 各作業単位の実装、文書更新、自動検証が完了した後、Codexが現在のmainへcommitし、originのmainへpushする。
- コミットメッセージ: 既存履歴に合わせ、作業単位または主要な変更を端的に表す短い日本語を用いる。
- 安全確認: commit前にブランチ、作業ツリー、差分を確認し、利用者による無関係な変更を混在させない。競合、認証失敗、意図しない差分がある場合は、破壊的な解消を行わず利用者へ報告する。
- 公開確認: mainへのpushでD-025のGitHub Actionsを起動し、利用可能な範囲で対象commitの完了とGitHub Pages公開URLを確認してから結果を引き渡す。テストまたはデプロイが失敗した場合は、公開済みの直前版が維持されることと失敗箇所を報告する。
- 範囲: この決定は日常更新の担当を変更する。GitHubのSettings、Secrets、リポジトリ設定等を別途変更する権限まで自動的に広げない。

### D-031 2D一次結合エクスプローラ

- 状態: **決定・4.1〜4.5実装・利用者確認・統合棚卸し済み**（2026-08-20、フェーズ4完了）
- フェーズ構成: 3D教材版の前に新しいフェーズ4「2D一次結合エクスプローラ」を挿入し、従来のフェーズ4以降を5〜8へ一つずつ送る。2Dで確立した数学・共有・操作をフェーズ5の3Dへ拡張する。
- 学習目的: 選択集合が生成する空間を、任意のターゲットベクトル `v` が一次結合で表現できるか、その係数が一意かという事実へ結び付ける。2本の一次独立な2Dベクトルでは、平面上の任意の `v` に一意な係数が存在することを数値例と一般式で示す。
- 対象集合: 一次結合には既存のspan選択をそのまま使い、0〜8本を許可する。2本へ制限せず、表現不可能・一意・無数の違いを教材として扱う。ターゲット `v` は解析対象の集合へ追加する通常ベクトルではなく、所属と係数を調べる別の教材状態とする。
- 数学結果: 選択ベクトルを列に持つ行列を `A`、係数列を `c` とし、`Ac = v` と拡大行列のrankから解なし・一意・無数を分類する。表現可能なら特解、異なる最大2例、自由係数の数、一般解の零空間基底を扱う。D-009と同じ相対許容誤差を使う。
- 共有とReset: ターゲット `v` とエクスプローラ表示を共有URLへ含め、共有状態v1の読込を維持した後方互換なv2を設ける。Resetは共有時のターゲットを含むInitialStateへ戻す。
- 共有状態v2: `linearCombination` に `visible: boolean` と、次元に一致する有限座標または未配置を表す `target: number[] | null` を持たせる。v1読込時は `{ visible: false, target: null }` を補ってv2へ移行し、新しく生成するURLは常にv2とする。両フィールドは独立で、非表示時にもターゲットを保持でき、表示中で未配置の状態も表せる。
- 係数表示: 当面は小数とする。一般の転置横書き列ベクトル `{}^t[a, b]` に対する係数公式は既定で折り畳み、利用者が展開して常時表示できるようにする。分数・根号の自動認識は初期実装に含めない。
- ターゲット操作: rank 1の生成直線とrank 0の原点への吸着距離は、D-019と同じ現在表示幅の1%を初期値とする。数学判定のD-009とは分離し、実装後の操作確認で調整できるようにする。
- ターゲット操作の具体化: モード中の短いクリック・タップは配置、一定距離以上の背景移動はパン、2点操作はピンチとする。ターゲット先端は通常ベクトルと独立してドラッグできる。吸着はクリック・タップと先端ドラッグだけに適用し、明示的な数値入力は変更しない。ターゲットは深い青の破線矢印とボールド斜体の `v` で通常ベクトルと区別する。
- 幾何表示: 2項の一次結合では `c₁a₁` と `c₂a₂` をともに原点から描き、その2本を辺とする平行四辺形を補助線で示し、原点の対角に `v` の先端を置く。頭尾接続による移動経路の描画は採用しない。
- 無数解の表示: D-034により、異なる2例を既定表示し、自由係数を使った一般解だけを折り畳む。3本以上では2項の平行四辺形と誤解させないため幾何表示を行わない。
- 作業分割: 4.1求解ロジック、4.2共有状態v2、4.3ターゲット操作、4.4数式・幾何表示、4.5共有・授業シナリオ・統合確認の順に進める。

### D-032 正式リリース前の共有URL互換性

- 状態: **決定済み**（2026-08-20、4.3着手時）
- 保証開始時点: 学生へ正式に公開するリリース以後は、共有URLの後方互換性を要求する。それより前の開発中URLは互換性保証の対象外とし、教育上または実装上より良いスキーマへ破壊的に変更できる。
- 現行v1読込: 4.2で実装したv1読込・v2移行は、現行v2 URLの長さや送信データを増やさず、読込側コードだけで完結しているため当面残す。今後の設計を妨げる場合は、正式リリース前であることを明記して削除できる。
- URL長: 新しいURLの長さは現行v2として実際に出力するフィールドだけで決まり、旧版の読込コードには依存しない。冗長な互換用メタデータを共有状態へ追加しない。

### D-033 一次結合結果の教材表示

- 状態: **決定・実装・利用者確認済み**（2026-08-20）
- 分類語: `Ac = v` の結果は教科書に合わせて「不能（解なし）」「唯一解」「不定（解が無数）」と表示し、それぞれ「一次結合では表現できない」「表し方は一意」「表し方は無数」と対応付ける。係数行列と拡大係数行列のrankも併記する。
- 数値表示: 係数と式中の数は有効数字6桁を初期規則とする。表示値への丸めがなければ等号、丸めがあれば近似記号を用いる。分数・根号の自動認識は引き続き扱わない。
- 無数解: D-034により係数の異なる2例を既定表示し、自由係数・零空間基底を用いた一般解だけを折り畳む。
- 一般のターゲット: 選択が2本かつ一次独立な場合だけ、一般の `{}^t[a, b]` に対する2×2逆行列による係数公式を既定で折り畳んで表示する。開閉状態は共有しない。
- 幾何表示: 選択がちょうど2本でターゲットを表現可能な場合だけ、特解の `c₁a₁` と `c₂a₂` を原点からの2辺として描き、対辺を補う。一次従属なら退化した平行四辺形として説明し、3本以上では誤解を避けるため平行四辺形を描かない。

### D-034 一次結合エクスプローラの記号体系と一般解の説明

- 状態: **決定・実装・利用者確認済み**（2026-08-20、4.4表示改訂）
- 統一記号: 通常ベクトルと自動IDを `a₁`〜`a₈` / `a1`〜`a8`、選択集合を `S = {a₁, …}`、列を並べた行列を `A = [a₁ …]`、ターゲットを `v` とする。選択カードと全ベクトルカードはいずれも、そのカードで対象とする列を `A` と定義し、`B` は使わない。
- 一次結合の主式: `v = Ac = (a₁, …, aₙ)[c₁; …; cₙ]` と、同じ係数列の転置横書き `c = {}^t[c₁, …, cₙ]` を併記する。係数例は `c = [c₁; …] = [値; …]`、展開式は記号による一次結合と値を代入した一次結合の2段で示す。
- 転置横書き: 列ベクトルを横に書く場合はMATLAB風の `[a; b]` を使わず、教科書どおり `{}^t[a, b]` とする。スカラー、座標軸、式中の変数も可能な限り数式用フォントのイタリック体で示す。
- 数式スタイル: 一次結合の主式、右端の係数列、直下の `c = {}^t[c₁, …]` は色・数式フォント・文字サイズを統一する。一般ターゲットの定義は `v = {}^t[a, b]` と横書きできるが、逆行列の積の右端は行列積として読みやすい列表示 `\left[ a \\ b \right]` とし、`1 / det(A)` は分子・分母と横線を持つ分数で示す。
- 無数解: 「係数の例1」「係数の例2」を常時表示し、「自由係数を使った一般解」だけを既定で折り畳む。一般解では、自由係数を0として `Ac = v` を解く特解 `c_p`、`An_j = 0` を解く零空間方向 `n_j`、`A(c_p + Σt_jn_j) = v` の関係を短く説明し、最後に各係数を展開した `v = (…)a₁ + …` も表示する。
- レスポンシブ: 0〜8本と長い小数でも、係数列と一次結合の項を安全な単位で折り返す。390px CSSビューポートで一般解を展開しても横スクロールしないことを実測する。

### D-035 フェーズ4の統合完了と一次結合教材シナリオ

- 状態: **決定・実装・利用者確認済み**（2026-08-20、4.5・フェーズ4終了）
- 代表例: 1本で一意、一次従属な2本で不能、一次独立な2本で唯一解、3本で不定となる4状態を、ターゲットを含む共有URLとして授業シナリオの正本へ追加する。
- 自動検証: 各代表例についてベクトル集合のrank・一次独立性、`rank(A)`・`rank([A | v])`・解分類、共有URLの往復、文書内本番URLとの一致を固定する。
- 利用者確認: 4.5に列挙した共有復元、Reset、レスポンシブ配置、アクセシビリティ、PC・タブレット・スマートフォンでの一連の操作は利用者確認済みとする。
- 3D境界: `analyzeLinearCombination`、共有するターゲット、解分類・特解・零空間一般解、教材シナリオ構造は3Dへ再利用する。2D座標への配置・吸着、SVG、平行四辺形、2×2逆行列の一般式は2D固有とする。
- 完了判断: 4.1〜4.5の実装・自動検証・利用者確認・文書棚卸しが揃ったため、フェーズ4を完了し、次はフェーズ5の作業分割を検討する。

### D-036 共有URLのQRコード表示・PNG保存

- 状態: **決定・実装・利用者確認済み**（2026-08-20、フェーズ5前の追加作業4.6）
- 授業目的: 教員が作成した教材状態の共有URLをプロジェクタへQRコードとして表示し、学生がスマートフォンで同じ状態を直接開けるようにする。
- 表示位置: 共有URLダイアログのURL欄の上へ、現在の共有URLと同一内容のQRコードを表示する。
- 生成方法: 共有URLを外部サービスへ送信せず、`qrcode` 1.5.4を使ってブラウザ内で768×768のPNGを生成する。誤り訂正レベルM、余白4モジュール、濃色と白の高コントラストを初期値とする。
- 保存操作: 「QRコードを保存」を「テキストで保存」の左隣へ置き、`linear-algebra-visual-lab-qr-YYYYMMDD-HHmm.png` として保存する。同じPNGデータを画面表示と保存に使う。
- 失敗時: 生成中は保存を無効にする。URLがQRコード容量を超える等の失敗では理由を表示し、URLのコピー、テキスト保存、閉じる操作は維持する。
- レスポンシブ・アクセシビリティ: デスクトップでは4操作を横並び、640px以下では1列にする。QR画像に代替テキスト、生成中・失敗に状態通知を付ける。
- 利用者確認: 公開版でスマートフォン読取、同じ教材状態の復元、PNG保存、PC・スマートフォン表示を確認し、すべて問題なしとした。

### D-037 フェーズ5の3D設計境界と次元切替

- 状態: **決定・5.1実装・利用者確認済み**（2026-08-24）
- 2D/3D切替: Lab上部に「2D座標平面」「3D座標空間」の対等な2択セグメント型タブを置く。オン／オフ型スイッチは用いず、左右矢印・Home・Endキーと狭い画面にも対応する。
- 状態境界: 2Dと3Dの`InitialState`、`CurrentState`、未確定入力、一時UIを別々に保持する。同一閲覧セッション内の切替往復で各状態を維持し、`z = 0`の補完やz成分の破棄を含む暗黙の座標変換は行わない。各次元には専用の既定例を置く。
- 共有URLとReset: 共有URLには現在開いている一方の次元だけを保存し、その次元を初期選択する。別次元はセッション内状態または専用既定例を使う。Resetは現在の次元だけを、その次元の`InitialState`へ戻す。
- 座標系と投影: 数学で一般的な右手座標系を用いる。初期の等角的視点では、x軸は右向き、y軸は左下（手前）から右上（奥）、z軸は鉛直下から上を正方向とする。z軸上向きの正投影を採用し、等角・正面・右・上・全体表示を用意する。
- カメラ共有: 3Dカメラの向き、注視点、拡大率は教材上意味のある共有状態に含める。共有状態v3の具体形と安全上限はD-039で決定・実装した。
- 3D直接編集: 数値入力を先行し、5.6で明示的な「視点操作／ベクトル操作」モードとx・y・z軸拘束ハンドルを試作する。当初の試作方針はD-042で実装し、操作確認後にD-043の画面平行面ドラッグへ置換した。
- 一次結合の幾何表示: 2項は3D空間内でも`c₁a₁`と`c₂a₂`を原点からの2辺とする平行四辺形を使う。3項は原点基準の平行六面体を5.5で試作し、退化形・視認性・4本以上との差を確認して最終採否を決める。

### D-038 固定3D表示と正投影カメラ

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.2）
- 描画基盤: Three.js 0.185.1と型定義0.185.4を採用し、Reactは教材状態とマウント境界だけを管理する。Three.jsのscene、renderer、camera、controls、リソース破棄は`VectorSpace3D`内の描画ランタイムへ分離する。
- 座標と初期視点: 数学座標`(x, y, z)`をThree.jsワールド座標へそのまま割り当て、カメラのupをz正方向とする。初期等角視点はx正側・y負側・z正側から原点を見て、画面上でx正方向が右、y正方向が右上（奥）、z正方向が上に見える配置とする。
- 表示物: xy平面の適応補助格子、正方向に矢じりを持つx・y・z軸、負方向の破線、原点、固定3Dベクトルと矢じり、数式規則に沿う基本文字だけボールド斜体・添え字立体レギュラーのラベルを表示する。
- カメラ操作: `OrthographicCamera`と`OrbitControls`を使い、左ドラッグ／1本指で回転、ホイール／ピンチで拡大縮小、右ドラッグ／2本指ドラッグでパンする。正面・右・上・等角の4視点と全体表示をボタンで提供する。カメラ共有はD-037どおり5.3で追加する。
- 描画負荷: 連続アニメーションループを設けず、操作、プリセット、リサイズ時だけ再描画する。3Dモジュールは3Dタブを初めて開くまで遅延ロードし、以後はタブ往復でもマウントとカメラ状態を維持する。
- 堅牢性: `ResizeObserver`、devicePixelRatio最大2、高さを持つレスポンシブCanvas、WebGL初期化失敗・context lost案内、コンポーネント破棄時のgeometry・material・controls・renderer解放を行う。

### D-039 3D数値編集・解析と共有状態v3

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.3）
- 3D編集: 5.1で決めた専用既定例を使い、2Dとは独立した入力途中状態と詳細タブを持つ。0〜8本のx・y・z数値入力、追加・削除、自動命名、表示順から導く8色、span選択を既存の集合編集へ接続する。未確定入力中は直前の有効値を表示・判定へ使い、その次元のエクスポートを停止する。
- 数学表示: 全ベクトルと選択集合に共通の`analyzeVectorSet`を使い、`rank(A)`、`dim(span(S))`、一次独立・一次従属を2Dと同じ用語・数式規則で表示する。3Dではrank 0を原点、rank 1を直線、rank 2を原点を通る平面、rank 3を3次元座標空間全体と説明する。幾何描画は5.4へ残す。
- 自動範囲: 全ベクトルの最大絶対座標に25%の余白を加え、最低半径5を保つ。適応格子と矢じり・ラベル寸法、正投影視野、near/farを範囲に追従させる。ベクトル編集時は現在のカメラ方向・注視点・拡大率を維持して再構築し、「全体を表示」で原点と全ベクトルへ戻す。
- 共有状態v3: `visualization.camera`へ、注視点からカメラへの単位`direction`、`target`、画面上方向の単位`up`、正投影の`zoom`を保存する。方向とupは互いに非平行、target各成分は絶対値10,000,000以下、zoomは0.05〜100として厳密に検証する。2Dではcameraを`null`とする。
- 互換とReset: v1は一次結合を非表示・未配置、v2はcameraを`null`としてv3へ移行し、既定等角視点で開く。新規URLはv3を生成する。3D共有URLはカメラを含むInitialStateとなり、Resetで共有時のベクトルとカメラを復元する。QRコードとテキスト保存は同じURLを再利用する。
- 確認結果: ローカル実画面で成分変更によるrank 3→2、追加・削除、上視点のv3 URL生成、別タブの完全復元、別視点からResetした際の共有視点復元を確認した。確認例のURL長は548文字で、390px幅でも主要操作が1列に収まることを確認した。

### D-040 3Dで生成する空間の幾何表現

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.4）
- 共通表現: D-029と同じくベクトル8色と競合しない中立的な灰色系を使う。座標空間内の日本語ラベル、カードの説明、線種、格子を併用し、色だけに依存しない。`visualization.showSpan`で幾何表示だけを切り替え、選択集合と解析は維持する。
- rank 0・1: rank 0は原点を半透明球と外側ワイヤーフレームで強調する。rank 1は最長の非零選択ベクトルから方向を得て、現在の表示立方体の境界まで延びる円柱状の太い破線とする。
- rank 2: 選択ベクトルを正規化して最大の外積を持つ組から安定した正規直交基底を作り、原点を通る格子なし・両面の半透明平面を描く。有限平面は表示立方体の断面を覆う余裕を持たせ、真横では線に見えてもラベルとカードで平面だと分かるようにする。
- rank 3: 旧MATLAB版の表示を参照し、表示範囲内を薄い半透明グレーの立方体で満たす。span専用格子は置かず、3D座標軸全体の範囲を示す実線の外枠Boxと座標空間内ラベルを表示する。結果カードのアイコンも、目のない半透明3面と奥側の破線を持つ立方体とする。
- 重なり: 幾何表示中は未選択ベクトルとラベルを薄くし、選択ベクトルはspan形状より前面で判別できる描画順・深度設定とする。形状の透明材質は深度を書き込まず、軸とラベルも維持する。
- 再評価条件: 公開版でrank 0〜3、3本以上の一次従属、平面の真横、極端な座標、PC・スマートフォンの回転操作を確認し、格子密度、透明度、破線の太さ、ラベル位置が過密または不明瞭なら調整する。

### D-041 3D一次結合エクスプローラ

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.5）
- ターゲット操作: 3Dターゲット`v`は当初x・y・zの数値入力と消去に限定したが、D-046で原点を通る画面平行面という奥行き規則を定め、Canvasの短いクリック・タップ配置を追加した。未確定入力は直前の有効値を維持し、該当次元のエクスポートを停止する。
- 数学表示: 2Dで確立した`analyzeLinearCombination`と結果カードを再利用し、不能・唯一解・不定、係数例、異なる第2例、自由係数を使った一般解を同じ記号・用語で示す。3D固有の共有スキーマ追加は不要で、既存v3のターゲットと表示フラグを使う。
- 幾何表示: 選択2本では第1係数例の`c₁a₁`と`c₂a₂`を原点からの辺とする平行四辺形、選択3本では`c₁a₁`、`c₂a₂`、`c₃a₃`を辺とする平行六面体を描く。頭尾接続の移動経路にはしない。不定の第2例と一般解は数式表示だけとし、4本以上は単一の立体に対応させず数学カードだけにする。
- 退化形: 元の選択集合だけでなく、表示に使う係数を乗じた辺のrankで判定する。3辺のうち係数が0、または寄与ベクトルが一次従属なら平行六面体を退化形として説明する。
- ラベル配置: 通常ベクトル名と係数付き寄与ベクトル名は、CSS2Dの基準点を矢先へ固定し、ラベル矩形全体が矢じりの外側になる4方向の画面座標オフセットを使う。同じ終点になりやすい`a_i`と`c_i a_i`は反対方向へ配置する。背景は完全には削除せず、通常ラベル66%、係数付きラベル62%の白系半透明背景、薄い縁取りと文字影を使い、図形の視認性と文字の可読性を両立する。
- 表示範囲と共有: ターゲットと平行四辺形・平行六面体の全頂点を自動表示範囲へ含める。共有URL、QRコード、Resetは既存v3により一次結合モード、ターゲット、選択集合、ベクトル、3Dカメラを復元する。
- 確認結果: 利用者が3D一次結合の表示と操作、ラベル調整後の視認性を確認し、問題なしとして5.5を完了した。3本の平行六面体は現行の試行表示を維持し、異なる授業例や端末で教育的理解を助けない問題が見つかった場合に簡略化または見送りを再評価する。

### D-042 3D軸拘束ベクトル編集の初回試作

- 状態: **試作・利用者評価後にD-043で置換**（2026-08-24、作業単位5.6）
- モード分離: 数値入力は常時利用可能とし、Canvas操作だけを「視点を操作」と「ベクトルを操作」で明示的に切り替える。視点操作では従来の回転・ズーム・パン、ベクトル操作では選択ベクトルのハンドルだけをドラッグ対象とし、背景ドラッグでカメラを動かさない。
- 軸拘束: 選択ベクトルの矢先へx・y・zの双方向ハンドルを置く。ドラッグ量を現在カメラへ投影した該当軸へ射影して1成分だけを変更し、他の2成分を維持する。自由な画面向き平面ドラッグは行わない。軸の画面上投影長が短い場合は編集せず、正面・右・上・等角の視点プリセット変更を案内する。
- 入力手段: Pointer Eventsでマウス、タッチ、ペンを共通処理し、ドラッグ中は変更後の矢印と成分値をプレビューする。タッチ対象は見た目の端点より広くする。各成分の`−0.1`・`＋0.1`ボタンと既存数値入力をキーボード代替として維持する。
- 状態境界: 確定値だけを既存の3Dベクトル状態へ反映し、span、rank、一次結合、自動範囲、数値入力を再計算する。操作モードと選択ベクトルは教材の数学状態ではないため共有URLへ追加しない。Resetでは視点操作とInitialStateの先頭ベクトル選択へ戻す。
- 安全性: ドラッグと微調整は共有状態と同じ成分絶対値`1,000,000`上限を使う。ドラッグ確定値は不要な浮動小数ノイズを避けるため小数6桁へ丸める。
- 評価結果: 利用者確認で「視点を操作」「ベクトルを操作」を切り替える負担が明確になった。機能を軸拘束の精密操作として残すより、矢先と背景の開始位置で自動分離するほうが教材の試行錯誤に合うと判断し、モードUI、軸ハンドル、±0.1専用パネルをD-043で削除した。

### D-043 3D画面平行面ベクトル編集の統一操作

- 状態: **決定・利用者確認済み**（2026-08-24、作業単位5.6改修）
- 自動分離: 操作モードを設けない。通常ベクトルの矢先から始めた左ボタン・1本指・ペンのドラッグはベクトル移動、矢先以外から始めたドラッグはOrbitControlsの視点回転とする。ホイール・ピンチのズーム、右ドラッグ・2本指ドラッグのパン、視点プリセットを維持する。
- 操作面: pointerdown時の矢先を通り、現在のカメラ視線方向へ垂直な平面を固定する。正投影カメラから現在ポインターへのrayとこの平面の交点差を初期座標へ加え、画面の上下・左右に対応する2自由度で移動する。一般の斜め視点では3成分が同時に変化し、上・正面・右のプリセットでは対応する座標平面方向を調整できる。
- 所有範囲: pointerdown時にベクトルとカメラの所有者を確定し、途中で変更しない。ベクトル移動中はカメラを無効化して表示範囲を固定し、同時に加わった別ポインターをカメラ操作へ渡さない。矢先のヒット半径はCSS画面距離でマウス15px、ペン19px、タッチ24pxとし、候補が重なる場合は画面距離、同距離ではカメラに近い矢先を選ぶ。
- 視覚フィードバック: 各矢先へ常時小さな色付き球とワイヤーフレーム球の二重マーカーを置く。ドラッグ中は元の矢印を薄く残し、画面の上下・左右に対応する一時的な破線ガイド、移動線、新しい矢印、現在の3成分を示す。確定後に通常描画へ戻す。
- 状態と安全性: 自由移動の3成分を小数6桁へ丸め、絶対値`1,000,000`以内へ収めて既存3D状態へ反映する。D-044で吸着した場合は、その丸め済み自由移動座標から従属関係を保つ直交射影値へ置き換える。その後に数値入力、span、rank、一次結合、自動範囲を再計算する。厳密な設定とキーボード代替には既存数値入力を使う。共有URLへは確定座標だけを保存し、操作面、選択、プレビューを追加しない。
- 評価結果と再評価条件: 公開版の操作確認で、モード切替なしの矢先移動と背景回転が「非常に操作しやすい」と評価され、本採用とした。今後、端末差による矢先の見つけにくさ、背景回転との誤操作、斜め視点の理解に問題が見つかった場合はヒット領域とガイドを再調整する。

### D-044 3Dの平行・同一平面スナップ

- 状態: **決定・利用者確認済み**（2026-08-24、作業単位5.6追加）
- 教材目的: 3Dで3本を同一平面上へ手操作だけで厳密に置くことは事実上不可能であるため、D-009のrank許容誤差を緩めず、直接操作中だけ従属関係を作る補助を加える。
- 候補と優先順位: ドラッグ対象以外の表示中の通常ベクトルを候補にする。各非零ベクトルが張る最寄り直線への平行スナップを最優先し、候補がない場合だけ、D-009の相対許容誤差で一次独立な各2本が張る最寄り平面への同一平面スナップを試す。span選択状態には依存せず、零ベクトルと一次従属な2本の組は除外する。
- 距離と座標: 2DのD-019と同じく現在の正投影表示幅の`1%`を世界座標の距離へ換算し、直線または原点を通る平面へ直交射影する。ズーム後も画面上の吸着幅をほぼ一定にし、ヒステリシスは設けない。安全上限が必要な場合は全成分を同率で縮小して従属関係を保つ。
- フィードバック: 吸着中は画面平行ガイドと移動線を緑系へ変え、矢先へ画面幅相対のリングを置く。状態表示には対象のベクトル名と「平行」または「同一平面上」を示す。
- 状態境界: 数値入力と一次結合ターゲットへは適用しない。D-009の`1e-10`、共有状態v3、Resetを変更せず、共有URLには吸着後に確定した通常座標だけを保存する。
- 評価結果と再評価条件: 公開版の操作確認でスナップは良好と評価され、採用した。今後、スマートフォン、複数候補、ズーム前後で問題が見つかった場合は表示幅に対する比率をD-009とは独立に調整する。

### D-045 3Dドラッグ中のspanリアルタイム表示

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.6追加）
- 教材目的: ベクトルが平面へ吸着した瞬間と、生成する空間が3次元全体から平面へ変わる瞬間を同時に示し、一次従属への変化をポインターを離す前に理解できるようにする。
- 更新対象: span表示がオンでドラッグ対象が選択集合`S`に含まれる場合だけ、D-044適用後のプレビュー座標からD-009のrankを再計算する。灰色のspan形状、グラフ内ラベル、操作中の状態表示を原点・直線・平面・3次元全体の間で更新する。
- 状態境界: 右側の解析カード、数値入力、一次結合、共有状態v3はドラッグ中に変更せず、終了時だけ確定する。対象が`S`に含まれない場合とspan表示オフではプレビューを作らない。キャンセル時は元のspan表示へ戻す。
- 描画負荷: 確定表示とドラッグ用spanを別グループにする。rank 0と3は方向に依存しないため同じrank中は形状を再生成せず、rank 1と2は向きの変化を反映するため更新する。
- 再評価条件: PCとスマートフォンでrank 3と2の往復、rank 1、連続ドラッグ時の滑らかさ、ラベルの残留がないことを確認し、必要なら更新頻度を調整する。

### D-046 3Dターゲットのワンタップ配置

- 状態: **決定・利用者確認済み**（2026-08-24、作業単位5.6追加）
- 背景: D-041では3D画面の1点から奥行きを決められないため数値入力へ限定したが、後続計画にもCanvas配置がなく、2Dと同様に授業中の素早いターゲット作成が必要と確認されたため方針を置換する。
- 奥行き規則: タップ位置から出る正投影rayと、原点を通り現在のカメラ視線へ垂直な面との交点をターゲット先端にする。正面・右・上では対応する座標平面、等角・任意視点ではその視点に正対する原点通過面となる。配置後は数値入力で3成分を厳密に調整できる。
- 操作分離: 一次結合モード中のCanvas背景で、開始から終了まで8 CSS px以内の短い主ポインター操作だけを配置とする。8px超はOrbitControlsの視点回転、2点操作はズーム・パン、通常ベクトル矢先はD-043の直接編集を優先する。既存ターゲットがあっても短い背景タップで再配置できる。
- 状態と案内: 交点座標は小数6桁・絶対値`1,000,000`以内へ正規化し、既存のターゲット状態、3成分入力、解析、幾何表示、共有URL、Resetへ接続する。一次結合モードでは十字カーソル、操作凡例、ヘルプ、配置後の3成分状態を表示する。共有スキーマは変更しない。
- 再評価条件: PCとスマートフォンで短いタップ、わずかな手ぶれ、視点ドラッグ、2点操作、通常ベクトル矢先との近接、各視点プリセットの配置面を確認し、必要なら8px閾値を調整する。

### D-047 3Dターゲット矢先ドラッグと一次結合幾何のリアルタイム更新

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.6追加）
- 操作: 配置済みターゲット`v`の青い矢先をマウス15px、ペン19px、タッチ24pxの画面距離で選択し、開始時の矢先を通る画面平行面内で移動する。ターゲット矢先を背景配置と通常ベクトルより先に判定し、ドラッグ中はカメラを固定する。ターゲットにはD-044の平行・同一平面スナップを適用しない。
- 幾何プレビュー: ドラッグ中の3成分から既存の一次結合解析を毎フレーム再計算する。表現可能な2本・3本なら第1係数例の平行四辺形・平行六面体、係数倍矢印、ラベル、ターゲットを専用グループへ再生成する。表現不能または4本以上では単一の係数幾何を表示せず、ターゲットだけを追従させる。
- 状態境界と負荷: 右側の3成分入力、解分類・係数カード、共有状態v3は終了時に一度だけ確定する。キャンセル時は元のターゲットと係数幾何へ戻す。解析・描画コードは3D遅延チャンク内へ保ち、2D初期バンドルを増やさない。
- 再評価条件: PCとスマートフォンで矢先のつかみやすさ、平行六面体の追従性、表現可能・不能の境界、通常ベクトルや背景操作との近接、連続操作の滑らかさを確認する。

### D-048 3D教材シナリオとCanvas代替導線

- 状態: **決定・実装・利用者確認済み**（2026-08-24、作業単位5.7）
- 代表例: 3Dの空集合、零ベクトル、1本が生成する直線、同一平面上の3本、一次独立な3本による唯一解、平行な2本による不能、同一平面上の3本による不定の7例を正本化する。これらでrank 0〜3、一次独立・一次従属、一次結合の不能・唯一解・不定を網羅する。
- 共有と検証: 各例はベクトル、span選択、幾何表示、ターゲット、3Dカメラを含む共有状態v3とし、期待するrank・一次独立性・2つのrank・解分類、本番共有URLとの一致を自動検証する。教員がURLまたはQRコードを配布し、学生が同じ状態を復元してResetできる授業手順を文書化する。
- 視点と操作: 正面・右・上・等角のプリセットを例に含め、視点変更と数学的状態変更を区別する。直接編集、吸着、spanのリアルタイム変化、ターゲット配置・ドラッグ、平行四辺形・平行六面体を一連の観察手順へ含める。
- 代替導線: Canvasは補助的な可視化と位置付け、関連付けた代替説明で、数値入力と解析カードから座標、rank、生成する空間、一次独立性、一次結合の結果へ到達できることを示す。WebGL初期化失敗・context lost時も数値入力、共有URL、Resetが利用可能であることをエラー内に明記する。
- 確認ゲート: 公開版のPCと実機スマートフォンで代表例10〜16、縦横回転、Canvas内外のジェスチャー分離、URL・QR復元、描画要素が多い唯一解例の追従性を確認する。主要ブラウザの実態は学内端末情報と合わせて5.8で棚卸しする。

### D-049 フェーズ5の統合完了と3D責務境界

- 状態: **決定・実装・利用者確認・棚卸し済み**（2026-08-24、作業単位5.8・フェーズ5終了）
- 完了判断: 5.1〜5.7の実装と自動検証、公開版のPC・スマートフォン確認、5.8の2D回帰確認と文書・依存関係棚卸しが揃った。3Dの代表例を操作・共有・復元できるというフェーズ5の終了条件を満たす。
- 責務境界: `src/domain`は次元共通のrank・一次独立性・一次結合、`src/sharing`は共有状態v3・カメラ・URL・QR、`src/state`は次元別InitialState／CurrentStateと集合編集・吸着、`src/visualization`は2D SVGと遅延ロードするThree.js 3D描画・直接操作、`src/app`は次元別状態接続・入力・解析カード・レスポンシブUI、`src/teaching`は2D・3D代表例の正本を担当する。描画オブジェクトを数学状態の正本にしない。
- 置換・不採用: D-042の明示的な操作モード・軸拘束ハンドルはD-043の矢先／背景による自動分離へ置換した。D-041の3Dターゲット数値入力限定はD-046の画面平行面ワンタップ配置へ置換した。rank 2・3のspan専用格子は過密表示になるため採用せず、格子なし平面・半透明空間と外枠Boxを使う。4本以上の一次結合は一つの幾何形状へ一意に対応させず代数表示に限定する。
- 再評価条件: D-009の数学許容誤差と操作専用スナップ距離は分離を維持する。Three.js遅延チャンクは約577kBで500kB警告が続くため、初回3D読込が授業を妨げる場合に分割を再検討する。学内端末・主要ブラウザ、WebGL/GPU、共有URL長、nginx、数式描画、極端な座標、各ジェスチャー閾値は実運用で問題が見つかった場合に再評価する。
- 次の段階: フェーズ5を完了し、一部を4.6・6.1で前倒し済みのフェーズ6「共有と配布の仕上げ」について、6.2〜6.7の作業単位案を作成した。これは新しい決定ではなく利用者承認待ちであり、承認後に6.2から進める。

### D-050 共有URL・QRコードの授業用上限

- 状態: **決定・実装・自動検証済み**（2026-08-24、作業単位6.2）
- 上限の分離: Base64URL状態の8192文字は不正・過大な受信データをデコード前に拒否する防御上限として維持する。アプリが授業用に生成する完全URLは、origin、配布パス、`?state=`を含め2048文字以内とする。
- 実測: 6.2時点の文書に保存済みの代表例16件は旧版を含め268〜612文字、現行v3で新規生成すると286〜612文字だった。6.7で配布文書の全16件をv3へ更新した。画面から到達可能な厳しい境界例として、8本の3Dベクトル、全span選択、最大絶対座標、3Dカメラ、ターゲットを組み合わせても本番URLは1309文字だった。
- QR範囲: 2048文字以内をURL欄・コピー・テキスト保存・QRコードの共通運用範囲とする。1309文字の境界URLを誤り訂正M、768×768 PNGへ変換できることを自動検証した。2048文字を超えるURLはQR生成へ進めず、上限を含む理由を表示する。
- 圧縮: 現行の画面到達可能状態には739文字以上の余裕があるため、初版候補では圧縮や外部短縮サービスを導入しない。共有項目の追加、公開パスの大幅な延長、境界テストの超過が起きた場合は、保証済みv3を維持した新版スキーマまたは配布方式として再検討する。
- エラー: 空、重複、不正Base64URL、不正JSON、未知バージョン、次元不整合、座標・本数・状態長超過は分類済みエラーとし、読込時は既定2D例へ安全にフォールバックする。ローカルブラウザで不正URLの案内・既定例と通常のQRダイアログを確認した。

### D-051 正式リリース後の共有URL互換契約

- 状態: **利用者承認・実装・自動検証済み**（2026-08-24、作業単位6.3）
- 最初の保証版: 現行v3を最初の正式な保証対象候補とし、リリース識別だけを目的とするv4は作らない。v3は2D/3D、ベクトル、span選択、幾何表示、一次結合ターゲット、3Dカメラを保持する。
- 保証期間: フェーズ7で正式リリースを承認した時点から、本アプリを提供している期間は固定年数を設けず、有効なv3 URLの読込を維持する。v3に生成時期がないため、現在までに生成した有効なv3も同じ保証対象になる。
- 将来版: 新規URLは常に現行版だけを生成する。v4以降を追加しても、保証対象の旧版を厳密に検証して現行`ShareState`へ移行するデコーダーを維持し、互換用フィールドを新規URLへ重複させない。未知の将来版は既定例へフォールバックして理由を示す。
- 開発版: v1・v2は正式保証の対象外だが、現在の厳密な読込とv3への移行は当面残す。削除する必要が生じた場合は新規URL長とは独立に再評価する。
- fixture: `tests/fixtures/share-url-v3.json`へ、3Dカメラと一次結合を含む固定v3 URLと期待状態を保存する。将来版ではこれを変更せず、新版fixtureと旧版からの移行テストを追加する。
- 終了条件: アプリ自体の終了または互換不能な全面刷新だけは、事前告知と利用可能な移行手段を別途決定したうえで扱う。6.3完了だけでは正式リリースを宣言せず、保証開始はフェーズ7の明示承認時とする。

### D-052 GitHub Pagesを正規配布先とする可搬配布方針

- 状態: **決定・実装・自動検証済み**（2026-08-24、作業単位6.4）
- 正規配布先: 初版は既に継続運用できているGitHub Pagesを正規URLとする。mainへのpush、Actionsのテスト・ビルド、検証済み`dist/`のデプロイというD-025・D-030の流れを維持する。
- 成果物検証: `scripts/verify-static-build.mjs`と`pnpm verify:dist`で、`dist/index.html`、期待するVite base path、参照するJavaScript・CSS等の存在を検証する。Pages Actionsにもartifact upload前の検証ステップを追加する。
- 可搬性: Vite公式の`base`に合わせ、ルート`/`、任意サブパス`/class/tools/vector/`、Pagesサブパス`/linear-algebra-visual-lab/`で実ビルドと4アセットの整合を確認した。共有URLは実行時のoriginとパスを使うため、各配布先自身を指す。
- nginx: `docs/NGINX.md`にルート・サブパス配置、MIME type、HTTPS、gzip、HTMLとハッシュ付きアセットのキャッシュ、2048文字のリクエスト行、更新・ロールバックを記録する。現在はクライアント側ルートがないためSPA fallbackを設定しない。
- 完了境界: nginx実サーバーへの配置は初版候補の必須条件にしない。学内で採用する場合だけ、管理者のTLS・アクセス制御・プロキシ・WAFを含む実環境で手順を実施する。GitHub Pagesと併用する場合は学生へ案内する正規URLを一つ決める。

### D-053 能力別の対応環境と授業前チェック

- 状態: **決定・文書化・自動検証・現有環境確認済み**（2026-08-24、作業単位6.5）
- 対応方針: 特定ブラウザの固定版番号ではなく、基本教材、3D、Clipboard、ファイル保存、タッチ操作に必要な能力と代替導線を分ける。授業時点でサポート中のChrome、Edge、Firefox、Safari系を推奨対象とするが、推奨と実機確認済みを混同しない。
- 基本保証: HTTPS、JavaScript、ES modules、SVG、フォームが使えれば2D、数値入力、解析、共有URL、Resetを利用できる。3D幾何はCanvas・WebGLを使うが、利用できない場合も数値入力、解析、共有、Resetで授業を継続する。
- 操作代替: Clipboard API失敗時は選択済みURLの手動コピー、ダウンロード制限時は画面QR・URL欄、タッチ競合時は数値入力・視点プリセットを使う。
- 確認実績: GitHub PagesのChromium系デスクトップ、390px・820px相当のレスポンシブ表示、利用者の実機スマートフォンによる2D/3Dジェスチャー・QR読取・共有復元を基準点とする。6.5では公開版の通常QRダイアログと固定v3の3D・rank 3・ターゲット・カメラ復元を再確認した。
- 変動環境: 学内管理端末の具体版、WebGL制限、教室ネットワーク、物理的なプロジェクター距離はリポジトリだけでは固定できないため、フェーズ6の実装を止めず、`docs/CLASSROOM_DISTRIBUTION.md`の授業前チェックへ記録する。
- 運用資料: 教員の配布、学生の確認、Reset、QR・ネットワーク・不正URL・Clipboard・保存・WebGL・ジェスチャーの復旧、問い合わせ時の記録項目を同文書へ統合する。

### D-054 初版候補の版、ライセンス、公開案内

- 状態: **利用者承認・実装・文書化済み**（2026-08-24、作業単位6.6）
- 版: 初版候補を`1.0.0-rc.1`、フェーズ7の正式承認後を`1.0.0`とする。版の正本は`package.json`とし、アプリのヘッダー・フッター、変更履歴、配布文書へ表示する。初版候補ではGit tagとGitHub Releaseを作成しない。
- 本体ライセンス: MIT License。著作権表示は`Copyright (c) 2026 Daichi Kitamura`とする。
- 旧教材: 旧MATLAB教材`plot2d_plot3d`は着想・表示仕様の参照元として明記し、本リポジトリの実装コードは新規実装としてMIT Licenseの対象にする。
- 第三者ライセンス: 公開アプリの直接依存とブラウザバンドルに入る実行時依存の著作権・ライセンスを`THIRD_PARTY_NOTICES.md`へ記録し、版の更新時に`pnpm-lock.yaml`と照合する。
- 公開案内: アプリのフッターから開く概要と`USAGE_AND_PRIVACY.md`の詳細を併用する。外部APIへ数学状態・QRを送らないこと、アプリ独自のアカウント・Cookie・解析・サーバー保存がないこと、共有状態がURLへ入ること、GitHub Pages側の通常のアクセス情報、個人情報を含めない注意を公開する。

### D-055 フェーズ6統合確認とv3教材リンク統一

- 状態: **技術確認・棚卸し・利用者確認済み**（2026-08-24、作業単位6.7）
- 初版候補: `1.0.0-rc.1`について34テストファイル・264テスト、型検査、3種類のbase pathビルド、公開版の版・利用案内・代表例16件・8本境界・QR・Resetを確認した。結果と証拠を`PHASE6_ACCEPTANCE.md`へ集約する。
- 教材リンク: 棚卸しで、2D代表例9件の文書URLだけが保証対象外の旧v2で残っていることを検出した。数学状態は正しくv3へ移行できていたが、初版配布資料は現行v3で統一し、文書URLと正本から生成したURLの完全一致を自動検証する。
- 文書整合: 6.2〜6.7が承認待ち案とされていたロードマップ表現、URL長・互換期間が未決定だった仕様記述、D-050の再評価時期を現状へ更新した。全Markdownの相対リンク先を自動検証する。
- 完了境界: 利用者が公開版`1.0.0-rc.1`を確認し、フェーズ6完了とフェーズ7への引渡しを承認した。正式リリース、v3保証開始、`1.0.0`、Git tag、GitHub Releaseはフェーズ7まで行わない。

### D-056 フェーズ7の延期と複数Lab・共有状態の境界

- 状態: **利用者承認・仕様化済み**（2026-08-24、作業単位8.1）
- フェーズ順: 授業実施が先であるため、フェーズ7「初版評価と棚卸し」は完了扱いにせず授業で評価できる時期まで延期する。正式リリース、`1.0.0`、Git tag、GitHub Release、D-051のv3互換保証開始も延期し、正式リリース前の候補版という位置付けを維持する。現時点の版番号は`1.0.0-rc.1`とし、追加開発に伴う次のRC番号は別の作業単位で決める。
- 拡張対象: フェーズ8を「複数Lab基盤と基底・次元Lab」とし、現行教材を「ベクトル空間Lab」`vector-space`、最初の追加教材を「基底・次元Lab」`basis-dimension`とする。線形写像以降は、一つの巨大なフェーズ8へ含めず、教育目的と受入条件を個別承認する後続フェーズとする。
- ナビゲーション: 共通ヘッダーにPC・タッチ・キーボードで利用できるLabメニューを置き、将来すべてのLabを横一列のタブへ並べない。現行2D/3Dタブはベクトル空間Lab内に維持する。状態なしURLは当面ベクトル空間Labを開き、共有URLは対象Labへ直接到達させる。独立した教材一覧トップ画面はフェーズ8の必須範囲に含めない。
- 状態所有: 各Labが`DefaultState`、`InitialState`、`CurrentState`、一時UI状態を所有する。同一セッション中にLabを切り替えても他Labの`CurrentState`を変更せず、共有URLやブラウザ保存領域へ他Labの隠れた状態を保存しない。
- 共有とReset: 共有URL、QR、コピー、テキスト保存は現在Labの教材状態だけを扱う。Resetも現在LabだけをそのLabの`InitialState`へ戻す。ベクトル空間Lab内ではD-024・D-037の現在次元だけをResetする規則を維持する。
- スキーマ版: 共有状態は`lab`と`v`の組で判別するLab別の直和型とし、対応するLab固有スキーマで厳密に検証する。現行`vector-space` v3は変更せず、`basis-dimension`は独立したv1から開始する。新Lab追加だけを理由に現行状態をv4へ上げず、未知のLab・版・組合せは安全に拒否する。
- 実装境界: 8.2では共通シェルと現行ベクトル空間Labの責務分離だけを行い、基底・次元機能を同時に追加しない。現行v3 fixture、代表例、Reset、QR、2D/3D操作を回帰条件とする。基底記号と座標記法は、8.3の学生向けUI実装前に教科書と`math-writing-rules.txt`へ合わせて確定する。

## 3. 決定後も残る確認事項

- 学内nginxを実際に採用する場合に、2048文字の完全URL、TLS、プロキシ・WAFを含む実経路で確認できるか。
- 学内で実際に管理されている端末・ブラウザの具体版と、各授業前の教室ネットワーク・投影距離。
- D-009 の相対許容誤差と、許容誤差付近を学生へどう説明するか。
- GitHub Pagesに加えて学内nginxへも配置するか、およびnginxのリクエスト行上限。
- D-012 の基準表示範囲と、今後追加する教材例に対する見やすさ。
- D-013・D-014 の採用値について、異なる端末で問題が見つかった場合の再調整。
- D-015 の数式描画手段と、`math-writing-rules.txt` に今後追加される表記規則。
- D-016 の余白率、目盛密度、入力確定方式、OS・ブラウザ間の数式フォント差。
- D-017 のホイール・ピンチ感度、倍率上下限、パン操作、原点が表示外にある場合の軸・目盛表示。
- D-018〜D-021 は現在の操作感と表示で利用者確認済み。異なる実端末、教材例、内容追加で問題が見つかった場合に再評価する。
- D-022〜D-024 は利用者確認済み。公開先固有のClipboard API、URL長、ダウンロード制約は実運用環境で再確認する。
- D-026はデプロイ先で利用者確認済みである。
- D-027の入力案内、キーボード操作、狭い画面、代表例は利用者確認済みである。
- D-028のモバイルジェスチャー修正は実端末で利用者確認済みである。
- D-029の灰色系span幾何表示は利用者確認済みである。
- D-031の4.1〜4.5は実装・自動検証・利用者確認・統合棚卸し済みであり、フェーズ4を完了した。
- D-032により正式リリース前の共有URLは後方互換性を保証しない。現行v1読込はURL長に影響しないため残しているが、今後のスキーマ設計を制約しない。
- D-033・D-034の有効数字6桁、等号・近似記号、分類語、2本だけの平行四辺形、数式表記、一般解は4.4で利用者確認済みである。
- D-036のQRコードは公開版でのスマートフォン読取、PNG保存、PC・スマートフォン表示まで利用者確認済みである。

これらは既存の決定を覆すものではなく、それぞれの実装フェーズで試作または運用環境を確認して具体化する。
