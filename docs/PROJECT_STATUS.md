# プロジェクト状況

最終更新: 2026-08-19

## 現在地

- 完了したフェーズ: 0「要件と進め方の確立」、1「状態・数学ロジックの基礎」
- 完了した作業単位: 0.1、0.2、1.1、1.2、1.3「共有状態の最小版を作る」
- 次の作業単位: 2.1「固定例の 2D 表示」
- 配布・公開: 未実施。ローカルファイルの編集のみを行う
- Git 操作: コミット、プッシュともに未実施

## 決定済み・試行採用の方針

- D-001〜D-008 は 2026-08-19 に提案どおり承認された。
- D-009 の相対許容誤差 `1e-10` は利用者確認済みの試行採用とし、2D の操作テスト後に再評価する。
- D-010 の共有状態 v1 と安全上限は利用者確認済みの試行採用とした。
- D-011 として、GitHub Pages または学内 nginx を公開候補として記録した。
- 初版は TypeScript、Vite、React の完全クライアントサイド静的アプリとする。
- 共有状態は検証済みの UTF-8 JSON をパディングなしの Base64URL 形式で格納する。

## 作業単位 1.3 の変更

- 共有状態 v1 に `v`、`lab`、`dim`、`vectors`、`spanSelection`、`visualization.showSpan` を定義した。
- 未知のフィールド、次元不一致、ID・表示名・座標の不正、ID 重複、存在しない span 対象を拒否する。
- ベクトル 8 本、ID 32 文字、表示名 40 文字、座標絶対値 `1,000,000`、エンコード文字列 8192 文字を安全上限とした。
- 日本語を含む状態を UTF-8 JSON と Base64URL の間で決定的に往復できるようにした。
- デコードは例外を外へ漏らさず、成功または分類済みエラーを返す。
- 壊れた Base64URL、UTF-8、JSON、未対応バージョン、過大入力を区別する。
- 壊れた Unicode 表示名を拒否し、`-0` を `0` へ正規化する。
- UI、描画、URL クエリーパラメーターへの接続は変更していない。

## 検証結果

- 自動テスト: 3 ファイル、46 テスト成功
- 数学テスト: rank、span の次元、一次独立・一次従属の代表例と境界例を確認
- 共有テスト: 2D/3D、日本語表示名、決定的エンコード、往復、厳密なスキーマ、安全上限を確認
- 不正入力テスト: Base64URL、UTF-8、JSON、バージョン、次元、座標、ID、span 選択、未知フィールドを確認
- TypeScript の型検査: 成功
- 静的ビルド: 成功

## 次に行うこと

フェーズ 2 の最初の作業単位 2.1 に限定し、固定例を使用する 2D 教材画面を作る。

- 共有状態と同じ構造を持つ既定の 2D 例を用意する。
- 状態から SVG の表示範囲、座標軸、目盛線を構築する。
- 複数ベクトルを、色だけに依存しない矢印とラベルで表示する。
- 既存の数学ロジックを接続し、rank、span の次元、一次独立・一次従属を日本語主体で表示する。
- この作業単位では数値入力、ドラッグ、span の直線・平面、共有ボタンを追加しない。

## 既知のリスクと後続確認

- D-009 の相対許容誤差は、2D の直接操作と操作感を確認した後に再評価する。
- D-010 の 8192 文字は防御上限であり、学内サーバー、LMS、スライド等で保証される URL 長ではない。
- GitHub Pages のプロジェクトサイトではリポジトリ名のサブパスに対応する必要がある。
- nginx の既定構成では一つのリクエスト行が 8 KiB を超えられないため、共有 URL の実用上限は防御上限より短くなる可能性がある。
- URL クエリーパラメーターへの接続、不正 URL の画面表示、Reset はフェーズ 2 の後続作業で実装する。
- 3D カメラ状態を共有対象へ含めるかは 3D 実装時に判断する。
