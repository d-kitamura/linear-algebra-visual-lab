# ベクトル空間可視化 Web 教材アプリ 実装仕様書

## 0. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | ベクトル空間可視化 Web 教材アプリ 実装仕様書 |
| ファイル名 | `SPEC.md` |
| 文書バージョン | 0.45 |
| ステータス | 5.1〜5.5利用者確認済み。作業単位5.6「3D直接編集とジェスチャー競合の試作」実装済み・利用者確認待ち |
| 作成日 | 2026-08-19 |
| 主な根拠 | ChatGPT 会話「教材設計方針」、添付された授業内容、旧 MATLAB 教材 |
| 旧教材 | <https://github.com/d-kitamura/plot2d_plot3d> |

### 0.1 要件ステータス

この文書では、会話中の合意と提案を混同しないため、各要件を次の区分で扱う。

| 区分 | 意味 | 実装上の扱い |
|---|---|---|
| **決定済み** | ユーザーが明示した、または明示的に重要性へ同意した事項 | 原則として実装対象とする |
| **授業対応要件** | 添付授業内容に含まれる学習項目。アプリへの具体的な実装方法は未決定 | 対応可能な設計にするが、承認前に機能を作り込まない |
| **未決定（提案あり）** | 会話中に案として提示されたが、ユーザーによる明示的な採用確認がない事項 | 実装前に確認する |
| **未決定** | 会話で決まっていない事項 | Codex が独断で確定しない |
| **派生制約** | 決定済み要件を安全・確実に成立させるために必要な最低限の技術制約 | 仕様を拡張しない範囲で守る |

### 0.2 仕様解釈の原則

1. 「決定済み」を「未決定（提案あり）」より優先する。
2. 会話中のアシスタントによる推薦表現（「〜がよい」「〜を推す」等）は、ユーザーが明示的に同意していない限り決定事項にしない。
3. 本書に記載のない教材機能、画面、ライブラリ、保存方式、認証機能を追加しない。
4. 未決定事項が実装の分岐を生む場合は、最小限の試作またはユーザー確認を先に行う。

---

## 1. アプリの目的

### 1.1 目的（決定済み）

本アプリは、線形代数を学ぶ学生が、ブラウザ上でベクトルとベクトル空間に関する概念を視覚的・対話的に理解するための Web 教材である。

旧 MATLAB 教材が対象としていた次の学習目的を引き継ぐ。

1. 複数の数ベクトルを 2 次元空間・3 次元空間上で可視化する。
2. 一次独立・一次従属の概念を理解する。
3. ベクトルの集合が張る空間を理解する。

旧教材の課題だった、MATLAB の起動、ファイルや関数の準備、列ベクトルや関数呼び出しの記法といった、数学そのものではない操作負荷を取り除く。

学内 Web サーバー上で提供し、学生が URL または QR コードから短時間でアクセスできる教材とする。

### 1.2 中核設計原則（決定済み）

#### State Reproducibility Principle

教材として意味のある現在状態は、共有 URL としてエクスポートでき、その URL を開くことで再現できなければならない。

共有 URL は単なるページリンクではなく、授業で使用する「例そのもの」の保存・共有手段として扱う。

主な利用目的は次のとおりである。

- 教員が作成した例を教材スライドへ埋め込む。
- 教員から学生へ同じ例を共有する。
- 学生間で現在の状態を共有する。

### 1.3 本アプリが解決する問題（決定済み）

- 教材を起動するまでの準備時間が長い。
- ベクトル入力時に MATLAB 固有の配列・列ベクトル記法が必要になる。
- 描画関数へベクトルや span 対象を渡す操作が煩雑である。
- 授業中に教員と学生が同じ数学的状態をすばやく共有しにくい。

---

## 2. 想定ユーザー

### 2.1 主ユーザー（決定済み）

- 線形代数の授業を受講する学生
- 線形代数の授業で可視化教材を提示・配布する教員

### 2.2 利用場面（決定済み）

- 教員が授業スライド内の URL または QR コードを提示する。
- 学生が PC、スマートフォン、タブレット等のブラウザからアクセスする。
- 教員または学生がアプリ内で作成した状態を URL として共有する。

### 2.3 前提知識

- 学生の数学的前提は授業進度に依存する。
- MATLAB、プログラミング、コマンドラインの知識を利用前提にしない。（決定済み）
- 対象学年、履修前提科目、具体的なアクセシビリティ要件は未決定。

---

## 3. スコープ

### 3.1 初期実装の必須スコープ（決定済み）

初期実装では、少なくとも旧 MATLAB 教材の学習目的をブラウザ上で実現する。

| ID | 機能 | 要件ステータス |
|---|---|---|
| CORE-01 | 2 次元の複数ベクトル表示 | 決定済み |
| CORE-02 | 3 次元の複数ベクトル表示 | 決定済み |
| CORE-03 | 一次独立・一次従属の理解を支援する表示 | 決定済み |
| CORE-04 | 選択したベクトル集合が張る空間の表示 | 決定済み |
| CORE-05 | URL または QR コードからブラウザで利用開始 | 決定済み |
| CORE-06 | 現在の教材状態を URL としてエクスポート | 決定済み |
| CORE-07 | 共有 URL から同じ教材状態を再現 | 決定済み |

### 3.2 授業内容に含まれる拡張スコープ（授業対応要件）

添付授業内容には、次のトピックが含まれている。これらをアプリでどの範囲まで実装するか、各機能の UI、初版への採否は未決定である。

| 授業時期 | 主な授業内容 | 会話中に提示された教材案 | 扱い |
|---|---|---|---|
| 第 1〜2 週 | ベクトル空間、部分空間 | Vector Space / Subspace Explorer | 未決定（提案あり） |
| 第 2〜5 週 | span、一次独立・一次従属、一次独立な最大個数、rank | Span & Independence Lab、一次結合エクスプローラ | 中核目的とフェーズ4の一次結合UIを実装対象とする |
| 第 6〜7 週 | 基底、次元、座標 | Basis & Dimension Lab | 未決定（提案あり） |
| 第 8 週 | 中間試験 | アプリの新規学習項目は設定しない | シラバス記載事項 |
| 第 9〜11 週 | 線形写像、核、像、rank、退化次数、次元定理、表現行列 | Linear Mapping Lab | 未決定（提案あり） |
| 第 12〜14 週 | 固有値、固有ベクトル、固有空間、対角化、Cayley–Hamilton | Eigen Lab 等 | 未決定（提案あり） |
| 第 15 週 | 内積、射影、Gram–Schmidt | Inner Product Lab | 未決定（提案あり） |

### 3.3 初期スコープ外または採否未決定

次の項目は会話中に提案されたが、決定済みではない。

- 多項式、関数、行列を抽象ベクトルとして扱う表示
- 抽象対象・座標・幾何表示の同期
- 部分空間判定教材
- 基底変更と座標格子の同期
- 線形写像、核、像、次元定理、表現行列
- 固有値、固有ベクトル、固有空間、対角化
- Cayley–Hamilton の可視化
- 内積、射影、Gram–Schmidt のアニメーション
- 集合・写像の図示
- Explore モード、Challenge/Mission モード
- 学生が共有 URL を回答として提出するワークフロー
- ランダム例生成

---

## 4. 機能要件

### 4.1 ベクトル表示

#### FR-VEC-01: 2D 表示（決定済み）

- 2 次元数ベクトルを座標空間上のベクトルとして表示する。
- 複数ベクトルを同時に表示できる。
- 各ベクトルは、表示順の色、番号、数式表記した表示名を併用して識別する。span表示中は選択状態、線種、説明文も併用し、色だけに依存しない。（D-012、D-020、D-026）

#### FR-VEC-02: 3D 表示（決定済み）

- 3 次元数ベクトルを座標空間上のベクトルとして表示する。
- 複数ベクトルを同時に表示できる。
- 3D の視点操作を提供する。具体的なジェスチャーは試作で確認する。旧教材と同様、視点回転を直観的理解に利用する。
- 数学で一般的な右手座標系とし、初期の等角的視点では、x軸は右向き、y軸は左下（手前）から右上（奥）、z軸は鉛直下から上をそれぞれ正方向として配置する。
- 数学的な長さと平行性の比較を優先して正投影を用いる。カメラはz軸上向きとし、初期の等角的視点に加えて正面・右・上・等角・全体表示を提供する。（D-037）
- xy平面の補助格子、正方向に矢じりを持つ3軸、負方向の破線、原点、固定ベクトルとラベルを表示する。回転・ズーム・パンはPCとタッチ端末に対応し、Canvas内で開始した操作をページ操作と分離する。
- 3D描画は操作・プリセット・リサイズ時だけ更新し、常時アニメーションしない。3Dモジュールは初回の3Dタブ選択時に遅延ロードし、タブ往復後はマウントとカメラ状態を維持する。高DPI上限、リサイズ、WebGL失敗、リソース解放を扱う。（D-038）

#### FR-VEC-03: ベクトル入力・編集（決定済み）

次の二つを同期させる。

- ベクトル先端のドラッグまたはタッチによる直接操作
- 座標値の数値入力

2D では数値入力に加えて、マウス、ペン、タッチの Pointer Events による先端ドラッグを提供する。先端には見た目の点より広い操作領域を設け、座標面の背景に割り当てたパン操作と分離する。キーボードでは同じ座標を数値入力欄から変更できる。モバイルではSVG座標面全体へ `touch-action: none` を適用し、内側で開始した先端ドラッグ、1本指パン、2本指ピンチがページ操作へ途中で移行しないようにする。ページスクロールとページ全体のズームは座標面の外側で維持する。（D-028）

ドラッグ中は開始時の表示範囲を固定する。自動表示では終了後に全ベクトルが収まる範囲を再計算し、手動表示では利用者が設定した範囲を維持する。ドラッグ座標はグリッドへ固定せず、表示上の1ピクセルに相当する数学座標の桁を基準に丸め、各成分を絶対値 `1,000,000` 以下に制限する。（D-018）

ドラッグ中のベクトル `v` の先端から、他の非零ベクトル `w` が張る直線までの垂直距離 `|det(v, w)| / ||w||` が現在の表示幅の `1%` 以下になった場合、最も距離が近い直線へ `v` を直交射影し、完全に平行な座標へスナップする。基準表示範囲 `[−5, 5] × [−5, 5]` では吸着距離を `0.1` とし、表示幅100では `1`、表示幅4では `0.04` とすることで画面上の吸着幅を一定にする。スナップは直接操作中だけに適用し、ヒステリシスは設けない。数値入力および D-009 の rank 判定閾値 `1e-10` は変更しない。（D-019）

3Dは数値入力を常時利用可能にしたまま、「視点を操作」と「ベクトルを操作」を明示的に切り替える。ベクトル操作では対象を1本選び、矢先に置いたx・y・zの双方向ハンドルをマウス、タッチ、ペンでドラッグして1成分だけを変更する。画面向き平面上の自由ドラッグは採用しない。カメラ操作は視点操作モードだけに割り当て、軸がほぼ奥行き方向を向き画面へ十分投影されない場合は視点プリセットの変更を案内する。各成分の±0.1ボタンと既存数値入力をキーボード代替とし、座標安全上限を共通化する。操作モードと選択ベクトルは一時UI状態であり、共有URLへ含めず、Resetで視点操作と先頭ベクトル選択へ戻す。5.6は試作であり、利用者確認後に本採用・PC限定・見送りを判断する。（D-037、D-042）

2Dでは、表示中のベクトルを0本から8本まで追加・削除できる。追加時は座標 `(1, 0)`、span対象に選択済みとし、既存IDまたは表示名と衝突しない最小の正の整数添え字を持つID・表示名を自動設定する。削除は確認なしで即時に行い、対応するspan選択も除く。空集合はrank 0、生成する空間の次元0、一次独立として既存の数学規則に従う。3.1では表示名と色の編集を設けず、色は表示順の8色固定パレットから割り当てる。追加・削除後の状態は共有状態v1で保存・復元し、ResetはInitialStateの本数を復元する。（D-026）

#### FR-VEC-04: 2D 表示範囲の操作（決定済み）

- ベクトルの操作・編集により現在の表示範囲を超える場合を含め、原則として全ベクトルを観察できるよう表示範囲を自動的にスケーリングする。
- PC では少なくともマウスホイール、タッチ端末では少なくともピンチ操作で、表示範囲を手動でも拡大・縮小できるようにする。
- ホイールまたはピンチを利用できない場合にも、画面上のボタンをキーボードで操作して拡大、縮小、全体表示を行えるようにする。
- 自動追従と手動操作の切替、パンは D-014・D-017 の試行方針に従う。手動表示範囲は D-023 により共有対象外とし、共有URLの復元時は自動範囲を再計算する。

#### FR-VEC-05: 2D/3Dの次元切替（決定済み）

- Lab上部に「2D座標平面」「3D座標空間」を対等な2択として示すセグメント型タブを置く。スイッチは用いない。
- マウス・タッチに加えて、左右矢印・Home・Endキーで切り替えられるようにし、狭い画面でも2択を同時に表示する。
- 2Dと3Dの`InitialState`、`CurrentState`、未確定入力、一時UIを別々に保持する。同一閲覧セッション内で往復しても各状態を失わず、座標を暗黙に変換しない。
- 共有URLは現在の一方の次元だけを保存し、URLの次元を初期選択する。Resetは現在の次元だけを、その次元の`InitialState`へ戻す。（D-037）

### 4.2 span の表示

#### FR-SPAN-01: 張る空間の可視化（決定済み）

- 一つ以上のベクトルから、表示対象となる集合を指定できること。
- 指定した集合が張る空間を可視化すること。
- 2D/3D で数学的に同じ集合を同じ意味になるよう扱うこと。

2D Web版の初期表現は次のとおりとする。（D-020、D-029）

- rank 0: 原点の二重マーカーと「原点」のラベル
- rank 1: 原点を通る太い破線と「原点を通る直線」のラベル
- rank 2: 座標面全体の薄い面・斜線パターンと「2次元座標平面全体」のラベル
- `spanSelection` に含まれないベクトルは、幾何表示中だけ薄くして選択対象と区別する。

rank 0・1・2の幾何表示と座標面内ラベルは、特定のベクトル色と混同しない中立的な灰色系とする。rank 1の破線は濃い灰色、rank 2の背景と斜線は淡い灰色を用い、ベクトルの8色パレットは変更しない。形、破線、斜線、ラベル、説明文も併用する。

各ベクトルにはネイティブなチェックボックスを設け、マウス、タッチ、キーボードで選択できるようにする。空集合も許可し、その生成する空間を零部分空間として表示する。`visualization.showSpan` で幾何表示だけをオン・オフでき、選択状態と解析値は維持する。既定例は全ベクトルを選択し、幾何表示をオンにする。

選択集合を `S`、そのベクトル `a₁, …, aₙ` を列に並べた行列を `A` と定義し、`rank(A)` と `dim(span(S))` を対応付ける。全ベクトルの判定カードでも、そのカードで表示する全列を同じ規則で `A` と定義し、両カードの用途は明確に分ける。（D-034）

3Dでは、rank 0を原点の半透明球とワイヤーフレーム、rank 1を原点を通り現在の表示立方体を横切る太い破線、rank 2を原点を通る格子なし・両面表示の半透明平面として可視化する。rank 3は表示範囲内を薄い半透明グレーの立方体で満たし、span専用格子を置かず、3D座標軸全体を示す実線の外枠Boxと「3次元座標空間全体」のラベルを組み合わせる。幾何表示は中立的な灰色系とし、表示中は未選択ベクトルを薄くする。`visualization.showSpan`で表示だけを切り替え、選択集合と解析結果は維持する。（D-037、D-040）

### 4.3 一次独立・一次従属

#### FR-LI-01: 概念理解の支援（決定済み）

- 表示中または選択中のベクトル集合について、一次独立・一次従属の違いを理解できる表示を提供する。
- ベクトル集合と張る空間の次元との関係を観察できること。

次を、可視化と対応する日本語主体の短い説明として表示する。（決定済み）

- `Linearly independent` / `Linearly dependent`
- rank
- span の次元
- 行列式、2D の平行四辺形面積、3D の平行六面体体積は初版の必須表示に含めない。

#### FR-LI-02: 一次結合エクスプローラ（決定済み）

- 既存のspan選択を一次結合の対象集合 `S` とし、0〜8本を選択できる。別の選択集合は追加しない。
- 表示ベクトルを `a₁, …, a₈`、選択ベクトルを列に持つ行列を `A`、係数列を `c`、ターゲットベクトルを `v` として `Ac = v` を解析する。
- `rank(A)` と拡大行列 `rank([A | v])` から、`v` が表現不可能、一意に表現可能、無数に表現可能のいずれかを示す。
- 表現可能なら小数の係数例を示す。無数の場合はなるべく異なる2例を常時表示し、自由係数の数と零空間基底による一般解だけを既定で折り畳む。
- 2本の一次独立な2Dベクトルでは、任意の転置横書き列ベクトル `{}^t[a, b]` を表す係数の一般式を用意し、既定では折り畳む。利用者は展開して常時表示できる。
- ターゲットはクリック・タップ、先端ドラッグ、数値入力で変更できる。rank 1の直線とrank 0の原点へはD-019と同じ画面幅1%の操作用距離で吸着し、D-009の数学判定と分離する。
- 一次結合モードでは短いクリック・タップを配置、一定距離以上の背景移動をパン、2点操作をピンチとして区別する。吸着は配置と先端ドラッグだけに適用し、数値入力値は自動変更しない。ターゲット未配置は正規状態とし、両方の入力欄を空にするか「ターゲットを消去」で戻せる。
- 2項の一次結合は、`c₁a₁` と `c₂a₂` を原点からの2辺とする平行四辺形で示す。頭尾接続による移動経路としては描かない。
- `Ac = v` の結果は「不能（解なし）」「唯一解」「不定（解が無数）」と、一次結合で表現できるか・表し方が一意かを対応付ける。係数行列と拡大係数行列のrankも示す。
- 係数は有効数字6桁で表示し、表示値に丸めがなければ等号、丸めがあれば近似記号を使う。無数解では異なる2例を常時表示し、自由係数・零空間基底による一般解だけを折り畳む。
- 一般の `{}^t[a, b]` に対する係数公式は、選択が2本かつ一次独立な場合だけ2×2逆行列として折り畳み表示する。平行四辺形は選択がちょうど2本で表現可能な場合だけ描き、一次従属なら退化した形として説明し、3本以上では描かない。（D-033、D-034）
- 主式は `v = Ac = (a₁, …, aₙ)[c₁; …; cₙ]` と `c = {}^t[c₁, …, cₙ]` を併記する。係数例は記号列と値列を明示し、一次結合も記号式と値を代入した式の2段で示す。
- 主式、主式右端の係数列、直下の係数転置表示は同じ色・数式フォント・文字サイズを用いる。一般ターゲット公式では説明中の `v = {}^t[a, b]` を維持し、逆行列の積の右端は縦の列ベクトル `\left[ a \\ b \right]`、逆行列係数 `1 / det(A)` は横線を持つ上下分数として描画する。
- 不定では異なる係数例を2つ常時表示し、自由係数を使った一般解だけを既定で折り畳む。一般解は特解 `c_p`、`An_j = 0` を満たす零空間方向、`A(c_p + Σt_jn_j) = v` の理由、各係数を展開した一次結合を示す。（D-034）
- 数学ロジックは2Dと3Dで再利用可能にし、フェーズ5で3Dへ拡張する。（D-031）
- 3Dではターゲット `v` のx・y・z成分を数値入力し、曖昧なCanvasクリック配置は行わない。不能・唯一解・不定の分類、係数例、一般解の数式表示は2Dと同じ求解・表示ロジックを再利用する。
- 3Dで選択が2本かつ表現可能な場合は、`c₁a₁` と `c₂a₂` を原点からの2辺とする平行四辺形を描く。選択が3本かつ表現可能な場合は、第1の係数例による `c₁a₁`、`c₂a₂`、`c₃a₃` を3辺とする原点基準の平行六面体を試行表示する。係数を乗じた3辺のrankが3未満なら退化形として説明する。
- 3本で不定の場合、第2の係数例と自由係数を使った一般解は数式カードだけに示し、幾何表示は第1例に固定する。4本以上では係数と一般解を数式表示し、単一の立体へ対応しないため幾何表示を行わない。ターゲットと2・3項の幾何頂点は3D自動表示範囲へ含める。（D-041）
- 3Dの通常ベクトル名と係数付き寄与ベクトル名は、ラベル枠が矢じりを覆わない画面上の位置へ置く。同じ終点になる場合は互いに反対側へ配置する。背景は図形が透ける半透明色とし、薄い縁取りと文字影で可読性を維持する。（D-041）

### 4.4 状態共有 URL

#### FR-SHARE-01: 状態のエクスポート（決定済み）

- 現在の教材状態を URL としてエクスポートできること。
- 教員がその URL を授業スライドへリンクとして埋め込めること。
- URL は教員から学生、学生から学生へ共有できること。

#### FR-SHARE-02: 状態の再現（決定済み）

- 共有 URL を新しいブラウザセッションで開いたとき、教材として意味のある状態が再現されること。
- URL から再現された状態は、開いた後に通常どおり操作できること。

#### FR-SHARE-03: 共有対象（決定済み）

共有 URL は「例そのもの」を再現する。最低限、次元、ベクトルの ID・表示名・座標、span 選択、教材上意味のある表示切替を含める。

共有対象:

- 対象 Lab または画面
- 2D/3D の次元モード
- ベクトルの ID、表示名、座標
- span の対象選択
- 教材上意味のある表示 ON/OFF

3D カメラの向き、注視点、上方向、拡大率は、教員の意図した見え方を再現する教材状態として共有状態v3へ含める。視線方向と上方向は正規化し互いに平行でないこと、注視点と拡大率が安全上限内であることを検証する。（D-037、D-039）

2D の手動表示範囲、自動／手動表示モードは共有対象に含めない。共有URLの復元時はベクトルから自動表示範囲を再計算し、エクスポート時の表示範囲は変更しない。（D-023）共有状態v3はターゲット `v`、エクスプローラ表示、3Dカメラを共有する。現行実装はv1・v2も読み込めるが、正式リリース前の互換性は保証しない。（D-031、D-032、D-034、D-039）

共有対象外とする一時的 UI 状態:

- パネルの開閉状態
- 選択中の詳細タブ
- 2D の手動表示範囲と表示モード
- 入力途中の未確定文字列
- ツールチップ
- マウスポインター位置
- ドラッグ中などの一時状態

一次結合のターゲット `v` とエクスプローラを表示するかは教材上意味のある状態として共有する。係数例と一般解はベクトル・選択・ターゲットから再計算し、共有データへ重複して保存しない。係数公式と一般解の折り畳み状態は一時的UI状態として共有しない。

#### FR-SHARE-04: URL 表現方式（決定済み）

- バージョン付き JSON スキーマを使用する。
- JSON を Base64URL 形式で一つのクエリーパラメーターへ格納する。
- 初版では圧縮しない。
- 復元前にスキーマを検証する。
- デコードは例外を外へ漏らさず、成功または分類済みエラーを返す。
- エンコード文字列は安全上 8192 文字までとする。この値は実運用で保証する URL 長ではない。
- 正式リリース前の開発中URLは後方互換性を保証せず、正式リリース以後に保証を開始する。現行v1・v2読込はv3の出力データとURL長に影響しないため残すが、今後のスキーマ変更を制約しない。（D-032、D-039）
- 正式リリース後のマイグレーション方式と URL 長上限は、運用条件を確認して具体化する。

#### FR-SHARE-05: 共有 UI（決定済み）

- 2D Lab上部の「共有URLをエクスポート」ボタンからモーダルダイアログを開く。
- URL欄の上に、同じ共有URLを表す余白付きQRコードを表示する。URLを外部サービスへ送信せず、ブラウザ内でPNGを生成する。
- 読み取り専用で選択可能なURL欄と、「クリップボードにコピー」「QRコードを保存」「テキストで保存」「閉じる」を表示する。QRコード保存はテキスト保存の左隣とする。
- コピー成功・失敗、QRコードとテキストのダウンロード開始をダイアログ内へ通知する。QR生成失敗時も手動コピーとテキスト保存を利用できるようにする。
- QRコードは768×768ピクセル、誤り訂正レベルM、4モジュール分の余白を初期値とし、時刻付きファイル名のPNGとして保存する。共有URLがQRコードの容量を超える場合は理由を案内する。
- テキストファイルにはURLと末尾改行だけをUTF-8で保存し、ファイル名に日時を含める。
- PCでは4ボタンを横並び、640px以下では縦並びとする。Escape、背景クリック、閉じるボタンに対応し、フォーカスを適切に管理する。
- 未確定の不正な座標文字列がある間はエクスポートを無効にする。

D-022時点ではアプリ内QR生成を2.6に含めなかったが、D-036によりフェーズ5前の追加作業4.6として採用した。

### 4.5 ディープリンク

#### FR-NAV-01: URL/QR から教材へアクセス（決定済み）

- 学生が URL または QR コードからブラウザで教材を開けること。
- ページ表示後の計算・描画に MATLAB や追加のローカル教材ファイルを要求しないこと。

各 Lab へトップ画面を経由せず直接移動する URL 構造は提案されているが、画面構成自体が未決定のため正式仕様ではない。

### 4.6 Reset（決定済み）

`DefaultState`、`InitialState`、`CurrentState` を区別する。Reset は現在開いている次元の `CurrentState` だけを、共有 URL を含むページ読み込み時の同次元の `InitialState` へ戻す。別次元の状態は変更しない。入力欄を復元値へ同期し、表示を自動範囲、詳細タブを「ベクトル編集」へ戻し、一時的なドラッグ・吸着・エクスポート状態を消去する。閲覧中URLは変更しない。アプリ既定例へ戻る操作が必要な場合は、Reset とは別の名称で提供する。（D-024、D-037）

---

## 5. 画面一覧と画面遷移

### 5.1 決定済みの画面要件

- ブラウザ上で動作する教材画面が存在する。
- 2D/3D のベクトルと span を確認できる。
- Lab上部のセグメント型タブで2Dと3Dを切り替え、各次元の状態を独立して保持する。
- 共有 URL から教材状態を直接開ける。

### 5.2 全体の画面構成（未決定）

トップ画面、Lab の分割単位、ナビゲーション、画面名、URL パスは未決定である。現行2D Lab内の座標面、要約、詳細タブの配置は6.4およびD-021で試行採用・利用者確認済みであり、この全体ナビゲーションの保留とは区別する。

会話中には次の Lab/画面案が提示された。

1. Vector Playground / Vector Space
2. Subspace
3. Span
4. Linear Independence
5. Basis & Coordinates
6. Linear Mapping
7. Matrix Representation
8. Eigenvectors
9. Diagonalization
10. Inner Product / Gram–Schmidt

これらは授業内容との対応候補であり、そのまま実装する決定ではない。

### 5.3 遷移案（未決定）

```text
トップ画面（任意）
  └─ 各 Lab
       ├─ 状態を編集
       ├─ 共有 URL を生成
       └─ 共有 URL から同じ Lab と状態を復元
```

トップ画面を設けない構成、単一画面内のモード切替、各 Lab の独立 URL なども選択肢である。

---

## 6. UI/UX 仕様

### 6.1 決定済み原則

- MATLAB を起動させない。
- コマンドや MATLAB 固有の配列記法を学生へ要求しない。
- URL または QR コードから短時間で教材へ到達できる。
- 現在の例を URL として容易に共有できる。
- 数学的概念の理解以外の操作負荷を小さくする。

### 6.2 旧教材から参照する視覚上の知見

旧教材には次の工夫がある。これらを Web 版へ継承するかは個別に未決定だが、視認性レビュー時の基準候補とする。

- 座標軸を表示する。
- 平行なベクトルは短いものを前面に描き、背面へ隠れないようにする。
- 1 次元部分空間を太い直線として背面へ描く。
- 2 次元部分空間を半透明平面として描く。
- 3D では視点回転を理解に利用する。

### 6.3 操作仕様（初版方針は決定済み）

| 操作 | PC 案 | タッチ端末案 | ステータス |
|---|---|---|---|
| 2D ベクトル編集 | 先端ドラッグ | 先端ドラッグ | 採用 |
| 座標の厳密入力 | 数値入力欄 | 数値入力欄 | 採用 |
| 3D カメラ回転 | ドラッグ | タッチ操作 | 採用。詳細は試作で確認 |
| 2D ズーム | ホイール、画面ボタン | ピンチ、画面ボタン | 試行採用・利用者確認済み。D-013・D-014・D-017 |
| 2D パン | 座標面の背景ドラッグ | 座標面の背景ドラッグ | 試行採用・利用者確認済み。D-014・D-017 |
| 3D ズーム | ホイール | ピンチ | 採用。詳細は試作で確認 |
| 3D パン | ドラッグ操作 | タッチ操作 | 採用。詳細は試作で確認 |
| Reset | ボタン | ボタン | 採用 |

ベクトル操作と 3D カメラ操作のジェスチャー競合をどう解決するかも未決定である。

### 6.4 2D Labのレスポンシブ配置（試行採用・利用者確認済み）

D-021に基づき、座標面と編集・解析領域を次のように配置する。

- 編集・解析領域の上部には、選択集合 `S` の形と `dim(span(S))`、全ベクトルの一次独立・一次従属と `rank(A)` の2要約を常時表示する。要約を押すと対応する詳細を開く。
- 詳細は「ベクトル編集」「生成する空間」「全ベクトル」の3タブとし、一度に1パネルだけ表示する。既定タブは「ベクトル編集」とする。
- タブ選択は一時的なUI状態であり、共有URLの対象に含めない。
- PCの十分に広い画面では座標面とインスペクターを2列にする。画面高さにも余裕がある場合だけ座標面をsticky表示し、詳細が伸びても座標面を参照できるようにする。
- タブレット相当以下では座標面、2要約、タブ、詳細の順に1列で表示する。要約は中間幅では2列、スマートフォンでは1列とする。
- スマートフォンではタブ名を「編集」「span」「全体」へ短縮する。座標面のsticky表示と右列だけの独立スクロールは使用しない。
- タブはネイティブなボタンを用い、クリック・タッチに加えて左右矢印、Home、Endキーで移動できるようにする。

将来カードが増える場合は無制限にタブを追加せず、「入力」「span」「判定」等の既存カテゴリ内へ整理する。比較に必要な主要値は常時表示の要約へ残す。

### 6.5 表示言語と数学表記（教科書準拠方針は決定済み）

学生向け画面、説明、例題の数学用語と数式表記は、利用者が作成した `math-writing-rules.txt` を基準とする。（D-015）このファイルは授業内容に合わせて更新される生きた基準文書として扱い、各作業単位の画面確認と各フェーズ終了時に照合する。

フェーズ 2 に直接関係する主な規則は次のとおりである。

- スカラーはレギュラー斜体小文字、ベクトルの基本文字はボールド斜体小文字、行列はボールド斜体大文字で表す。ベクトル記号の添え字数字は立体レギュラーで表す。
- 数ベクトルは特に断らない限り列ベクトルとして表示する。
- 「一次結合」「一次関係」「一次独立」「一次従属」を主用語とし、「線形結合」等は別名として紹介する場合に限る。ただし「線形写像」は主用語とする。
- 教科書の主表現である「ベクトルが生成する空間」を先に示し、`span` と「張る空間」を対応する補助語として紹介する。
- `rank` は行列または写像を引数に取り、`dim` はベクトル空間を引数に取る形で対象を明示する。
- 順序に依存しないベクトル集合と、順序に依存するベクトルの組を区別する。`span` の既定表記では集合を波括弧で示す。
- 学生向け画面には生の TeX コマンドを表示せず、意味に対応する視覚表現と読み上げ可能な代替表現を用意する。

内部コードの型名、プロパティ名、テスト名はこの表記規則の対象外とする。現在の2D画面はHTML・SVGと数式用フォント候補で組版している。より複雑な数式の描画方式、UIの英語併記、色・テーマ、スクリーンリーダー対応の具体的な達成水準は必要な段階で決める。

---

## 7. データモデル

### 7.1 必要となる概念モデル（派生制約）

状態 URL の再現性を保証するため、少なくとも次の概念を区別する。

```text
ApplicationState
  ├─ MathematicalState    教材例の数学的内容
  ├─ VisualizationState   教材上意味のある表示状態
  └─ TemporaryUIState     操作中・一時的な UI 状態
```

現行実装では共有可能な数学・表示状態を `ShareStateV3`、入力途中の文字列、2D手動表示範囲、ドラッグ・吸着、表示中の詳細タブ等を React の一時状態として分離している。v3は一次結合エクスプローラとターゲットに加え3Dカメラを持ち、旧 `ShareStateV1`・`ShareStateV2` は読込時にv3へ移行する。共有 URL へ含めるのは確定した共有対象のみとし、`TemporaryUIState` は共有しない。

### 7.2 中核数学データ（概念レベル）

```text
Vector
  id: 安定した識別子
  coordinates: 数値列

VectorScene
  dimension: 2 | 3
  vectors: Vector[]
  spanSelection: Vector.id[]

LinearCombinationExplorer
  target: Vector coordinates | null
  visible: boolean
```

上記はデータモデルの最低限の概念である。現行 v3 のベクトルは `id`、`name`、`coordinates` を持ち、ID形式と数値上限は7.3の検証規則に従う。2D・3Dの8色は表示順から導出する表示資源であり、共有状態には含めない。

### 7.3 共有状態の概念スキーマ（形式・最低項目は決定済み）

```json
{
  "v": 3,
  "lab": "vector-space",
  "dim": 3,
  "vectors": [
    { "id": "a1", "name": "a₁", "coordinates": [1, 0, 1] },
    { "id": "a2", "name": "a₂", "coordinates": [0, 1, 1] }
  ],
  "spanSelection": ["a1", "a2"],
  "visualization": {
    "showSpan": true,
    "camera": {
      "direction": [0.50305546, -0.68060445, 0.53264696],
      "target": [0, 0, 0],
      "up": [0, 0, 1],
      "zoom": 1
    }
  },
  "linearCombination": {
    "visible": true,
    "target": [2, -1, 3]
  }
}
```

バージョン付き JSON を Base64URL 形式で一つのクエリーパラメーターへ格納する。現行v3は上記のフィールドだけを受け付け、未知のフィールドを拒否する。旧v1は一次結合を非表示・未配置、旧v2はカメラ未指定としてv3へ移行する。新しく生成するURLは常にv3とする。D-032により正式リリース前の後方互換性は保証しないが、既存教材リンクの検証に使える厳密なv1・v2読込は残す。`visible` と `target` は独立した共有状態で、`target` は次元に一致する座標列または `null` とする。3Dカメラの`direction`は注視点からカメラへの単位方向、`target`は注視点、`up`は画面上方向の単位方向、`zoom`は正投影の拡大率を表す。2Dと旧版から移行した3Dでは`camera: null`を許し、既定等角視点として解釈する。安全上の初期上限は次のとおりとする。

- ベクトル: 8 本
- ID: 英数字、ハイフン、アンダースコアからなる 32 文字以内
- 表示名: 正しい Unicode からなる空でない 40 文字以内
- 座標: 有限値かつ絶対値 `1,000,000` 以下
- ターゲット座標: 現在の次元と同じ要素数で、ベクトル座標と同じ有限値・絶対値上限
- 3Dカメラの方向・上方向: 3個の有限値からなる長さ1のベクトルで、互いに平行でない
- 3Dカメラの注視点: 3個の有限値で、各成分の絶対値 `10,000,000` 以下
- 3Dカメラの拡大率: `0.05` 以上 `100` 以下の有限値
- span 選択: 重複がなく、存在するベクトル ID のみ
- エンコード文字列: 8192 文字以内

`-0` はベクトル座標、ターゲット座標、カメラ状態で数学的に等価な `0` へ正規化する。カメラ方向と上方向は小数8桁、注視点は小数6桁、拡大率は小数8桁へ丸めて決定的に保存する。状態の圧縮と実運用で保証する URL 長、v4以降が必要になった場合のマイグレーションは運用テスト後に判断する。

### 7.4 数値モデル（初版方針を試行採用）

- 初版の内部計算は JavaScript の倍精度浮動小数点数を使用する。
- 座標には有限値だけを許可し、`NaN` と正負の無限大を拒否する。
- rank 判定の前に、各ベクトルをその最大絶対座標で個別に正規化する。これにより、各ベクトルの単純な定数倍に対して判定を安定させる。
- 部分ピボット付き消去法を使用し、既定の相対許容誤差を `1e-10` とする。
- 許容誤差は数学ロジックの呼び出し時に変更可能とし、授業で扱う入力範囲を確認した後に再評価する。
- 正規化後のピボットが許容誤差以下なら、教材上はほぼ一次従属として扱う。
- 空集合は rank 0、span の次元 0、一次独立とする。
- 一次結合は列ごとの正規化と同じ許容誤差を使って `Ac = v` と拡大行列を行基本変形し、解なし・一意・無数を分類する。表現可能な場合は特解と零空間基底を求める。

一次結合係数は当面小数で表示する。有理数・無理数の厳密計算、`1/3`、`√2` 等の教材向け表示は初期の一次結合エクスプローラに含めず、教材上の必要性を確認して判断する。

---

## 8. API / Interface 仕様

### 8.1 外部 API（初版は不使用）

- 初版はサーバー API を使用しない。
- 静的ファイル配信とクライアント側処理だけで構成する。
- ユーザーアカウント、データベース、クラウド保存、LMS API 連携は初版に含めない。

### 8.2 状態共有インターフェース（派生制約）

実装方式にかかわらず、次の論理インターフェースを分離すること。

```ts
encodeShareState(state) -> string
decodeShareState(encoded) -> Result<State, ShareStateError>
buildShareUrl(state, location) -> URL
```

関数名は説明用の例である。重要なのは、状態のエンコード、検証付きデコード、URL 構築を描画処理から分離し、往復テスト可能にすることである。

### 8.3 URL 要件

- 共有 URL は一般的な教材スライド、チャット、LMS 等へリンクとして貼れる形式にする。（決定済み）
- 同一 URL から教材状態を再現する。（決定済み）
- 初版の共有状態は圧縮しない。URL の最大長、短縮 URL サービス、サーバー保存型 ID は未決定。
- 現在のoriginと公開サブパスを維持し、クエリーパラメーター名を `state` とする。エクスポートURLでは既存クエリーとハッシュを除き、`state`を1件だけ格納する。（D-024）

---

## 9. 状態管理

### 9.1 必須性（決定済み要件からの派生制約）

URL で例を再現するため、共有可能な状態は一つの明確なスナップショットとして取得できなければならない。

描画オブジェクトだけに状態を保持せず、状態から表示を再構築できる設計とする。

### 9.2 状態の区分

| 状態 | 例 | URL 共有 | ステータス |
|---|---|---|---|
| MathematicalState | 次元、ベクトル | 共有する | 最低項目は決定済み |
| VisualizationState | span 表示、選択 | 共有する | 最低項目は決定済み |
| VisualizationState | 3Dカメラの向き、注視点、上方向、拡大率 | 共有する | D-039・共有状態v3として実装済み |
| TemporaryUIState | 2D手動表示範囲、表示モード、詳細タブ | 共有しない | 決定済み |
| TemporaryUIState | hover、tooltip、drag 中 | 共有しない | 決定済み |

### 9.3 初期状態と Reset（決定済み）

次の三状態を分ける。

```text
DefaultState  アプリ既定値
InitialState  ページまたは共有 URL を開いた時点
CurrentState  現在編集中の状態
```

2Dと3Dにそれぞれ`DefaultState`、`InitialState`、`CurrentState`を持つ。`state`がなければ両次元の`DefaultState`を各`InitialState`とし、2Dを初期選択する。有効な2Dまたは3D共有状態があれば、その復元結果を該当次元の`InitialState`として同次元を初期選択し、他方には専用の`DefaultState`を使う。不正・重複・未対応状態は停止せず、両次元の`DefaultState`と2D初期選択へフォールバックして理由を表示する。

Reset は現在開いている次元の`CurrentState`だけを同次元の`InitialState`へ戻し、他方を変更しない。2Dでは同時に入力途中の文字列を同期し、自動表示、「ベクトル編集」タブへ戻し、一時的なドラッグ・吸着・エクスポート状態を消去する。（D-024、D-037）

### 9.4 状態管理ライブラリ（未決定）

専用ライブラリの採否、履歴、Undo/Redo、ブラウザの戻る・進むとの同期は未決定。

---

## 10. 使用技術・ライブラリ

### 10.1 決定済み

- Web ブラウザで利用するアプリとする。
- 学内 Web サーバーで提供する想定とする。
- 利用者に MATLAB の起動やコード実行を要求しない。

### 10.2 初版で採用する構成（決定済み）

| 技術 | 想定用途 | ステータス |
|---|---|---|
| TypeScript | アプリ実装 | 採用 |
| Vite | 開発・静的ビルド | 採用 |
| React | UI と状態に応じた描画の同期 | 採用 |
| SVG | 2D 座標、ベクトル、span | 採用 |
| Three.js 0.185.1 / `@types/three` 0.185.4 | 3D 座標、ベクトル、span、視点操作 | 5.2で採用。3Dタブ初回選択時に遅延ロード |
| D3 | 2D 描画補助 | 必要性が明確になるまで不採用 |
| math.js | 行列計算 | 必要性が明確になるまで不採用 |
| KaTeX | 数式表示 | 必要性を確認するまで保留 |

### 10.3 配置方式（決定済み）

完全クライアントサイドの静的 Web アプリとしてビルドし、公開先は HTML/CSS/JavaScript 等の静的ファイルを配信する。サーバー API、認証、データベースは初版に含めない。GitHub Pagesの公開パスはD-025で決定済みとし、学内nginxのURL書き換え設定とオフライン対応は未決定とする。

初回公開先はGitHub Pagesのプロジェクトサイトとし、GitHub Actionsがmainをpnpmでテスト・ビルドして生成した `dist/` を公開する。学内 Web サーバー上のnginxは将来の代替公開先として残す。（D-011、D-025）

- GitHub Pages のプロジェクトサイトでは、通常 `/<repositoryname>/` 以下で公開されるため、Vite の `base` を公開パスへ合わせる。
- 現行リポジトリではビルド時に `APP_BASE_PATH=/linear-algebra-visual-lab/` を指定し、PagesのSourceを「GitHub Actions」とする。
- mainのソースや開発用 `index.html` を直接配信せず、ワークフローが生成した `dist/` だけをPages artifactとしてデプロイする。`dist/`はmainへコミットしない。
- nginx では、ルート配信かサブパス配信かを確認する。クライアント側ルーティングを導入する場合は、存在しないパスを `index.html` へ戻す設定を検討する。
- 初版の共有状態は同じページのクエリーパラメーターで表現し、公開先固有の URL 書き換えへの依存を小さくする。
- nginx の既定の大きなリクエストヘッダーバッファは 8 KiB であり、リクエスト行は一つのバッファを超えられない。D-010 の 8192 文字は状態文字列の防御上限であって、完全な URL として利用可能な長さを保証しない。

---

## 11. 現在のディレクトリ構成

初版では次の責務分離を採用している。必要になるまで空の Lab ディレクトリや外部ライブラリは追加しない。

```text
/
├─ src/
│  ├─ app/              # 画面構成、CurrentState、一時UI状態
│  ├─ domain/           # ベクトル、span、rank 等の数学ロジック
│  ├─ sharing/          # 共有状態の encode/decode/validation
│  ├─ state/            # 既定状態と描画非依存の状態更新処理
│  ├─ teaching/         # 授業シナリオの状態、学習目的、期待結果
│  ├─ ui/               # 数学表記等の共通UI補助
│  └─ visualization/    # 2D描画と座標変換
├─ tests/
│  ├─ domain/
│  ├─ sharing/
│  ├─ state/
│  ├─ teaching/
│  ├─ ui/
│  └─ visualization/
├─ docs/
│  ├─ DECISIONS.md
│  ├─ GITHUB_PAGES.md
│  ├─ PROJECT_STATUS.md
│  └─ TEACHING_SCENARIOS.md
├─ .github/workflows/   # GitHub Pagesへの自動テスト・ビルド・公開
├─ SPEC.md
├─ ROADMAP.md
└─ README.md
```

Lab別ディレクトリ、E2Eテスト、公開用静的資源は必要性が明確になった段階で追加する。

---

## 12. エラー処理

### 12.1 共有 URL 読み込み（派生制約）

URL は利用者が変更できる入力であるため、復元前に検証する。

- 不正なエンコードをアプリ全体のクラッシュへつなげない。
- 必須項目不足、次元不整合、不正な数値を検出する。
- 復元できない場合は、読込エラーの見出し、理由、閉じる操作を表示する。
- エラー時は既定の2D例をInitialStateとして安全に表示し、教材の操作を継続できるようにする。（D-024、D-027）

### 12.2 数値入力（決定済み）

- 空欄、非数、無限大、許容範囲外を区別する。
- エラーを数学的判定結果として扱わない。
- 整数、小数、指数表記を受け付け、各成分を絶対値 `1,000,000` 以下に制限する。
- 未確定の欄には理由を対応付け、数学状態には直前の有効値を維持する。画面上部で未確定数と影響を示し、最初の該当欄へ移動できるようにする。訂正されるまで共有URLのエクスポートを無効にする。（D-016、D-022、D-027）

### 12.3 WebGL/描画機能非対応（未決定）

3D 実装方式に応じて、非対応ブラウザや GPU/描画初期化失敗時の扱いを決める必要がある。

---

## 13. セキュリティ上の要件

会話中に認証・個人情報・外部 API に関する要件は明示されていない。以下は状態 URL を安全に扱うための派生制約である。

- URL から読み込んだ値を信頼せず、型、次元、配列長、数値範囲、文字列長を検証する。
- URL の内容を `eval`、動的コード、未サニタイズ HTML として実行・挿入しない。
- 数式やラベルを DOM へ表示する場合は XSS を生まない API を用いる。
- 異常に大きい共有状態による過剰なメモリ使用や描画負荷を避けるため、D-010 の安全上限を適用する。実運用テスト後に再評価する。
- URL に学生の氏名、学籍番号、回答者を識別する個人情報を自動付与しない。アカウント機能自体が未決定である。
- 外部 CDN、アクセス解析、外部短縮 URL、クラウド保存を使う場合は、学内方針を確認する。現時点では採用未決定。

---

## 14. 非機能要件

### 14.1 利用容易性（決定済み）

- URL または QR コードからアクセスできる。
- MATLAB、コマンド入力、関数ファイルの準備を不要とする。
- 授業中の準備時間を旧 MATLAB 教材より短縮する。

### 14.2 互換性（初版方針は決定済み）

- 現行の Chrome、Edge、Firefox、Safari の直近主要版を対象とする。
- PC、タブレット、スマートフォンで閲覧可能にする。
- 2D はキーボード、マウス、タッチでの操作を目標とする。
- 3D は少なくともマウスとタッチでの操作を目標とする。

学内で管理されている端末・ブラウザの実態と最小画面幅は、運用確認時に具体化する。

### 14.3 性能（方向性は決定済み、数値目標は未決定）

- 授業中に学生が短時間で開けること。
- 操作に対する描画・数学的結果が理解を妨げない速度で更新されること。
- 同時利用人数、初回ロード時間、フレームレート、バンドルサイズの目標値は未決定。

### 14.4 保守性（派生制約）

- 数学ロジック、描画、UI、状態 URL 変換を分離する。
- 状態 URL の往復を自動テストできるようにする。
- 将来 URL 形式を変更する場合に備え、状態スキーマのバージョンを持たせる案を実装前に判断する。

### 14.5 可用性・ネットワーク（未決定）

- 一度読み込んだ後の操作をクライアント内で完結させる案がある。
- オフライン利用、PWA、Service Worker、キャッシュ戦略は未決定。

### 14.6 アクセシビリティ（初版基準は決定済み）

色だけに依存しない識別、明示的なラベル、キーボードで変更できる数値入力、十分なタッチ領域を必須とする。2D教材では、操作領域へのスキップリンク、主要操作の44px以上の高さ、数値添え字を明示する読み上げ、入力欄ごとのエラー説明、未確定入力から最初の該当欄へ移動する操作、ARIAを備えたタブとネイティブdialogを提供する。座標面はSVGの代替説明を持ち、色に加えて番号、名前、形、線種、説明文を併用する。（D-008、D-021、D-027）

---

## 15. 決定済み事項一覧

1. 線形代数のベクトル空間可視化教材を Web ブラウザで提供する。
2. 主な利用者は授業を行う教員と履修する学生である。
3. 初回はGitHub Pagesで提供し、URLまたはQRコードからアクセスできるようにする。学内Webサーバー上のnginxは将来の代替公開先とする。
4. 旧 MATLAB 教材の主要目的である、2D/3D 数ベクトル、一次独立・一次従属、張る空間の理解を引き継ぐ。
5. MATLAB 起動、コマンド、列ベクトル記法、関数呼び出し等の利用準備を学生へ要求しない。
6. 現在の教材状態を URL としてエクスポートできるようにする。
7. 共有 URL を開くことで教材例を再現できるようにする。
8. 共有 URL を教材スライド、教員から学生、学生間の共有に利用できるようにする。
9. 初版は旧 MATLAB 教材相当の中核機能に限定する。
10. TypeScript、Vite、React による完全クライアントサイドの静的 Web アプリとする。
11. 2D は SVG、3D は Three.js で描画する。
12. 2D のベクトル編集は数値入力と先端ドラッグを同期して提供する。
13. span 対象はベクトルごとに選択し、rank、span の次元、一次独立・一次従属を可視化と対応付けて表示する。
14. 共有状態はバージョン付き JSON を Base64URL 形式で一つのクエリーパラメーターへ格納する。
15. Reset はページ読み込み時の `InitialState` へ戻す。
16. PC、タブレット、スマートフォンと現行主要ブラウザを初版の対象とする。
17. mainを直接配信せず、GitHub Actionsがpnpmでテスト・ビルドして生成した `dist/` をGitHub Pagesへ公開する。
18. 2Dのベクトル集合は0本から8本まで追加・削除でき、追加時の既定値、命名、span選択、色、共有・Resetとの整合はD-026に従う。
19. 2Dの入力案内、アクセシビリティ、代表的な授業シナリオと3Dへ再利用する境界はD-027に従う。
20. 2D一次結合エクスプローラは既存span選択、共有ターゲット、小数係数、一例・第2例・一般解、原点基準の平行四辺形をD-031に従って扱う。
21. 2D/3Dはセグメント型タブで切り替え、状態を次元別に保持し、共有URLとResetは現在の次元だけを対象とする。
22. 3Dは右手座標系、z軸上向きの正投影を採用し、カメラ状態を共有する。数値編集を先行し、直接編集は操作モードと軸拘束ハンドルを試作する。

---

## 16. 却下・置換する案と理由

### 16.1 MATLAB を実行環境として継続する案（却下）

理由:

- 起動や準備に時間がかかる。
- ベクトル入力と関数呼び出しが煩雑である。
- 数学概念の学習以外の認知負荷が生じる。
- URL/QR から即時に同じ例を開く運用に適さない。

### 16.2 MATLAB 記法をそのまま Web UI へ移植する案（却下）

理由:

- 列ベクトルを作る記法や、span 対象を番号配列で渡す操作は、今回解消したい課題そのものである。

### 16.3 URL を単なる画面リンクとして扱う案（却下）

理由:

- URL は教材例そのものを再現し、授業スライドや利用者間で共有できる必要がある。

### 16.4 React を使用しない案（置換）

初期提案では React は必須でないとされていたが、D-002 により React を採用する構成へ置き換えた。状態に応じた UI と描画の同期、自動テスト、将来の機能追加を見通しよく保つためである。

---

## 17. 未決定事項

初期アーキテクチャに関する事項は D-001〜D-008 で決定した。次の詳細は、該当する実装フェーズまでに確認する。

### 17.1 プロダクト範囲

- Lab の数と名称。
- Explore/Challenge/Mission 機能の採否。

### 17.2 操作と UI

- ベクトルの追加・削除はD-026で採用済み。表示名と色を将来編集可能にする必要性。
- 2D/3D切替方法、3D投影、カメラ共有の基本方針はD-037で決定済み。5.2以降でカメラ操作感と具体的ジェスチャーを確認する。
- アプリ内QR表示・PNG保存はD-036で採用・実装・利用者確認済み。
- トップ画面と各 Lab のナビゲーション。

### 17.3 共有 URL

- URL 長上限。
- 共有状態v3の3DカメラはD-039として実装済み。実運用で保証するURL長と、正式リリース後の互換期間は未決定。
- D-039の共有状態v3からさらに将来スキーマを変更する場合の状態バージョンとマイグレーション。

### 17.4 数学・授業仕様

- D-009 で試行採用した相対許容誤差 `1e-10` の授業上の妥当性と説明方法。
- 一次結合係数の小数桁数と、丸め時に等号・近似記号を切り替える具体的な表示規則。
- 0 ベクトルや重複ベクトルを学生へ説明する具体的な UI。
- 各授業週で使用する具体的な教材シナリオと、授業内での利用時間。

### 17.5 技術・運用

- 学内サーバーのパス・URL 書き換え・HTTPS 条件。
- GitHub Pages公開後にnginxへも配置するか、およびGitHub Pagesのカスタムドメインを導入するか。
- nginx のリクエスト行上限を変更できるか。
- 学内管理端末の実態と最小画面幅。
- GitHub Pages以外の公開先におけるCIとデプロイ手順。GitHub PagesではD-025を採用済みであり、単体テスト基盤にはVitestを採用済み。
- ライセンス表記と旧教材コードの再利用範囲。

---

## 18. 実装優先順位

### 18.1 合意済み要件に基づく優先順位

| 優先度 | 実装対象 | 根拠 |
|---|---|---|
| P0 | 共有可能な状態モデルと URL 往復の最小プロトタイプ | 中核仕様を後付けにしないため |
| P0 | 2D ベクトル表示 | 旧教材の主要目的 |
| P0 | 3D ベクトル表示 | 旧教材の主要目的 |
| P0 | span 表示 | 旧教材の主要目的 |
| P0 | 一次独立・一次従属を理解できる最小表示 | 旧教材の主要目的 |
| P0 | 2D一次結合エクスプローラ | 生成する空間と一次結合の存在・一意性を結び付けてから3Dへ拡張するため |
| P0 | 学内サーバーから URL で開ける静的成果物または同等の配布物 | 利用方法の必須要件 |
| P1 | URL/QR を使った授業スライドからの実機確認 | 実運用の受入確認 |
| P2 | 授業内容に対応する追加 Lab | 採否・優先順とも未決定 |

### 18.2 会話中に提示された追加機能の優先度案

以下は以前の提案であり、正式な優先順位ではない。

- P0 案: 共通 2D/3D UI、ベクトル drag + span、一次独立 + rank、基底変更 + 座標
- P1 案: 多項式、線形写像、固有ベクトル・対角化
- P2 案: 集合写像、Gram–Schmidt
- P3 案: Cayley–Hamilton

基底以降を初版へ含める決定はまだない。

---

## 19. Acceptance Criteria

### AC-01: ブラウザからの利用開始

```gherkin
Given 学生が対応ブラウザを利用している
When 教員が提示した教材 URL を開く
Then MATLAB の起動、コード入力、教材ファイルの手動準備なしで教材画面が表示される
```

### AC-02: 2D ベクトル表示

```gherkin
Given 有効な 2 次元ベクトルが複数設定されている
When 2D 表示を開く
Then 各ベクトルが 2 次元座標空間上に表示され、互いを識別できる
```

### AC-03: 3D ベクトル表示

```gherkin
Given 有効な 3 次元ベクトルが複数設定されている
When 3D 表示を開く
Then 各ベクトルが 3 次元座標空間上に表示され、互いを識別できる
```

### AC-04: span 表示

```gherkin
Given 2D または 3D のベクトル集合が表示されている
And span の対象集合が指定されている
When span 表示を有効にする
Then 指定集合が張る部分空間と数学的に一致する表示が現れる
```

確認例:

- 一つの非零ベクトルの span は、そのベクトルを通る原点中心の直線となる。
- 2D の平行でない二ベクトルの span は 2 次元空間となる。
- 3D の独立な二ベクトルの span は原点を通る平面となる。
- 3D の独立な三ベクトルの span は 3 次元空間全体となる。
- 空集合または選択集合の rank が 0 の場合は、原点の二重マーカーと零部分空間の説明を表示する。

### AC-05: 一次独立・一次従属

```gherkin
Given 表示または選択されたベクトル集合がある
When 一次独立な集合と一次従属な集合をそれぞれ表示する
Then 学生が両者と span の次元の違いを区別できる情報が示される
```

可視化に加え、日本語主体の短い判定文、rank、span の次元を対応付けて表示する。数学的判定が正しく、視覚表示と矛盾しないこと。

### AC-06: 状態 URL の生成

```gherkin
Given 教材として意味のある状態が画面に設定されている
When その状態を URL としてエクスポートする
Then 一般的なスライドまたはメッセージへ貼り付け可能な URL が得られる
And URLを選択、クリップボードへコピー、またはURLだけのテキストファイルとして保存できる
And エクスポートによって閲覧中URL、ブラウザ履歴、現在の手動表示範囲は変更されない
```

未確定の不正な座標文字列がある間はエクスポートを無効にする。コピーに失敗してもURL欄とテキスト保存を利用できる。

### AC-07: 状態 URL の往復再現

```gherkin
Given 教材状態 A から共有 URL を生成した
When 別の新しいブラウザセッションでその URL を開く
Then URL の共有対象として定義された全項目が状態 A と等価に復元される
And 復元後の状態を操作できる
And 2D表示範囲は復元されたベクトルから自動計算される
```

等価性は見た目だけでなく、共有対象となる数学的状態の構造比較で検証する。

### AC-08: URL の決定性

```gherkin
Given 同じ共有対象状態 A がある
When 同じバージョンのエンコーダーで URL 状態を複数回生成する
Then デコード結果は毎回状態 A と等価になる
```

同じ検証済み状態と同じバージョンからは、同じエンコード文字列を生成する。

### AC-09: 不正な共有 URL

```gherkin
Given 壊れた、欠損した、または許容上限を超える共有状態を含む URL がある
When その URL を開く
Then 任意コードは実行されない
And アプリ全体が未処理例外で停止しない
And 利用者に復元できなかったことが示される
And 既定例をInitialStateとして操作とResetを継続できる
```

### AC-10: QR コード運用

```gherkin
Given 教員が共有 URL を QR コードとして授業スライドに掲載している
When 学生が QR コードを読み取る
Then 対象ブラウザで対応する教材状態が開く
```

アプリ内 QR 生成機能はこの Acceptance Criteria の必須条件ではない。

### AC-11: 旧 MATLAB 操作への非依存

```gherkin
Given 学生が MATLAB をインストールしていない
When 必須スコープの全機能を利用する
Then MATLAB の配列記法、関数名、コマンド入力を要求されない
```

### AC-12: 数学ロジックと URL 復元のテスト

- span の次元と一次独立判定について、代表例と境界例の自動テストがある。
- `decode(encode(state))` が定義された等価性を満たす自動テストがある。
- 不正 URL の検証テストがある。

### AC-13: 2D 表示範囲

```gherkin
Given 2D 座標面に複数のベクトルが設定されている
When 数値入力または直接操作によってベクトルが現在の表示範囲を超える
Then 原則として原点と全ベクトルの終点を観察できる範囲へ自動的に調整される
And 利用者はマウスホイール、ピンチ、または画面上の操作で表示範囲を手動調整できる
```

D-014・D-017 の切替規則を用い、自動調整の余白は15%、正方形表示の半幅は `0.1` 以上 `2,000,000` 以下、中心座標の絶対値は `2,000,000` 以下とする。ホイール、ピンチ、パン、画面ボタンと「全体を表示」の操作は利用者確認済みであり、端末固有の問題が見つかった場合に再調整する。

### AC-14: 教科書に準拠した数学表記

```gherkin
Given 学生向け画面にベクトル、行列、集合、rank、dim、一次独立、生成する空間が表示される
When 数学的なラベル、説明、例を読む
Then 用語と数式表記が math-writing-rules.txt の対応規則と一致する
And rank と dim の対象、列ベクトル、集合と組を区別できる情報が示される
```

アクセシブルな名前は見た目だけに依存せず、記号が表す数学的対象を文章でも確認できるようにする。

### AC-15: 2D ベクトルの直接操作

```gherkin
Given 2D 座標面にベクトルと対応する数値入力・解析結果が表示されている
When 利用者がマウス、ペン、またはタッチでベクトル先端をドラッグする
Then ベクトルの座標、数値入力、描画、rank、生成する空間の次元、一次独立・一次従属の判定が同期して更新される
And 背景のパン操作とは競合せず、自動表示と手動表示の規則が維持される
And 他の非零ベクトルが張る直線へ画面上で一定の距離まで近づくと、完全に平行な位置へスナップして一次従属になる
```

キーボード利用者は、同じ数学状態を数値入力欄から変更できる。平行スナップの操作閾値は D-009 の数学的な rank 判定閾値とは別に管理する。

### AC-16: 選択集合が生成する空間

```gherkin
Given 2D 座標面に複数のベクトルが表示されている
When 利用者が生成する空間の対象となるベクトルをチェック操作で変更する
Then 選択集合 S、行列 A、rank(A)、dim(span(S))、一次独立・一次従属の説明が同期して更新される
And rank 0 は原点、rank 1 は原点を通る直線、rank 2 は座標平面全体として表示される
And 幾何表示はベクトル色から独立した灰色系で、重なったベクトルを識別できる
And 全ベクトルを列に持つ行列 A の判定とは別のカードとして示される
```

幾何表示は色だけに依存せず、形、線種、パターン、ラベル、説明文を併用する。チェック操作と表示オン・オフはキーボードでも操作できる。

### AC-17: 編集・解析パネルの配置

```gherkin
Given 座標面と複数の編集・解析パネルがある
When PC、タブレット、またはスマートフォン相当の幅で教材を表示する
Then 選択集合と全ベクトルの主要な解析結果を常時確認できる
And 詳細は3つのタブから1つを選んで表示できる
And PCでは詳細が長くなっても座標面を参照しやすく、狭い画面では自然な1列配置になる
```

タブはクリック、タッチ、Tabキーによるフォーカス、左右矢印、Home、Endキーで操作できる。

### AC-18: InitialStateへのReset

```gherkin
Given 通常起動または有効な共有URLから2D教材を開き、その後に状態を編集している
When Resetを実行する
Then ベクトル、span選択、幾何表示がページ読み込み時のInitialStateへ戻る
And 一次結合エクスプローラの表示とターゲットもInitialStateへ戻る
And 入力欄は復元値と一致する
And 表示範囲は自動表示、詳細タブはベクトル編集へ戻る
And 閲覧中URLとブラウザ履歴は変更されない
```

### AC-19: ベクトル集合の追加・削除

```gherkin
Given 2D教材で0本以上8本以下のベクトルを表示している
When ベクトルを追加する
Then 上限8本を超えず、既定座標と衝突しないID・表示名を持つベクトルがspan対象として追加される
And ベクトルを削除すると、対応するspan選択と解析表示からも除かれる
And 0本では空集合のrank・生成する空間・一次独立の結果が表示される
And 追加・削除後の状態を共有URLで復元でき、ResetでInitialStateの本数へ戻る
```

### AC-20: 入力案内と2D教材シナリオ

```gherkin
Given 2D教材で成分を編集中、または代表例の共有URLを開いている
When 成分が未確定になる
Then 該当する入力欄に理由が対応付けられ、数学状態には直前の有効値が維持される
And 未確定数と最初の該当欄へ移動する操作が示され、訂正までエクスポートできない
When 空集合、零ベクトル、平行な2本、一次独立な2本、3本以上の一次従属の共有URLを開く
Then 文書化されたrank、生成する空間の次元、一次独立・一次従属と一致する
And 編集後の共有URLを別セッションで復元でき、Resetで配布時のInitialStateへ戻る
```

### AC-21: モバイル座標面のジェスチャー

```gherkin
Given タッチ端末で2D座標面を表示している
When 座標面の内側で矢印先端ドラッグ、1本指パン、または2本指ピンチを開始する
Then 操作終了まで座標面の操作として継続し、ページスクロールまたはページ全体のズームへ途中で移行しない
And 座標面の外側ではページスクロールとページ全体のズームを利用できる
```

### AC-22: 生成する空間とベクトルの色分離

```gherkin
Given 2D座標面に複数色のベクトルと生成する空間を表示している
When rank 0、rank 1、またはrank 2の幾何表示を確認する
Then 生成する空間はベクトルの8色とは独立した灰色系で表示される
And rank 1の破線上およびrank 2の背景上でも各ベクトルの色・矢印・ラベルを識別できる
And 形、線種、斜線、ラベル、説明文による色以外の識別も維持される
```

### AC-23: 一次結合の求解

```gherkin
Given 0〜8本の選択ベクトルと2Dまたは3Dのターゲットベクトル v がある
When 選択ベクトルを列に持つ行列 A について Ac = v を解析する
Then rank(A) と rank([A | v]) により表現不可能、一意、無数のいずれかに分類される
And 表現可能な場合はターゲットを再構成できる係数例が返る
And 無数の場合は異なる2例、自由係数の数、一般解を構成する零空間基底が返る
And D-009と同じ相対許容誤差を指定できる
```

### AC-24: 一次結合ターゲットの共有状態v2

```gherkin
Given ターゲットを持たない既存v1共有URL、またはターゲットとエクスプローラ表示を持つv2共有URLがある
When 共有URLを読み込む
Then v1はエクスプローラ非表示・ターゲット未配置を補ったv2へ移行される
And v2は次元に一致するターゲット、span選択、教材表示をInitialStateとして復元する
And 編集後にResetするとターゲットとエクスプローラ表示を含むInitialStateへ戻る
And 新しくエクスポートするURLは決定的なv2となる
And 未知フィールド、不正な表示値、次元不一致・非有限・上限超過のターゲットは安全に拒否される
```

AC-24のv1移行は現行実装の確認項目であり、D-032により正式リリース前の互換性保証条件とはしない。

### AC-25: 2D一次結合ターゲットの操作

```gherkin
Given 2D座標面で一次結合を調べるモードを開いている
When 背景を短くクリックまたはタップする
Then その座標を指すターゲットベクトル v が配置され、成分入力欄と同期する
When v の先端をドラッグする、またはキーボードで2成分を入力する
Then v の座標、表示、自動表示範囲、共有状態が同期する
And rank 0では原点、rank 1では選択集合が生成する直線へ画面幅1%以内の直接操作だけが吸着する
And rank 2または数値入力では吸着による座標変更を行わない
And 背景の一定距離以上のドラッグはパン、2本指操作はピンチとなり、ターゲット配置と競合しない
And 未確定入力では直前の有効ターゲットを維持し、訂正までエクスポートを停止する
```

### AC-26: 2D一次結合の係数・一般解・幾何表示

```gherkin
Given 2D座標面でターゲット v とspan対象の選択集合 S が定まっている
When 一次結合の解析結果を表示する
Then Ac = v を不能・唯一解・不定のいずれかに分類し、日本語の意味と2つのrankを併記する
And 表現可能なら有効数字6桁の係数例を示し、丸めがある式だけ近似記号を使う
And 不定なら異なる2例を既定で示し、自由係数による一般解だけを折り畳んで示す
And 一般解には特解・同次解方向の導出理由と、各係数を展開した一次結合を示す
And 2本が一次独立なら一般の {}^t[a, b] に対する係数公式を既定で折り畳んで示す
And 選択がちょうど2本で表現可能なら c₁a₁ と c₂a₂ を原点からの2辺とする平行四辺形を描く
And 2本が一次従属なら退化した平行四辺形として説明し、3本以上なら平行四辺形を描かない
```

### AC-27: 2D一次結合エクスプローラの統合

```gherkin
Given ターゲットを含む2D一次結合の共有URLをPC、タブレット、またはスマートフォンで開く
When ターゲット、選択集合、ベクトルを操作し、共有URLを再生成してResetする
Then 一次結合モード、ターゲット、選択集合、ベクトルが共有・復元される
And Resetで配布されたInitialStateへ戻る
And 座標面の直接操作、ページ操作、キーボード操作、読み上げ名が端末幅によらず利用できる
And 1本、一次従属な2本、一次独立な2本、3本以上の代表例で不能・唯一解・不定を比較できる
```

### AC-28: 共有URLのQRコード

```gherkin
Given 有効な教材状態で共有URLダイアログを開く
When 共有URLが生成される
Then URL欄の上に同じURLを表す余白付きQRコードが表示される
And QR生成のために共有URLを外部サービスへ送信しない
And 「QRコードを保存」は「テキストで保存」の左隣にあり、PNG画像を時刻付きファイル名で保存する
And QR画像には代替テキストがあり、生成中と失敗は読み上げ可能な状態通知となる
And QR生成に失敗してもURLの手動コピー、クリップボードへのコピー、テキスト保存、閉じる操作を利用できる
And 640px以下では4操作が1列となり、ダイアログ内だけをスクロールして全内容へ到達できる
```

### AC-29: 2D/3D次元切替と状態境界

```gherkin
Given 2Dと3Dにそれぞれ専用のInitialStateとCurrentStateがある
When セグメント型タブをマウス、タッチ、またはキーボードで切り替える
Then 選択した次元の教材パネルが表示され、他方のCurrentStateと一時UIは変更されない
And 切替を往復しても各次元の状態が維持され、座標の暗黙変換は行われない
And 2D共有URLは2D、3D共有URLは3Dを初期選択し、エクスポートは現在の次元だけを含む
And Resetは現在の次元だけを同次元のInitialStateへ戻す
And 640px以下でも2Dと3Dの2択を同時に操作できる
```

### AC-30: 固定3D表示とカメラ操作

```gherkin
Given 3Dタブを初めて開く
When Three.jsの描画モジュールが読み込まれる
Then 右手座標系のx・y・z軸、xy平面の補助格子、原点、固定3Dベクトル、矢じり、数式規則に沿うラベルが正投影で表示される
And 初期視点ではx正方向が右、y正方向が右上（奥）、z正方向が上に見える
And マウスまたはタッチで回転・拡大縮小・パンでき、正面・右・上・等角・全体表示をボタンで選べる
And 3Dタブと2Dタブを往復してもカメラ状態が維持される
And 操作していない間は連続描画せず、リサイズと高DPI表示へ追従する
And WebGLを初期化できない、またはcontextを失った場合は、ページを停止せず案内を表示する
```

### AC-31: 3D数値編集・解析・カメラ共有

```gherkin
Given 3Dタブを開いている
When 0〜8本のベクトルのx・y・z成分、追加・削除、span選択を変更する
Then 固定3D表示、8色、自動表示範囲、rank(A)、dim(span(S))、一次独立・一次従属が同じCurrentStateから更新される
And 入力途中の不正な文字列は直前の有効値を維持し、その次元の共有URLエクスポートを停止する

Given 3Dベクトルとカメラを操作した状態がある
When 共有URLまたはQRコードを生成して別セッションで開く
Then 共有状態v3からベクトル、span選択、カメラの向き・注視点・上方向・拡大率が復元される
And Resetは共有時の3Dベクトルとカメラへ戻す
And v1・v2 URLはカメラ未指定のv3へ移行し、既定等角視点で開く
And 不正・非有限・上限外・退化したカメラ状態は描画前に拒否される
```

### AC-32: 3Dで生成する空間の幾何表示

```gherkin
Given 3Dでspanの対象集合と幾何表示オンを選択している
When 選択集合のrankが0、1、2、3のいずれかになる
Then rank 0は原点強調、rank 1は原点を通る太い破線、rank 2は格子なし・両面の半透明平面、rank 3は薄い半透明グレーの空間と実線の外枠Boxで表示される
And 幾何形状とラベルは中立的な灰色系を使い、選択ベクトル、軸、矢じり、数式ラベルを隠しにくい
And rank 2を真横から見ても平面の輪郭、座標空間内ラベル、解析カードにより原点を通る平面だと識別できる
And 幾何表示をオフにするとspan形状と未選択ベクトルの減光だけが消え、選択集合とrank・次元の解析結果は維持される
And ベクトルまたは表示範囲を変更すると、直線、平面、半透明空間と外枠Boxの有限表示範囲が安全に再計算される
```

### AC-33: 3D一次結合エクスプローラ

```gherkin
Given 3D座標空間で一次結合を調べるモードを開いている
When ターゲットvのx・y・z成分を数値入力するか、ターゲットを消去する
Then Canvasクリックへ依存せずターゲット状態が更新される
And 選択集合に対するAc = vが不能、唯一解、不定のいずれかに分類され、2Dと共通の係数・一般解表示が使われる
And 選択が2本で表現可能なら原点基準の平行四辺形、3本で表現可能なら第1係数例による平行六面体が表示される
And 係数を乗じた辺が一次従属なら退化形として説明され、4本以上では数式表示だけになる
And 通常ベクトル名と係数付き寄与ベクトル名は矢じりを覆わず、同じ終点では反対側へ分かれ、半透明背景を通して背後の図形を識別できる
And ターゲットと幾何頂点は全体表示の範囲へ含まれる
And 共有URLとResetは一次結合モード、ターゲット、選択集合、ベクトル、3Dカメラを復元する
And ターゲットに入力途中の不正な文字列があれば直前の有効値を維持し、3D共有URLのエクスポートを停止する
```

### AC-34: 3D軸拘束ベクトル編集の試作

```gherkin
Given 3D座標空間に1本以上のベクトルが表示されている
When 「ベクトルを操作」を選び、対象ベクトルのx・y・zハンドルの一つをマウス、指、またはペンでドラッグする
Then 選んだ軸の成分だけが投影軸方向の移動量に応じて変更され、他の2成分は維持される
And ドラッグ中は変更後の矢印をプレビューし、終了時に数値入力、span、rank、一次結合の解析へ同じ値が反映される
And 「視点を操作」では従来の回転・拡大縮小・パンが使え、「ベクトルを操作」では背景ドラッグによってカメラが動かない
And 軸が画面の奥行き方向を向き操作しにくい場合は視点プリセットの変更を案内する
And 数値入力と各成分の±0.1ボタンをキーボードで利用でき、座標は共有状態と同じ安全上限内に収まる
And 操作モードと選択ベクトルは共有URLへ追加されず、Resetで視点操作とInitialStateの先頭ベクトル選択へ戻る
```

---

## 20. Codex が実装時に守るべき制約

1. 本書の「決定済み」と「未決定」を混同しない。
2. 未決定の Lab、追加ライブラリ、UI をユーザー確認なしに本実装へ固定しない。
3. 状態共有を後付け機能にしない。数学状態と可視化状態を、URL に安全に直列化できる設計から始める。
4. 描画オブジェクトを唯一の状態源にしない。状態から画面を再構築可能にする。
5. 数学ロジックを描画・UI から分離し、単体テスト可能にする。
6. URL からの入力を信頼しない。検証せずに描画、HTML 挿入、コード実行へ渡さない。
7. MATLAB の記法や関数呼び出しを Web UI にそのまま持ち込まない。
8. 学生にプログラミング知識を要求しない。
9. URL で再現する対象を変更した場合、Acceptance Criteria と状態スキーマを同時に更新する。
10. 既存のユーザー成果物やリポジトリ構成がある場合は、調査せずに上書き・再構成しない。
11. 実装開始前に「17. 未決定事項」のうち、選択によってアーキテクチャが変わる項目を確認する。
12. 提案機能を実験的に試作する場合は、必須スコープと明確に分離し、採用済みと記載しない。

---

## 21. 実装開始前の確認ゲート

2026-08-19 に D-001〜D-008 が承認され、初期実装を開始するための次の項目は決定済みとなった。

1. 初版は旧教材相当の中核機能に限定する。
2. 2D は数値入力と先端ドラッグを採用する。3Dは数値入力を先行し、操作モードと軸拘束ハンドルによる直接編集を試作後に最終判断する。
3. span 選択、rank、span の次元、一次独立・一次従属を対応付けて表示する。
4. 共有 URL の最低限の状態一覧を定義した。
5. バージョン付き JSON と Base64URL を採用する。
6. TypeScript、Vite、React の静的 Web アプリとする。
7. PC、タブレット、スマートフォンと現行主要ブラウザを対象とする。

URL 長、3Dカメラ共有スキーマの具体形、学内サーバーの詳細条件等は、それぞれの実装・運用フェーズの確認ゲートとして残す。

---

## 22. 参照情報

- 旧 MATLAB 教材: [d-kitamura/plot2d_plot3d](https://github.com/d-kitamura/plot2d_plot3d)
- 設計会話: 「教材設計方針」
- 添付授業内容: ベクトル空間、部分空間、span、一次独立、rank、基底、次元、線形写像、核・像、表現行列、固有値・固有空間、対角化、Cayley–Hamilton、内積、Gram–Schmidt 等

本書は会話で合意された内容を優先して整理したものである。提案段階の内容は、明示的に「未決定」または「未決定（提案あり）」とした。

---

## 23. 変更履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| 0.1 | 2026-08-19 | 要件整理版を作成 |
| 0.2 | 2026-08-19 | `syllabus.txt` と照合し、第 8 週を中間試験として反映。ロードマップ策定時レビューを記録 |
| 0.3 | 2026-08-19 | D-001〜D-008 の承認を反映し、初版範囲、技術構成、操作、表示、共有 URL、Reset、対応環境を決定済みへ更新 |
| 0.4 | 2026-08-19 | D-009 と数学ロジック初版を反映し、rank 判定の正規化、許容誤差、空集合・不正値の扱いを記録 |
| 0.5 | 2026-08-19 | D-010 と共有状態 v1 を反映し、厳密なスキーマ、Base64URL 往復、安全上限、エラー結果を記録。フェーズ 1 を完了 |
| 0.6 | 2026-08-19 | D-009・D-010 の利用者確認を記録。D-011 として GitHub Pages／nginx の公開候補、ベースパス、URL 長の制約を追記 |
| 0.7 | 2026-08-19 | フェーズ 2.1 の固定 2D 表示を実装し、D-012 として初期例・表示範囲・識別方法を試行採用として記録 |
| 0.8 | 2026-08-19 | D-012 の利用者確認を記録。D-013 として 2D の自動スケール、ホイール／ピンチによる手動ズームを決定済みにし、D-014 と 2.2〜2.7 の分割案を追加 |
| 0.9 | 2026-08-19 | D-014 の利用者確認を記録。D-015 として `math-writing-rules.txt` を学生向け用語・数式表記の基準とし、フェーズ 2 に関係する規則と AC-14 を追加 |
| 0.10 | 2026-08-19 | D-015 の利用者確認と作業単位 2.2 の完了を記録。D-016 として数値入力、自動表示範囲、適応目盛、初期の数式フォント方針を追加 |
| 0.11 | 2026-08-19 | D-015 の補足として、ベクトル記号の基本文字だけをボールド斜体、添え字数字を立体レギュラーとする規則を明記し、HTML・SVG 表示へ反映 |
| 0.12 | 2026-08-19 | D-016 の利用者確認と作業単位 2.3 の完了を記録。D-017 としてホイール・ピンチ・パン・画面ボタン、自動／手動表示、安全範囲の初期値を追加 |
| 0.13 | 2026-08-19 | D-017 の利用者確認と作業単位 2.4 の実装を記録。D-018 として先端の操作領域、ドラッグ座標の丸め、安全上限、自動／手動表示との連携を追加 |
| 0.14 | 2026-08-19 | D-018 の操作確認を反映。D-009 の `1e-10` を維持したまま、ドラッグ時だけ正規化行列式 `1e-2` で完全な平行へ吸着する D-019 を追加 |
| 0.15 | 2026-08-19 | D-019 の操作確認を反映。角度基準の吸着を、基準範囲で距離 `0.1`、任意の表示倍率で画面幅 `1%` となる画面距離基準へ変更。現時点ではヒステリシスを採用しない |
| 0.16 | 2026-08-19 | D-019 の利用者確認と作業単位 2.5 の実装を記録。D-020 としてspan選択、rank 0・1・2の幾何表現、選択集合と全ベクトルの判定分離、初期表示を追加 |
| 0.17 | 2026-08-19 | D-020 と作業単位 2.5 の利用者確認を記録。座標面内の幾何ラベル枠を文字列長に応じた可変幅へ修正 |
| 0.18 | 2026-08-19 | D-021として、常時表示の2要約、3つの詳細タブ、条件付きsticky座標面、タブレット・スマートフォンの1列配置を追加 |
| 0.19 | 2026-08-19 | D-021 の利用者確認と2.6着手前の棚卸しを反映。節番号、確定済み状態モデル、ディレクトリ構成、AC-04・AC-08・AC-13、テスト基盤の記述を現行実装へ整合 |
| 0.20 | 2026-08-19 | 作業単位2.6を実装。D-022〜D-024として共有URLダイアログ、v1を維持して手動表示範囲を共有しない方針、`state`読込・不正URLフォールバック・InitialStateへのResetを追加。AC-06・AC-07・AC-09を具体化しAC-18を追加 |
| 0.21 | 2026-08-19 | D-022〜D-024の利用者確認と作業単位2.7の統合棚卸しを反映。D-009・D-012〜D-014を現状維持とし、空のベクトル集合の説明・ARIA表現とReset時の一時的なエクスポート状態を整合させ、フェーズ2を完了 |
| 0.22 | 2026-08-19 | D-025としてGitHub Pagesを初回公開先に選定。mainへのpushを起点にpnpmでテスト・Viteビルドし、公開サブパスを設定した `dist/` をGitHub Actionsからデプロイする方針と、現在の番号ではフェーズ3前へ前倒しした作業単位6.1を追加 |
| 0.23 | 2026-08-19 | 現在の作業単位6.1のGitHub Actionsワークフローと公開手順書を追加。main限定、pnpm 11・Node.js 24、テスト成功後の公開用ビルド、`dist/`だけのPages artifact、最小権限、手動再実行を設定し、公開サブパスでのローカル静的配信を検証 |
| 0.24 | 2026-08-19 | D-025のGitHub Pages初回公開と本番操作確認の完了を記録。フェーズ3へ移行し、D-026として0〜8本の追加・削除、既定座標、span選択、自動命名、表示順パレット、共有状態v1・Resetとの整合を決定して作業単位3.1を実装。AC-19を追加 |
| 0.25 | 2026-08-19 | D-026と3.1のデプロイ先確認完了を記録。D-027として未確定入力・共有URLエラーの案内、スキップリンク、数式読み上げ、タッチ領域、5つの2D授業シナリオ、共有URLによる授業手順、3Dへの再利用境界を決定して3.2・3.3を実装。AC-20を追加 |
| 0.26 | 2026-08-19 | スマートフォンで座標面のドラッグ・ピンチがページ操作へ移行する問題を記録。D-028としてSVG座標面全体へモバイルジェスチャーの所有範囲を広げ、グラフ外のページスクロール・ページズームを維持する方針とAC-21を追加 |
| 0.27 | 2026-08-19 | D-028のスマートフォン実機確認完了を記録。D-029として生成する空間のrank 0・1・2と座標面内ラベルをベクトル色から独立した灰色系へ変更し、AC-16を補足してAC-22を追加 |
| 0.28 | 2026-08-19 | D-027・D-029の利用者確認とフェーズ3終了時の統合棚卸しを記録。フェーズ3を完了し、D-030としてCodexが検証後のmainへのcommit・pushとPagesデプロイ確認まで担当する日常運用へ変更 |
| 0.29 | 2026-08-19 | D-031として3D前にフェーズ4「2D一次結合エクスプローラ」を挿入し、従来のフェーズ4以降を5〜8へ変更。ターゲット共有、Reset、小数係数、折り畳み一般式、画面幅1%吸着、原点基準の平行四辺形、無数解の2例と一般解を決定し、4.1の2D/3D共通求解ロジックとAC-23を追加 |
| 0.30 | 2026-08-19 | 作業単位4.2を実装。共有状態v2にエクスプローラ表示とターゲットを追加し、厳密なv1読込・v2移行、現行v2でのエクスポート、InitialState・Resetとの接続、安全検証、AC-24を追加 |
| 0.31 | 2026-08-20 | 作業単位4.3を実装。ターゲットのクリック・タップ配置、先端ドラッグ、数値入力、未配置、表示中ターゲットを含む自動範囲、rank 0・1への画面幅1%吸着、パン・ピンチとの競合分離、AC-25を追加。D-032として正式リリース前のURL互換性を保証せず、現行v1読込がv2 URL長へ影響しないことを記録 |
| 0.32 | 2026-08-20 | 4.3の利用者確認を完了し、作業単位4.4を実装。不能・唯一解・不定、係数例、第2例、自由係数による一般解、一般の `[a; b]` の係数公式、原点基準の平行四辺形、AC-26を追加。D-033として有効数字6桁と等号・近似記号、2本だけの幾何表示を決定 |
| 0.33 | 2026-08-20 | D-034として通常ベクトル `a₁`〜`a₈`、選択集合 `S`、行列 `A`、ターゲット `v` へ記号を統一。一次結合の主式・係数列・転置横書き表記を教科書へ合わせ、不定の2例を常時表示し、一般解に特解・同次解方向の導出説明と展開した一次結合を追加。390px幅の折返し条件とAC-26を更新 |
| 0.34 | 2026-08-20 | D-034の表示確認を反映。主式・係数列・係数転置表示の色・フォント・文字サイズを統一し、一般ターゲット公式の右端を縦の列ベクトル、逆行列係数を上下分数へ変更 |
| 0.35 | 2026-08-20 | 4.4の利用者確認と4.5の統合確認を完了。一次結合の1本・一次従属な2本・一次独立な2本・3本の共有可能な代表例、AC-27、2D/3D再利用境界、フェーズ4終了棚卸しを追加 |
| 0.36 | 2026-08-20 | D-036・追加作業4.6として共有URLダイアログへクライアント内QRコード生成、PNG保存、生成状態・失敗案内、4操作のレスポンシブ配置を追加し、AC-28を追加 |
| 0.37 | 2026-08-20 | D-036と4.6の公開版利用者確認を完了。フェーズ5の実装前レビューに合わせ、2D/3D切替、状態分離、カメラ共有、3D直接編集等の設計境界が未承認であることを現在地へ反映 |
| 0.38 | 2026-08-20 | D-037としてフェーズ5の設計境界を承認。右手座標系、z軸上向きの正投影、カメラ共有、数値編集先行、軸拘束直接編集、一次結合幾何を決定し、5.1の次元タブ・独立状態・3D共有URL初期選択・次元別ResetとAC-29を追加 |
| 0.39 | 2026-08-24 | 5.1の利用者確認を完了。D-038としてThree.jsによる固定3Dベクトル、右手座標系、補助格子、正投影カメラ、回転・ズーム・パン、4視点・全体表示、オンデマンド描画、遅延ロード、WebGL失敗・破棄処理とAC-30を追加 |
| 0.40 | 2026-08-24 | 5.2の利用者確認を完了。D-039として0〜8本の3D数値・集合編集、rank・span解析、自動3D範囲、共有状態v3の方向・注視点・上方向・拡大率、v1・v2移行、共有URL・QR・Reset復元とAC-31を追加 |
| 0.41 | 2026-08-24 | 5.3の共有状態確認と立方体アイコン修正を完了。D-040としてrank 0の原点強調、rank 1の太い破線、rank 2の両面半透明平面、rank 3の空間格子・立方体境界、表示切替、未選択ベクトル減光とAC-32を追加 |
| 0.42 | 2026-08-24 | 5.4の利用者確認を受け、rank 2のspan専用格子を削除。rank 3をspan専用格子・破線境界から、薄い半透明グレーの空間と3D座標軸全体を示す実線の外枠Boxへ変更 |
| 0.43 | 2026-08-24 | 5.4の利用者確認を完了。D-041として3Dターゲットの数値入力、2D共通の一次結合解析、2項の平行四辺形、3項第1係数例の試行的な平行六面体、4本以上の数式限定表示、共有・ResetとAC-33を追加 |
| 0.44 | 2026-08-24 | 5.5確認時の指摘を受け、3Dの通常ベクトル名と係数付き寄与ベクトル名を矢じりの外側かつ同じ終点では反対側へ配置し、背景を半透明化して縁取り・文字影で可読性を保つ条件をD-041とAC-33へ追加 |
| 0.45 | 2026-08-24 | 5.5の利用者確認を完了。D-042として視点／ベクトル操作モード、選択ベクトルのx・y・z軸拘束ハンドル、ドラッグプレビュー、±0.1キーボード代替、一時UI状態とAC-34を追加し、5.6を試作実装 |
